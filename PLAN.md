# KRTaker — Project Plan
**Key Responsibility Taker** — AI-Driven Autonomous Property Management Platform (Bangladesh SaaS)

Status: 🚀 Project start · Reference: REM ERP (design/code/styling) · Date: 2026-08-01
**Decisions:** AI provider = **DeepSeek** (Phase 5) · Repo = `kabirswe/KRTaker` (private; move to `deshiklab` when org access available)

## Phase Log
- **V2.0.5 ✅ (2026-08-11, branch `superadmin-panel`, commit `f04943d`)** — **Auto-invoice generator + monthly cron** (user: "Start next big update" — queued item #1). **Backend** (`app-invoice-auto` in 064_router.php, after app-invoice-email): service-key gated (cron) OR superadmin/owner/manager/accountant via UI. Creates one **Unpaid** rent invoice per **Active** lease for a month — idempotent (leases already billed for that month skipped), outside lease term excluded (`substr(start,0,7) <= m <= substr(end,0,7)`), optional `sub_email` scope for per-subscriber runs. Dry-run by default; `commit=1` writes (`invoice_next_id`, gross=net=rent, tds=0, status Unpaid); `send=1` also **queues tenant invoice email** via `send_mail(..., null, true)` → mail_queue (respects `mail_switch('docs')` + `notify_ok(notify_docs)` — identical gates to app-invoice-email; `email_render('invoice')` template + pay_url deep link). Stamps `platform_meta.last_invoice_auto` + audit "Auto-invoice run". **Frontend** (`InvoicesView.vue`): **⚡ Auto-generate** button in page head (canManage) → modal with billing-month picker (default current), Email-tenants checkbox, **live dry-run preview** (new/already-billed/outside-term counts + total rent + per-lease list), Generate disabled when nothing new (`!autoPreview?.created`), toast summary ("⚡ Created N invoice(s) for Aug 26 · M already billed · 📧 K emailed") + `data.bootstrap()` refresh. **Cron**: `krtaker_auto_invoice.py` (no_agent script, job **9c5d0e4f56b0**, monthly **1st 00:10 UTC**, delivers to origin) → POST `app-invoice-auto {commit:1,send:1}` with `X-Service-Key`; silent when all billed, summary when created, alert on error. **Verified live**: dry-run 2026-08 → 9 eligible / 5 billed / 3 outside term / 1 gap (L-006 Tanvir Hossain ৳2,10,000); commit created **INV-2026-0023**; re-run idempotent (0 new / 6 skipped); send=1 no-op safe; UI modal + preview + disabled state + 0 JS errors (owner live login owner@krtaker.com). Backup `krtaker_full_git_f04943d_20260811_1500.tar.gz` (4.5MB/510 files).
- **V2.0.4 ✅ (2026-08-11, branch `superadmin-panel`, commit `1ef7399`)** — **Board report update + print for all reports** (user: "update the Board report · 2026-08, add print options to all reports"). **Backend** (`board_report_md` in 063_lib_misc_063.php): report grew from 4 → **10 sections** — Executive summary (gross/collected/net, collection rate + on-time %, occupancy, arrears, vacancy loss, 12-mo net flow, top risk), P&L (now with Gross column + totals), Cashflow 12-mo (income/expenses/net/expense ratio + best/worst month via array_reduce), Collections (payments/on-time/avg days late/late amount/top method), Occupancy & renewals (per-property table + 90-day lease expiries), Maintenance (open/resolved/avg resolve + oldest open), At-risk tenants table (band/score/overdue/on-time/tickets), Aging, Vacancy loss, 12-month forecast + "Generated <date>" footer. **Frontend** (`AnalyticsView.vue`): board report now renders markdown → HTML via new zero-dep `mdToHtml()` (tables → `table.kr`, h2/h3/h4, ul/li, **bold**, `code`, hr) instead of raw `<pre>`; **🖨 Print button** in the page head (prints the ACTIVE tab: KPI cards + charts + tables) + a dedicated 🖨 Print on the board report; `printReport()` toggles `body.print-analytics` around `window.print()`. **Print CSS** (main.css): hides sidebar/topbar/page-head/head-actions/buttons/toasts; `.stats` → 4-col grid; panels `break-inside: avoid`; `.panel-b`/`.tbl-wrap` overflow visible so full tables/charts print; board-report print typography. **Verified live** (bundle `index-4Hcg_ZI0`, 91/91, API sha): report 10/10 sections, 3 real `kr` tables (P&L 7-col w/ **Total** bold, Occupancy 6-col, At-risk 6-col), print CSS rule live in stylesheet, class toggles + restores, 0 JS errors. Also: `api/data.db` added to .gitignore (local dev DB leaked into `git add -A`). Backup `krtaker_full_git_20260811_1443.tar.gz`.
- **V2.0.3 ✅ (2026-08-11, branch `superadmin-panel`, commit `def0a87`)** — **Analytics old-design polish** (user: "Old design not going well" — 7 sections flagged: P&L, Monthly detail, By property, Oldest open tickets, At-risk tenants, Vacant units, Past reports). **Root cause: all 9 AnalyticsView tables were plain `<table>` with NO `kr` class** → zero styling (15px default font, no sticky header, no padding/borders/hover) while every other view uses `table.kr`. **Frontend**: all 9 tables → `table.kr` (2 inner panel tables use `kr compact`); `.c-name` global style added to main.css (was used in 2 views but never defined); P&L panel gains `<tfoot>` totals row + friendly month label (`shortMonth` → "Aug 26" instead of raw 2026-08); at-risk tenant rows get band badges (`b-red` Risky / `b-orange` Fair / `b-blue` Good / `b-green` Excellent) + overdue red highlight (`—` when none); oldest-open-tickets rows get status + priority badges; expenses by_property falls back `p.name || p.prop`. **Backend** (`063_lib_misc_063.php`): `analytics_expenses` + `analytics_maintenance` `by_property` now `LEFT JOIN properties p ON p.id=m.prop` returning real property names (was raw P-001 IDs); rebuilt api/index.php. **Verified live** (bundle `index-B0vT27Xa`, 91/91, API sha `74cb6519`): P&L styled + totals row `Total ৳6,22,000 / ৳5,21,499 / ৳0 / ৳31,500 / ৳2,500 / ৳4,87,499`; badges render (Open:b-orange, high:b-orange, Risky:b-red, Fair:b-orange); expenses by_property shows "Green View Residency | 1 | 1 | ৳8,500" etc.; vacant units 8 rows; board past reports 9 rows; 0 JS errors. Backup `krtaker_full_git_20260811_1430.tar.gz` (4.5MB/515 files).
- **V2.0.2 ✅ (2026-06-12, branch `superadmin-panel`, commit `e6d643a`)** — **Analytics overhaul** (user: "start big update on Analytics"). **6 new `app-analytics` actions** (063_lib_misc_063.php helpers + router dispatch): `cashflow` (12-mo income=Success payments by month vs expenses=maintenance pay_paid + vendor_payouts Paid + statement_payouts Paid; net + cumulative + expense_ratio), `collections` (by_method mix, on-time vs late using 1st+3-day grace, on_time_rate, avg_days_late, late_amount, by_month issued/collected/rate), `expenses` (total/paid/est-open/avg job, by_category/by_vendor/by_property, 12-mo paid trend), `scores` (tenant_scorecard reused — band distribution Excellent/Good/Fair/Risky, avg score, at-risk list sorted asc), `occupancy` (per-property units/leased/vacant/occupancy/rent_roll/vacancy_loss + portfolio totals + 90-day lease expiries), `maintenance` (by_status/by_priority/by_charge/by_property, total_cost, avg_resolve_days via julianday, oldest-open aging list). **AnalyticsView.vue rewritten**: 10 tabs (Overview/Cashflow/Collections/Expenses/Maintenance/Tenants/Aging/Vacancy/Forecast/Board), new zero-dep SVG chart components `app-v3/src/components/charts/{LineChart,Donut,HBars}.vue` (theme-aware, title tooltips), KPI cards, per-tab ⬇ CSV export (BOM'd), month selector + loading state. **Pitfalls hit**: `??` mixed with `||` needs parens (Vue template parse error); SVG `transform` attr must be `:transform` bound not string-concat; rapid tab clicks race the shared err ref (wait ≥2.5s between tab checks). **Verified**: API actions live ~620ms each with real data (bKash ৳14,04,000/7 · on-time 88% · bands 2/3/4/2 · occupancy P-001 50% · maintenance Closed 5/Open 1); local preview all 10 tabs render + zero JS errors; live bundle `index-Ex1xbB2M.js` byte-identical, live overview KPIs (Gross ৳6,22,000 · Collected ৳5,21,499 · Net ৳4,87,499 · 88% · 50% · arrears ৳2,48,000 · 12-mo net ৳-4,54,201). Deployed: API sha `e2e1fcc6…`, app-v3 91/91.
- **V2.0.1 ✅ (2026-06-12, branch `superadmin-panel`, tag `v2.0.0` baseline)** — **dashboard-v2.html RETIRED** (user: "start for next big update" — the next cadence item). Legacy v2 dashboard replaced with a redirect shim → **everything now runs on app-v3** (`/app-v3/`). **Redirect page** (`docs/dashboard-v2.html`, deployed live): parses `?gw=&sid=` (preserves ALL gateway params incl. val_id/paymentID/status into the hash query), `?inv=&pay=1` (→ `#/invoices?open=X&pay=1`), legacy `#module` hashes (map incl. utilities→utility-bills, trust→compliance, tickets→support) → `location.replace` to app-v3; meta-refresh + fallback link; node-tested 11/11 mappings. **API URL literals repointed** (script `scripts/retire_dash_v2_urls.py`; api/src rebuilt → index.php **0** dashboard-v2 refs): gateway callback `021_lib_finance_2.php` → `/app-v3/#/dashboard?gw=…&sid=…`, invoice pay_url `030_lib_finance_4.php` → `/app-v3/#/invoices?open=…&pay=1`, welcome emails `011_lib_mail_4.php`/`040_lib_misc_040.php`/`064_router.php` workspace_url → `/app-v3/`, push_to_user default + test/rent-due pushes → `/app-v3/`, `#maintenance`/`#invoices` module links → `/app-v3/#/…`. **app-v3 deep-link support**: App.vue gateway-return handler (mirrors v2 `gatewayReturn()` — watches `[validated, route.query.gw, route.query.sid]`, calls `app-payment-confirm`, toasts, strips query; fires once); InvoicesView `pay=1` → auto-opens the payment modal after the `?open=` drawer. **CRITICAL FIX found during testing**: the router guard's login redirect `{name:'login'}` DROPPED the query — a fresh visitor landing on `?gw=&sid=` lost the callback params (URL got "stripped" to plain /dashboard by the login detour). Fix: guard now passes `{name:'login', query:{redirect: to.fullPath}}` and LoginView pushes `route.query.redirect || '/dashboard'` after login — deep links survive auth (also benefits `?open=`). Gateway handler watches the query too (validated flips while still on /login). **Rollback**: repo keeps `docs/dashboard-v2.legacy.html` (original v2 dashboard, 1MB) + workspace `/root/krtaker-deploy/dashboard-v2.html` (old deployed copy); redeploy that file to restore. SW is network-first for navigations → fresh redirect served (offline fallback may show cached v2 shell — acceptable). **Verified**: local preview + live (bundle `index-Bmcdr5ZT.js` byte-identical): live `/dashboard-v2.html#invoices` → `/app-v3/#/invoices`, live `?gw=sslcz&sid=LIVETEST1&val_id=abc123&status=VALID` → `/app-v3/#/dashboard?gw=…&sid=…` all params preserved → login as owner → handler called app-payment-confirm (fake session → error toast) → query stripped; `?open=INV-2026-0015&pay=1` → drawer + `💳 Record payment — INV-2026-0015` modal auto-opened; zero JS errors. Deployed: API (sha256 `7cc6e1fa…`), redirect (2782 chars), app-v3 91/91.
- **V2.0.0 🚀 (2026-06-12, branch `superadmin-panel`, tag `v2.0.0`)** — **New version cycle started** (user: "Git update and start new version"). All remotes synced (207 ahead / 0 behind origin/main), working tree clean at `65d5f57`. **app-v3 package.json bumped `0.0.0` → `2.0.0`** (first real version marker — the Vue 3 app never had one) and tagged `v2.0.0` (prior tag `v1.0.0` = original V1 release `13e096f`). This opens the app-v3 release line: global search (commit `288dc2a`), DMP Thana form print settings fix (`65d5f57`), and all prior app-v3 modernization now roll into v2.0.0 as the baseline. Next cadence item on deck: `dashboard-v2.html` retirement; carried gaps: auto-invoice cron, payment-proof uploads, SMS gateway (backend needed), MySQL conversion, reCAPTCHA, SSLCommerz KYC, OAuth publish.
- **SA1-fullsite-v18 ✅ (2026-08-06, branch `superadmin-panel`, no version bump)** — **Per-subscriber plan limits now ENFORCED at create time** (user: "Start the recommended one" — closes the last open finding from the lifecycle round: property/unit limits were display-only). **`sub_email` scoping**: idempotent ALTER on `properties`/`units` (+ one-time backfill guarded by `platform_meta.sub_email_backfill_v1` → seeded portfolio to `owner@krtaker.com`); `app-crud` create enforcement for subscribers (`kind='sub'`): property count ≥ `property_limit` → **403** "Plan limit reached: N of M properties. Upgrade to lift limits."; unit count ≥ `unit_limit` → 403 "…units…"; unit under another account's property → 403 "This property belongs to another account."; create stamps `sub_email` (client value overwritten), update path strips it (ownership can't be reassigned); staff/superadmin unlimited. **Real bug found during live verification**: live `plan_catalog` rows had `limits='[]'` (empty JSON array) and the P55 seed-endpoint backfill only treated `'{}'`/empty as stale → **per-plan caps never applied on live** (Trial users actually had 9999/99999). Fix: boot schema block now self-heals `modules`/`limits` for starter/business/enterprise when empty/`'{}'`/`'[]'` (verified live: starter 1/5/1, business 10/50/3, enterprise 9999/99999/10). Panel: `saveForm()` now toasts `d.error` (limit/scope messages previously vanished silently). Tests: lifecycle **166/166** (new SC-16: 9 checks — trial 1st prop OK/stamped, 2nd 403, 5 units OK, 6th 403, cross-account 403, Enterprise unaffected), regression **2841/2841**, superadmin **511/511**; live curl-verified end-to-end (prop2 403 "1 of 1", unit6 403 "5 of 5", cross 403, Enterprise create OK; live left pristine 5 props/10 units). Detail: `references/sa1-fullsite-v18.md`.
- **SA1-fullsite-v7 ✅ (2026-08-05, branch `superadmin-panel`, commit `a666e0f`)** — **User Impersonation** (user: "Start new version and start building next version" — top gap from the Ai-Instructions analysis). Superadmin views-as any active subscriber/staff for **30 min** via a temp token; all target permissions apply; red banner always visible. **API**: `app_tokens.impersonator` column (PRAGMA ALTER); `make_token($uid,$kind,$impersonator='',$ttl=TOKEN_TTL)`; `current_user()` SELECT includes t.impersonator (**first cut omitted it → impersonator silently empty until fixed**) + returns impersonator/token_expires; `user_payload()` adds impersonator+imp_expires (drives banner via app-me/bootstrap). **`app-admin impersonate`** (superadmin-only): email 400, self 400, subscribers-first then app_users (404), another superadmin 400, 1800s token, audit "Impersonation started", returns token/expires_at/impersonator/user. **`app-admin impersonations`** trail (last 25 audit rows). **Frontend**: superadmin 🔀 buttons in Subscribers + Users rows (hidden for superadmin role) → `impersonateUser()` stores token under `krtaker_dash_token` → redirect `/dashboard-v2.html`; dashboard red `.imp-banner` (impAs/impBy, dark variant) via `showImpBanner()` in applyLive (NOT demo path); `exitImpersonation()` = app-logout + clear + `/superadmin.html` (admin session survives — separate token key). Version **?v=3.61 / SW v68** (28 pages, `bump_v361.py`). Verified: test_superadmin **294/294** (17 new); regression **2458/2458**; live API **10/10**; live browser full loop (Subscribers 🔀 → confirm → dashboard as owner + banner "viewing Belal Ahmed (owner@krtaker.com) — impersonated by Kabir (Platform)" → exit → back to panel logged in) 0 JS errors. Detail: `references/sa1-fullsite-v7.md`.
- **SA1-fullsite-v6 ✅ (2026-08-05, branch `superadmin-panel`, commit `a919614`+)** — **Web CMS v2 — page dropdown + advanced filter** (user: "in Web CMS, change the features style of each chip… make it drop-down and apply a advanced filter option… to find and modify any features easily. Or change the design and make it more user friendliness."). The 29-page chip row → a single **page dropdown** (`cmsPageSel`, options `🏠 Home · 146` = icon+label+field count, 🔴 for draft pages; `cmsPageOptions`/`cmsPageSelChange`). New **⚙️ advanced filter bar** (`#cmsFilterBar`, `cmsFilterToggle`): Page / Section (`cmsFilterSecs` rebuilds options per page or whole site) / Field type (`cmsFilterTypeOf` = cmsFieldType with text→long) / Status (custom|default via defKeys) / Language (EN|বাংলা via `_bn`). `cmsFilterApply()` filters ALL blocks and renders **editable inline results** grouped by page→section with collapsible section rows — reuses `cmsFieldHtml` so dirty tracking, char counters, media/color/url inputs and 💾 Save all work unchanged; header "N matches across M pages — edit inline"; per-page `↗ page` + per-section `↗ section` (`cmsFilterJump` = clear filter, switch page, scroll to section). Header search (`cmsFilter`) now sets `CMS.flt.q` and applies live (old jump-only results replaced); `cmsClearFilter` resets; `cmsLang` respects active filter. Filter state `CMS.flt{...}/fltOpen/fltN` survives re-renders. Panel-only → **no version bump** (live stays ?v=3.60/SW v67). Verified: test_superadmin **277/277** (5 new guards incl. chips-gone check); regression **2441/2441** (API untouched); live browser — dropdown 30 pages w/ counts, filter Features+EN+custom → 104 matched, search "holding tax" → 3 editable matches, inline edit → Save all (1), clear restores, page switch works, 0 JS errors, live DB untouched (test edit reverted in-memory). Detail: `references/sa1-fullsite-v6.md`.
- **SA1-fullsite-v5 ✅ (2026-08-05, branch `superadmin-panel`, commits `e48aa37` + `75af8d9` + sidebar fix)** — **KR AI Caretaker console** (user: "Next" after v4.4 — closes the loose end where v4.4's `ai_provider`/`ai_model` App Settings fields were cosmetic: `AI_CONFIG()` was hardcoded to env `KRT_DS_KEY`/deepseek-chat). **Settings-driven AI**: `AI_CONFIG($pdo)` resolves key env `KRT_DS_KEY` → `admin_settings.ai_key` → REPLACE_ME (offline); provider/model from `ai_provider`/`ai_model` (App Settings), base URL from `ai_base_url` (**empty → provider default — first cut passed `''` to curl → "URL using bad/illegal format"**); `provider=offline` forces offline; `ai_mode($pdo)`/`ai_llm_call($messages,$pdo)` extended; `app-ai-meta` now returns provider/model. **Telemetry**: `ai_log` gains `ms` (PRAGMA ALTER guard); `app-ai-chat` stamps `$_t0` and logs latency. **`app-admin ai-config`** (superadmin-only): `get` (mode/provider/model/url/key_set/env_key — key NEVER echoed; `settings-get` can't leak it: only default-key rows merge, `ai_key` not in defaults; `settings-reset` clears only defaults so ai_key survives), `save` (provider deepseek|openai|offline else 400, model required non-offline else 400; key blank/`'********'` keeps stored; audited), `test` (offline → `ok:false mode:offline` message; else real `ai_llm_call` "Reply with exactly: OK" → ok/error + ms — invalid key = graceful 401 ~255ms), `stats` (total/llm/offline/h24/avg_ms + top 8 queries + recent 15), `clear-log` (deleted count, audited). **Frontend**: superadmin 🤖 **AI Caretaker** nav (NAV+NAV_DESC+render map) — Engine status badge (🟢 LLM ACTIVE/🟠 OFFLINE), provider config form (select+model+base URL+password key), 💾 Save + 🔌 Test connection (inline result), 📊 Chat telemetry (5 stat cards + recent table + 🧹 Clear log confirm). Version **?v=3.60 / SW v67** (28 pages + sw.js bumped via `bump_v360.py`). Verified rig: **271/271** (21 new guards: RBAC 403, round-trip, key masked/never echoed, graceful test paths, offline forcing, chat→stats→clear, restore + CLEAR sentinel + ai_key scrub); regression **2435/2435** (48 suites). Detail: `references/sa1-fullsite-v5.md`.
- **SA1-fullsite-v4.4 ✅ (2026-08-05, branch `superadmin-panel`, commit `c690371`)** — **Advanced App Settings** (user: "Add more advanced features to the App Settings (Super Admin)"). **21 new enforced settings** in `ADMIN_SETTING_DEFAULTS()` (33 total) across 6 grouped UI panels (🏢 Branding & contact · 📝 Signups & trials · 🔐 Security & access · ✉️ Email & notifications · 🚀 Platform & growth · 🗄️ Retention & limits) + a highlighted 🛑 Maintenance mode panel. **Enforcement wiring**: `password_policy_error()` (min-len + letters+numbers complexity, clamped 4–64) enforced at **register, profile password change, staff create** — defaults stay 6/off so nothing breaks, admin can tighten; `auth_blocked()` now reads `sec_login_attempts`/`sec_lockout_minutes` (clamped 3–100/1–1440); **email master switches** `mail_welcome/rent_reminders/collections/docs/renewal` gate the senders via `mail_switch()` (per-user opt-outs still apply; suppressed responses carry reason "disabled in App Settings"); `mail_from_name` → SMTP From display name (safe, SPF-neutral); `plat_referral_bonus` → KR referral_link text; `trial_days` → register/verify-otp trial_end (clamped 1–365). **New actions**: `settings-reset` (clears ONLY whitelisted keys — `wl_*` white-label rows in admin_settings survive; first cut wiped them, caught pre-deploy) + `settings-prune-audit` (deletes audit_log older than `sys_audit_retention_days`, returns count). **UI**: `SET_GROUPS` config → `settingsFieldHtml` (text/number/On-Off select), Save / ↺ Reset / 🧹 Prune audit buttons with confirm + toasts, per-group subtitle hints, "N settings" footer. No version bump (superadmin.html has no SW; landing untouched). Verified rig: 33 keys, save/round-trip, min-9 rejects 8-char register w/ clear message, complexity rejects letter-only, reset restores defaults + wl_* survive, prune shape ok; test_superadmin **250/250**; regression pending. Detail: `references/sa1-fullsite-v4.4.md`.
- **SA1-fullsite-v4.3 ✅ (2026-08-05, branch `superadmin-panel`, commit `2fff9f5`)** — **Blog manager** (next after the v4.2 SEO pack — new articles = new indexed pages; fully autonomous). **API**: `blog_posts` table (slug UNIQUE/title/tag/excerpt/body HTML/cover_emoji/read_min/status draft|published/created_at/updated_at) + superadmin-only `blog-posts`/`blog-save`/`blog-delete` (slug `^[a-z0-9][a-z0-9-]{1,79}$` 400, dup slug 409, bad status 400, unknown id 404, audit-logged) + public GET-whitelisted `blog-list` (published only, newest first) + **article renderer**: pre-switch handler matches `^blog/([a-z0-9-]+)$` (via new .htaccess rewrite `^blog/([a-z0-9-]+)/?$ api/index.php [L,QSA]`) → `blog_article_html($p)` echoes a full HTML page — SEO meta/OG/Twitter/JSON-LD BlogPosting, **`<base href="https://krtaker.com/">` fixes all relative links (nav/footer/css/js) at the /blog/<slug> path**, navbar+footer+scripts chrome copied from the static article pages (embedded as PHP heredocs), draft/unknown slug → 404 redirect to /404.html. **Frontend**: `js/blog-hydrate.js` fetches blog-list and appends published posts as `.blog-card`s to the blog index (dedupes against existing hrefs, silent fallback, esc/fmtDate helpers); blog.html loads it. **superadmin.html**: new **📝 Blog** nav item (Content & CRM group) + `renderBlog` (table: title/slug/tag/read/status 🟢/🔴/created/updated + ↗ open live + ✎ + 🗑) + `blogForm` drawer (title/slug/tag/cover emoji/read min/status select/excerpt/body textarea) + `blogSave`/`blogDelete`. Version **?v=3.59 / SW v66** (28 pages bumped). Verified rig: create→public list→article render (200, title/body/chrome/JSON-LD), draft→404+hidden, re-publish, bad slug 400/dup 409/bad status 400/unknown 404, delete cleans public list; **published blog posts appended to the sitemap** (probe: 28 locs → clean after delete); test_superadmin **238/238**; regression **2402/2402**. Verified live: UI E2E — 📝 Blog nav → table → ＋ New post drawer → save → live `/blog/ui-e2e-post` renders (200, title/body/chrome/JSON-LD) → public list → delete → 404 + list clean; sitemap 28 locs with a live post. Detail: `references/sa1-fullsite-v4.3.md`.
- **SA1-fullsite-v4.2 ✅ (2026-08-05, branch `superadmin-panel`, commit `79d3e53` + `dbe88df`)** — **SEO & discoverability pack** (from the standing GA4/Search Console/Bing offer — the plumbing is now all in place; user drops tokens in the Site tab). **Dynamic sitemap**: new public GET-whitelisted **`sitemap`** action — XML from a 27-page map (all CMS pages incl. listings, which the old static file lacked), **draft pages excluded** (cms_pub.published=0), **lastmod = last CMS edit** via `MAX(cms_history.ts)` per page (cms_content has NO timestamp column — first cut 500'd "no such column: updated_at"), falling back to per-page defaults; priorities/changefreq (home 1.0/daily … terms 0.3/yearly); `cms_pub_sweep` runs first so due scheduled pages publish before the index is built. Available at `/api/sitemap`. **Verification plumbing**: new CMS defaults `site.seo.verify_google` + `site.seo.verify_bing` (render automatically in the Site tab's new "Search engine verification" section); cms-hydrate.js injects `<meta name="google-site-verification">` / `<meta name="msvalidate.01">` only when set. **Superadmin Site tab**: new 🔍 "Sitemap & search engines" panel (readonly URL + 📋 Copy + 🔔 Ping Bing → new `cms-ping-sitemap` superadmin-only action, audit-logged). **Ping = IndexNow** (Bing's modern protocol; the legacy `/ping` endpoint now returns 410): 32-hex key auto-generated once into `platform_meta` (kept OUT of the CMS UI), key file served at `https://krtaker.com/<key>.txt` via a new .htaccess rewrite → API handler (public, hash_equals, 404 for wrong key), POSTs `{host,key,keyLocation,urlList}` to api.indexnow.org — URL list built from the shared `sitemap_page_map()` helper (NO HTTP self-fetch — live server couldn't fetch its own sitemap → fell back to 1 URL on first live try). **Static files**: sitemap.xml regenerated to match the dynamic output (+listings); robots.txt now lists both `Sitemap:` lines; deploy_landing.py now includes sitemap.xml + robots.txt (58 files). Version **?v=3.58 / SW v65** (28 pages bumped). Verified rig: sitemap 27 urls, home lastmod from edit history, privacy draft → excluded → republish → included, verify keys round-trip, ping RBAC owner 403 / superadmin 200 (IndexNow 202), key file 200 + wrong-key 404; regression **2384/2384** (superadmin **220/220** on final code). Verified live: `/api/sitemap` 200/27, key-file rewrite 200, ping IndexNow 200 with 25 URLs, meta injection (GSC token present on page, Bing absent when unset). Detail: `references/sa1-fullsite-v4.2.md`.
- **SA1-fullsite-v4.1 ✅ (2026-08-05, branch `superadmin-panel`, commit `cb71ba6`)** — **scheduled publish/draft for CMS** (chosen from open offers after v4 closeout). API: `cms_pub` gains `publish_at` (PRAGMA-guarded ALTER migration, auto on live); `cms_pub_sweep($pdo)` UPDATEs due pages (`publish_at <= now` → published=1, clear publish_at) called on every cms-read/cms-get/cms-pub-get — no cron needed; `cms-pub-save` accepts `publish_at` (YYYY-MM-DDTHH:MM, normalized T→space + :00; future → draft, past → publish-now, '' clears); cms-get + cms-pub-get pub maps return publish_at. UI: 📅 datetime-local input + Schedule button in the CMS page head; ⏳ "Scheduled <date>" chip with ✕ cancel (confirm); publish-now toggle clears schedule. **CRITICAL FIX discovered via node --check**: v4's CMS_PAGES keys `blog-eviction:` etc. were UNQUOTED (hyphens illegal in bare object keys) → the entire inline script failed to parse → **superadmin.html SPA was dead on live since the v4 deploy** (login screen rendered but doLogin undefined). Quoted all 6 hyphen keys; added a `node --check` JS-parse guard + quoted-key guard to test_superadmin so it can never regress. Second bug found via live browser E2E: schedule saved but ⏳ chip never rendered — `renderCms` loads pub from **cms-get** (not cms-pub-get) whose pub map lacked publish_at → fixed + guarded. **Test-infra fix**: test_p50/run_all gate-visit timestamps crossed UTC midnight (now-6h/now-8h → yesterday) breaking `today_in>=4` on the next run — run_all preamble + p50 cleanup now re-stamp all 4 visits to today (self-healing); regression **2360/2360 twice consecutively** (superadmin **197/197**). Verified live: schedule → draft (public serves defaults) → past timestamp publishes instantly → clear; panel functional (login, 30 CMS tabs, ⏳ chip renders, dashboard data). `?v=3.57` / SW v64. Detail: `references/sa1-fullsite-v4.md` (updated).
- **SA1-fullsite-v4 ✅ (2026-08-04, branch `superadmin-panel`, commit `5062774`)** — **every site page now fully CMS-driven** (user: "next" after the v3.55 redesign — remaining gap from "website not full dynamic yet"). 9 utility pages were still static: **6 blog articles** (blog-eviction, blog-holding-tax, blog-lease-registration, blog-nrb-buying, blog-nrb-remittance, blog-tds-commercial), **terms**, **privacy**, **listings**. Root cause of "not dynamic": they had no `data-page` tag AND didn't even load `cms-hydrate.js`. Fix: `<body data-page="…">` + `data-cms="page.hero.title/subtitle"` on H1/hero-p + `data-cms-html="page.article.body"` on the article container + missing hydrate `<script>` tag on all 9; **API `CMS_DEFAULTS()` +52 blocks → 548 total / 30 page tabs** (hero title/subtitle from real H1/subtext, article body = container innerHTML extracted by div-depth counter, `seo.<page>.meta_title/meta_description` "… — KRTaker"); listings USP band editable (`usp.1_title..4_body`); superadmin `CMS_PAGES` +9 entries (grouped after Blog, correct `f:` for ↗ Open) + `CMS_SEC_LABEL` `article`/`usp`; cms-hydrate pageKey() fallback map extended. **Default values extracted programmatically** so live text unchanged until edited (single-quoted PHP, `\'` escaped; pitfall: `\s*` in the page-body→container regex shifts `m.start()` — use capture group). Verified: draft/live round-trip on blog page (cms-save blocks array → public shows custom → `cms-pub-save published:0` → public serves DEFAULT → publish → custom; action is `cms-pub-save` not `cms-pub`); local `php -S` + router proxying `/api/*`→rig proved H1 hydrates after cms-save; regression **2351/2351** (48 suites, test_superadmin **187/187**, guards ≥500 blocks + 9 utility pages); deployed live (API + 56/56 landing files, SW v63, ?v=3.56); live browser 0 JS errors, H1/article/SEO title hydrate. Detail: `references/sa1-fullsite-v4.md`.
- **SA1-pricing-live ✅ (2026-08-04, branch `superadmin-panel`)** — **pricing.html now renders from `plan_catalog`** (user: "I want to change the package price and information in pricing.html — how from the super admin panel?"). Before: pricing.html was 100% static HTML (Starter ৳5,000 / Business ৳15,000 / Enterprise ৳45,000) while the superadmin **Packages** module edited `plan_catalog` in the DB (৳4,999/14,999/39,999) — the marketing page never read it. Fix: new public **`plans`** endpoint (GET-whitelisted, no auth) returning `plan_catalog` ordered by price with `features`/`limits` decoded and `modules` stripped (role matrices stay admin-only); new `web/js/plans-hydrate.js` renders the 3 `.price-card`s (name, ৳ price via `toLocaleString('en-IN')`, tag → desc, feature list, featured = index 1 with MOST POPULAR flag, Enterprise → Contact sales button) from `/api/plans` with silent static-HTML fallback; pricing.html section title/subtitle + SEO meta title/description now `data-cms`-tagged (CMS pricing.section.* + seo.pricing.*). **Bug fixed**: `cms-hydrate.js` was forcing `document.title = seo.home.meta_title` on EVERY page — now `[data-cms-title]` wins when present and the home fallback only applies if no page title tag matched (pricing page was showing the home title). Verified rig + live: superadmin `app-packages save` (price 4999→5555 + tag change) → public `plans` reflects → pricing page shows ৳5,555 → restored; cards/feature counts/title correct, 0 JS errors; test_superadmin 104/104. Deployed: FTP api/index.php + pricing.html + js/plans-hydrate.js + js/cms-hydrate.js. No version bump beyond ?v=3.52 on pricing.html asset links.
- **SA1-landing-cms ✅ (2026-08-04, branch `superadmin-panel`)** — **landing page now reads the CMS** (user: "does the live landing page get edited by the CMS? some dark-mode texts need to be lighter"). **API**: new public **`cms-read`** endpoint (GET-whitelisted, no auth) returning merged `CMS_DEFAULTS` + stored rows as `blocks[]` + flat `map` (`page.section.key` → value) for the landing pages; `CMS_DEFAULTS` realigned to the actual index.html copy (hero badge/title_a/title_b/subtitle, CTA primary/secondary, features section title/subtitle, image_alt) so nothing changes until edited. **Landing hydration** `web/js/cms-hydrate.js` (new): fetches `cms-read` (default `/api/`, override `KR_API_BASE`), applies `[data-cms]` (text), `[data-cms-bn]` (বাংলা only), `[data-cms-ph/-href/-img/-alt]`, `meta[data-cms-content]` (SEO description), `[data-cms-title]` + `document.title` (SEO meta_title); **language-aware** — in BN mode prefers `key_bn` else leaves the i18n dict alone (no English bleed); re-applies on `kri18n` (lang toggle) + `DOMContentLoaded`; silent fallback to static HTML when API is down. index.html tagged: hero badge/title-a/title-b/subtitle, both CTAs, features section title+subtitle, footer tagline, meta description + `<title>`. **Dark-mode text brightening** (user-reported): `--text-soft` `#A8B6CF→#C2CFE4`, `--text-mute` `#7483A0→#93A7C8`, `--text` `#E8EEF9→#EAF1FC`, section-dark subtitle + footer `#9FB0C6→#C2CFE4`, dark mock-url `#8FA3C8→#AFC1DE`. Deploy script includes `cms-hydrate.js`. Verified rig + live: cms-save (superadmin) → cms-read → landing shows new value end-to-end, EN/BN toggle keeps Bengali, dark computed colors lighter, 0 JS errors. Pitfall: live API is `/api/<action>` (rewrite) NOT `/api/index.php/<action>` — hydrate default must match; two concurrent run_all passes collided on the rig DB → transient login 500s (rerun clean). Deployed: FTP api/index.php + index.html + css/style.css + js/cms-hydrate.js. No version bump (landing has no SW control over index.html cache; ?v=3.52 bumped on the 3 script/css links).
- **SA1-cms ✅ (2026-08-04, branch `superadmin-panel`, commit `ad20fc6`)** — advanced CMS + global tooltips + sidebar flyout (user: "make CMS more advanced and features full, make tooltips more effective, when sidebar is collapsed hovering menu items will show full menu in preview"). **CMS API**: new `cms_history` table (id/page/section/k/v/by/ts + page index); `cms-save` records the OLD value before each change (response gains `changed` count); `cms-reset` archives every deleted row before wiping; new `cms-history` (page-filtered, last 100) + `cms-restore` actions; `CMS_DEFAULTS` 40→64 rows (hero `image` URL, `seo/home/og_image`, 21 Bengali `_bn` variants for hero/cta/features-cards/pricing/about/contact/footer). **CMS UI**: EN/বাংলা language toggle (filters `_bn` fields; labels strip `_bn` + show "(বাংলা)"); 🗒 History drawer (head + per-page buttons) listing section/key, editor, ts, old value with per-row ↺ Restore; ⬇ Export JSON (all blocks + exported_at); ⬆ Import JSON (array or export-file, validated, upserts); field hints on every label (`CMS_HINTS` + `_title`/`_desc`/`_bn` patterns); Save-all toast reports changed count; preview renders hero image when set. **Tooltips**: global JS tooltip singleton (`#tipEl` fixed, auto-clamped to viewport, `--tip-bg/--tip-fg` themed per dark/light) driven by `[data-tip]` + optional `data-tip-pos` (right/top) — works INSIDE `overflow:hidden` panels (CSS-only tooltips would clip there); applied to topbar buttons, CMS head buttons, page tabs, lang chips, field labels, custom-field remove. **Sidebar flyout**: in mini mode hovering a nav item shows a card (group label + icon + name + `NAV_DESC` description + "Click to open →") positioned at item right; `FLY_T` delayed-hide 220ms + cancel-on-enter prevents flicker across the item↔flyout gap; hides on leaving both. **Pitfall hit**: initial flyout listener hid the flyout when the mouse entered it (inverted handler) — replaced with cancel-on-enter + flyout mouseout delayed hide. Verified rig + live: tooltip text, flyout shows/stays, EN 7/BN 5 fields, history record→restore round-trip (title changed→saved→history row→restored), import persists + cleanup, 0 JS errors. Deployed: targeted FTP superadmin.html + api/index.php. No version bump (no SW).
- **SA1-ux ✅ (2026-08-04, branch `superadmin-panel`, commit `7b57df8`)** — collapsible sidebar, themed scrollbars, Web CMS redesign (user: "make the sidebar collapsible, adjust with dark and light theme, fix the scrollbar, redesign Web CMS, update/add advanced features"). **Sidebar**: collapse to 66px icon rail via topbar toggle (`toggleSideMini()`, `krtaker_admin_side` localStorage, `side-mini` class on `#app` flips `--sidebar-w` so `.main` margin animates; labels/group headers/brand text/user info hide, active indicator becomes a top bar, nav items get `title` tooltips; toggle hidden ≤900px where off-canvas takes over). **Scrollbars**: themed thin scrollbars — `scrollbar-color` + `::-webkit-scrollbar` with `--scroll-thumb`/`--scroll-thumb-h` per theme, lighter thumb on the dark sidebar. **Web CMS** (API + UI): `CMS_DEFAULTS` 13→40 blocks (home hero badge/image_alt, features cards 1–4 title+desc, pricing badge, contact address, footer copyright + 3 socials, per-page SEO meta_title/meta_description); `cms-get` now appends stored rows missing from defaults with `is_default` flag + new `cms-delete` (block) + `cms-reset` (page, deletes rows so defaults re-merge). UI: 7 page tabs (Home/Features/Pricing/About/Contact/Footer/SEO), section cards with field counts, textareas for long keys, live char counters, search filter, per-page Reset + Add field (validated slug, dup-guard), custom fields get purple badge + ✕ remove, landing preview drawer rendering hero/features/pricing/contact/footer from live values, Landing link. **Bug found via E2E**: custom blocks saved via `cms-save` were invisible in `cms-get` (it only iterated `CMS_DEFAULTS`) → add the stored-row pass. Verified rig + live (collapse 66px + main shift, dark+mini combo, CMS tabs/fields/textareas/SEO/preview, add→save→appear→delete round-trip, save-all persistence, 0 JS errors, phone 390px no overflow). Deployed: targeted FTP superadmin.html + api/index.php. No version bump (no SW on superadmin.html).
- **SA1-dash ✅ (2026-08-04, branch `superadmin-panel`, commit `d2fa2f9`)** — rich Super Admin dashboard (user: "improve dashboard of superadmin.html"). Overview endpoint gains `recent_subs` (6 latest subscribers — 1 extra query). Frontend: KPI cards show 30d delta on Subscribers (+N in 30d · M active); page-head gains quick-action buttons (＋Subscriber / 🎫 Ticket / 💾 Backup / 🌐 CMS, replaced the old Quick-actions panel); new pure-inline-SVG chart helpers — `barChartSVG` (vertical bars, value-on-top, `opt.fmt` for moneyC, month labels via `mLabel()`), `hbarChart` (responsive CSS bars), `donutChart` (SVG arcs + legend, center total); dashboard loads `overview` + `mis` in parallel (`Promise.all`); layout = 8 KPI → Signups-by-month + Subscription-revenue-by-month → Subscribers-by-plan (hbars) + Tickets-by-status (donut) → Recent subscribers (table, `min-width:0` so no scroll) + Recent activity; all `grid-2` collapse to 1 col ≤720px. Verified: node harness on chart math (6 bars/12 texts, ৳ fmt, 4 donut paths, empty states, no NaN); rig + live playwright (real data: 14 subs / ৳1.3L MRR / 3 pkgs / 7 users / 6 leads, 6 recent rows, 0 JS errors, no mobile overflow). Deployed: targeted FTP superadmin.html + api/index.php. No version bump (superadmin.html has no SW; dashboard untouched).
- **SA1-mobile ✅ (2026-08-04, branch `superadmin-panel`, commit `f27029c`)** — responsive/mobile fixes for the Super Admin Panel (user: "not working properly in mobile view… make the design change according to the mobile"). Root cause: **inline `grid-template-columns` styles were overriding the ≤900px media query** — Dashboard (1.5fr/1fr), MIS + Finance (1fr/1fr) and Payments (repeat(3,1fr)) stayed multi-column on a 390px phone (panels squeezed to ~150–190px). Fixes: replaced inline grid styles with classes (`.grid-dash`, `.grid-2/.grid-3` with `style="gap:…"` only) and moved grid collapse to a new **≤720px** breakpoint so tablets (721–900px) keep 2/3-col layouts; added **sidebar backdrop + auto-close on nav** (`closeSidebar()` in `nav()`; `toggleSidebar()` toggles `.show` on `#sbBackdrop`) so the off-canvas sidebar no longer traps the screen; **16px inputs at ≤640px** (prevents iOS auto-zoom on focus — 13px was zoom-triggering); stats grid min 150px → 2 columns on phones (was 1); topbar hides the "Dashboard" label text on phones (icon-only, no title clipping), tighter paddings, full-width page-head/action buttons, full-width toasts, larger touch targets, `-webkit-overflow-scrolling:touch` on table wrappers. Verified: playwright at 390×844 + 768×1024 against the rig — all 18 views: no page overflow, single-col grids on phone / 2–3 col on tablet, sidebar open→nav→auto-close + backdrop-dismiss, drawer full-width, 0 JS errors; pixel-diff QA on screenshots (dashboard clean). Live: targeted FTP of `superadmin.html` only → live playwright check (login, overflow=false, titleClipped=false, grids 362px 1-col, stats 174×2, inputs 16px, sidebar+backdrop+auto-close, 0 JS errors). Committed `f27029c`, pushed to origin/backup/mirror remotes. No version bump needed (superadmin.html has no SW; dashboard-v2.html untouched).
- **SA1 ✅ (2026-08-04, branch `superadmin-panel`, commit `c1e3d23`)** — separate **Super Admin Panel** (`docs/superadmin.html`, live at `/superadmin.html`) + `app-admin` API + Super Admin **removed from dashboard-v2.html**. User asked for a dedicated SaaS admin surface (subscribers, packages, web CMS, users, settings, providers, 3rd-party integrations, payment integrations, platform support & ticketing, system admin, backup, module/service management, company finance, super-admin dashboard, MIS reports, platform CRM/onboarding, white-label master settings) — "start a new branch or version". New tables: `admin_settings` (k/v whitelist via `ADMIN_SETTING_DEFAULTS()`), `cms_content` (page/section/k PK, `CMS_DEFAULTS()` 13 landing blocks), `integrations` (type/config/enabled/status), `platform_tickets` (SPT-###, reply/status/prio/assignee), `company_ledger` (income/expense, cats), `backup_log`. **`app-admin` endpoint** (superadmin-only via `admin_guard()`): overview (subscribers/active/trial/MRR+ARR incl. caretaker_subs monthly-equivalent/packages/users/open tickets/leads/DB size/files/30d signups/recent audit), subscribers CRUD, users CRUD (app_users; password reset revokes tokens; self-delete + last-superadmin guards), cms-get/save (upsert), settings-get/save (whitelisted), integrations list/save/test + **gateway-save** (writes `platform_meta gw_config` which `GATEWAYS()` now merges — payment creds + sandbox/live switch without code changes; `mask_secret()` masks creds `•••` + last 4; masked fields keep stored value), tickets create/reply(→Answered)/status/delete, backup-run/list (download = existing `app-backup`, export = `app-export`), modules (MODULE_REGISTRY + per-package coverage — matrix editing lives in Packages), finance (income/expense/balance/cats/ledger + ledger-add/delete), mis (by plan/status/role/ticket-status/signup-month/premium-month/ledger-month), onboarding (leads/onboarding/referrals), providers (partners CRUD), whitelabel-get/save (`wl_*` keys), health (php/db_size/tables/journal/audit/tokens), audit (last 200). **superadmin.html** = self-contained SPA (80 KB, same design tokens, login gate rejects non-superadmin, 18 nav items in 5 groups: Dashboard / Subscribers+Packages+Users+Providers / Web CMS+Onboarding & CRM+MIS / Payments+Finance / Settings+Integrations+Tickets+Backup+Modules+White Label+Health+Audit Logs). **Super Admin removed from dashboard-v2.html**: ROLES superadmin entry, Admin-group Packages nav item, `DASH.superadmin()` method, `renderers.packages` + all package*/PKG/DEMO_PACKAGES code (app-packages API kept for the new panel) — caretaker stays in Admin group; role-gate arrays `['superadmin',...]` remain (server roles still valid for API gating). **Landmine hit**: line-range deletion of the P55 packages block also swallowed `avatarMarkup`+`photoBlobUrl` (they sat between the packages block and attachPhoto) → `ReferenceError: avatarMarkup is not defined` at renderNav; restored from git. Verified: rig browser (login, dashboard 8 stats, subscribers/packages/users/providers/cms/tickets/finance/whitelabel flows, role switcher + nav clean, photo helpers intact) + live browser (kabir@ login → 14 subs / MRR ৳1.3L / 3 pkgs / 7 users / 6 leads, ticket create+cleanup via API, 0 JS errors; dashboard owner: 9 roles no superadmin, no Packages nav, tenants 10 rows / plans 3, 0 JS errors). Regression **2268/2268 (48 suites; test_superadmin 104/104)**. `?v=3.51` / SW v59. Detail: `references/sa1-superadmin-panel.md`.
- **P56.2c ✅ (2026-08-04, commits `e6613b8` bump+code)** — tenant call button, chat thread, owner-only private note, photo-drawer back button, live. **Call**: tenant profile header gains a 📞 **Call** link (`telHref()` normalizes BD numbers — `01712-445566` → `tel:+880****5566`, `880…`→`+880`, `+…` kept; visible superadmin/owner/manager, only when phone present). **Chat**: 5th 💬 **Chat** tab in the tenant drawer (staff roles only) — `tenant_messages` table + `app-tenant-chat` / `app-tenant-chat-send` (superadmin/owner/manager, 404 unknown tenant, 400 empty body, audit-logged); `loadTenantChat` eager-loads on drawer open, bubbles styled `.chat-b.you/.them` (current user right/gradient), Enter or ➤ sends, toast "Message sent" ok; demo parity via `DEMO_CHAT`. **Note**: owner-only 📝 **Private note** panel in the Profile tab (`tenant_notes` table + `app-tenant-note` / `app-tenant-note-save` — **superadmin/owner ONLY, manager/tenant 403**, audit-logged) — client-gated by `['superadmin','owner']` + server-enforced; textarea + 💾 Save → toast "Note saved" ok. **Back button**: photo drawer foot `Cancel` → **← Back** (`openDetail('tenants',id)` returns to the profile view). Verified on rig (owner: call href, 5 tabs, note block, chat send → bubble+toast, note save → DB, back → profile; manager: chat yes/note no; tenant: nav-gated) 0 JS errors; live browser (T-002: `tel:+880****5566`, chat send → "Message sent" + bubble persisted, note save → "Note saved" persisted, back button round-trip, cleanup probe) 0 JS errors; live API round-trip + manager/tenant 403s + cleanup. Regression **2164/2164 (47 suites; test_p562c 37/37)**. `?v=3.50` / SW v58. Detail in `references/p562c-call-chat-note.md`.
- **P56.2b ✅ (2026-08-04, commit `c6d4db7`)** — photo cache invalidation + visible toasts, live. User reported **"photo not changing"** after re-upload. Root cause: `PHOTO_CACHE[target+':'+id]` blob URLs were never invalidated — `attachPhoto` → `photoBlobUrl` returned the OLD cached blob URL, so the row/drawer kept showing the previous photo even though the DB + file on disk had the new one (browser-verified: DB photo changed, row img stayed the same blob). Fix: `photoCacheClear(target,id)` helpers called in `uploadPhoto` success path (tenant-upload → 'tenant:'+id; user-upload → 'user:'+uid) BEFORE `onDone.saved` re-renders. **Second bug**: toasts were INVISIBLE during uploads — `.toast-wrap` `z-index:99` UNDER the `z-index:120` drawer overlay, so success/fail notifications never showed. Fix: toast-wrap `z-index:999` + `toast()` type mapping (ok/success → ✓ green check, error → red ✕ bar) + `.toast.ok/.toast.error` CSS; saveForm/photo paths now fire `toast(...,'ok')`. Verified live with the user's actual photo: upload pic A → blob1; upload pic B → row shows NEW blob2 (`changed=true`), "Photo saved" (ok) toast visible above drawer, 0 JS errors; test photos cleaned via one-shot probe. Regression **2127/2127 (46 suites; test_p56 25/25)** + 3 new static guards. `?v=3.49` / SW v57. Detail merged into `references/p562-photo-uploader.md`.
- **P56.2 ✅ (2026-08-04, commits `57ece7f` bump + `d850629`)** — tenant row photos + photo-uploader overhaul, live. **Tenants table**: every row now shows a 36×40 avatar cell (`tn-av`) — the tenant photo via `fetch→blob` when present, or a **👤 + "No Photo" placeholder** when absent (was `display:none` when no photo). **Photo uploaders (all 3)**: `tenantPhotoDrawer`, the tenant add/edit form block, and the Settings profile photo are now **drag & drop zones** (`tpd-zone`, dashed border, `drag` highlight class) with click-to-browse (hidden file input), live **72px preview** (`tpd-prev` + `showPhotoPreview()`), and a **spinner overlay** (`tpd-prev.up` ::before ring + ::after dim) with the submit button disabled + "⏳ Uploading…" during upload. Dropped files are staged in `window.__pendingPhotoFile` (FormData can't be populated from DnD directly) and consumed by `uploadPhoto()`; `saveForm` now checks `|| window.__pendingPhotoFile` so a dropped photo saves with the tenant. **CRITICAL BUG FIX**: `uploadPhoto()` called `onDone(fd)` — `onDone` is an OBJECT `{append, saved}` — so it TypeError'd BEFORE the fetch and **every photo upload silently failed in live mode** (demo appeared to work only because the old code never ran `saved` either; test_p56 passed because it drives the API, not this JS). Now `onDone.append(fd)`. Also added a **DEMO_MODE branch** in `uploadPhoto` (simulated 600ms + local `demo_*.jpg` set) — demo previously 401'd. Static JS guards added to test_p56 (5 new checks on the bundle text — API tests can't catch JS TypeErrors). Regression **2124/2124 (46 suites; test_p56 22/22)**, smoke 9/9, dash 1,014,671 B byte-identical, live browser 0 JS errors, live upload through the real drawer verified + cleaned up. `?v=3.48` / SW v56.
- **Phase 24 ✅ (2026-08-02, commit `09c8bf0`)** — GDPR-style data export, live. `app-gdpr-export` (GET whitelist): tenant self-service (own data; foreign `?t=` → 403, no profile → 404) or staff superadmin/owner/manager via `?t=` (missing → 400, unknown → 404, legal/other → 403). Dump = account row (**password/otp hashes redacted** to `[redacted]` via `gdpr_account_row()` — top-level helper, NOT between switch cases or it's dead code), tenant row, leases, units, properties (scoped), invoices, payments, receipts, gateway_tx, tickets + thread, documents metadata, handover, meters, utility bills, renewals, settlement_reports, notices, user settings. `Content-Disposition: attachment; filename="krtaker-data-<name>-<date>.json"`, audited with byte count. Frontend: Settings drawer gains tenant-only **🗂️ My data (GDPR)** panel → `gdprDownload()` (fetch blob → a[download]; demo parity builds JSON from `demoPortalData`). Regression **490/490**. `?v=3.22` / SW v30.
- **Phase 23 ✅ (2026-08-02, commit `16b3903`)** — notification-preference completion, live. **`notify_premium`** 7th notify toggle (SETTINGS_DEFAULTS + Settings→Preferences UI + save loop). **`premium_welcome`** email template #8 (purple gradient, plan/property/billing/next-invoice summary, caretaker dashboard CTA) seeded idempotently via the per-id `email_templates` seed loop in db() — auto-inserts on live, no app-setup needed. `app-premium-subscribe` sends the confirmation gated on `notify_ok($pdo,$email,'notify_premium')`; opt-out → audit `Caretaker confirmation suppressed (opt-out)` (same pattern as P19; **resubscribe upserts the SAME CS id** so test suppression assertions must delete-then-count, not compare ids). `app-collections-run` now enforces `email_digest` per recipient too (`notify_collections` OR `email_digest` off → suppressed++). Email-template count asserts in test_p12/p13 bumped 7→8. Regression **473/473**. `?v=3.21` / SW v29.
- **Phase 22 ✅ (2026-08-02, commit `9e86dd1`)** — NRB Premium Tier (remote caretaker subscription), live. `caretaker_subs` table (CS-###; user_email/prop/tier/price/cycle/status/start/end/next_invoice/features). `PREMIUM_TIERS()`: `care_plus` (৳2,500/mo) + `nrb_caretaker` (৳4,500/mo · ৳12,000/qtr · ৳45,000/yr, popular, 7 features: video walkthrough, NRTA/NITA dossier, TPA desk, repair escrow, health report, WhatsApp concierge). Helpers `premium_cycle_price`/`premium_next_invoice` (+1/3/12 months)/`premium_sub_rows` (LEFT JOIN property). Endpoints: `app-premium-plans` (GET, any authed), `app-premium-sub-list` (GET; owner=own email, manager/superadmin=all), `app-premium-subscribe` (owner/superadmin; validates tier/cycle/prop; **upserts the active/paused row per user_email** — resubscribe keeps CS id; cancelled → NEW id; one active sub per user), `app-premium-cancel` (owner/superadmin; foreign → 403), `app-premium-toggle` (pause/resume; cancelled → 409). Frontend: **👑 Caretaker** nav view (owner/superadmin/manager; new Premium nav group) — tier cards w/ per-card cycle select + shared property select, active-subscription card (status badge, features checklist, pause/cancel), property health snapshot (rent roll/tickets/avg tenant score from DB), monthly care-tasks checklist, video-walkthrough booking (session-local). Demo parity (`demoPremiumPlans()` + `DEMO_PREMIUM` state; subscribe/toggle/cancel all demo-path). Regression **463/463**. `?v=3.20` / SW v28.
- **Fix deploy ✅ (2026-08-02, commit `bb29b4b`)** — email MIME + registration redirect. See skill pitfalls.
- **Phase 21 ✅ (2026-08-02, commit `4638568`)** — Settlement Statement + No-Dues Certificate (কোনো পাওনা নেই), live. **Backend**: `settlement_reports` table (SET-###; tenant/lease/status/total_due/balance/refund/payload/generated_by/ts). Helper `settlement_report($pdo,$tid,$lease_id=null,$opts)` aggregates across ALL tenant leases: **rent arrears** (invoices with due = max(0, net − ΣSuccess payments)), **utility dues** (utility_bills Unpaid on tenant units), **damages** (`deductions` `[{label,amount}]` ≥0), **deposit** = primary lease adv (requested → Active/Pending → latest). Status: total_due==0 → **NO_DUES** (full deposit refundable, cert eligible); balance = total_due − min(deposit,total_due) ≤ 0 → **SETTLED** (refund −balance); else **DUE** (refund 0). `persist` off by default (portal calls non-persisting). `settlement_report_html()` — printable certificate: status stamp (EN+বাংলা), statement table, deposit applied, EN+BN amount-in-words, move-out checklist chips, Landlord/Tenant/KRTaker signature blocks. Endpoint `app-settlement-report` (GET whitelist + POST): tenant self-service (foreign `?t=` → 403, own-lease check), staff superadmin/owner/manager/accountant via `?t=` or `?lease=` (missing → 400, unknown → 404, legal/other → 403); `?html=1` → print page (deductions accepted as GET JSON too); persists + audits each generation. `portal_data()` returns `settlement` summary (non-persisting). **Dashboard**: tenant portal **Settlement & no-dues panel** (status badge, arrears/utility/damages/total/deposit/refund, 🖨 statement button, status-aware note); staff **Settlement drawer** from score drawer 🧾 (statement table + totals + **damage-deduction editor** with chips/✕, POSTs deductions to regenerate — `SETTLE_DEDUCTIONS` synced from server); `printSettlement()` appends `deductions=` to the GET so the printed cert matches the drawer. Demo parity (`demoSettlementFor`/`demoSettlementHtml`). `?v=3.18` / SW v26. Tests **36/36 P21 + 392 regressions (428 total)** + rig E2E (SET-001 Settled w/ deposit cover → +৳5,000 damage → DUE ৳5,000 payable; print cert; tenant portal panel; demo parity; zero JS errors) + live prod (SET-001 live: rent ৳40,000 + utility ৳1,500 → DUE ৳1,500 balance; portal summary; tenant own/foreign 403; HTML cert with deduction → ৳6,500 balance + BN words; browser: Sultana panel **Amount due** badge, zero JS errors).
- **Phase 20 ✅ (2026-08-02, commit `22c523c`)** — Utility Billing Engine (readings → usage → bill → pay), live. **Backend**: `utility_tariffs` table (type PK; rate/standing/unit_label/enabled — idempotent seed electric ৳10/kWh · gas ৳15/m³ · water ৳20/m³) + `utility_bills` (UB-###; unit/tenant/type/month/prev_reading/curr_reading/usage/rate/standing/amount/status Unpaid|Paid|Void; **UNIQUE(unit,type,month)** upsert keeps id). Helpers: `UTILITY_TYPES()`, `utility_tariffs()` (defaults merged with rows), `utility_tariffs_save()` (per-type whitelist), `utility_bill_calc()` (usage = curr − latest prev month; **no prev → usage = curr** (first bill baseline); manual `prev_reading`/`curr_reading` overrides; missing current reading → 400; disabled tariff → 400; amount = round(usage×rate + standing)), `utility_bill_upsert()`, `utility_bill_next_id()`. Endpoints: `app-utility-tariff-get` (GET; superadmin/owner/manager/accountant), `app-utility-tariff-save` (POST; superadmin/owner/manager, audit), `app-utility-bill-preview` (POST; same staff — dry computation), `app-utility-bill-generate` (POST; staff — attaches tenant of active lease on unit, upsert), `app-utility-bill-list` (GET; staff all joined unit/property/tenant · tenant own units only), `app-utility-bill-pay` (POST; `{id, action: pay|void}` — tenant may only pay own-unit bills, tenants cannot void, Paid/Void → 409, foreign unit → 403). `portal_data()` returns `utility_bills` (non-Void) → tenant portal. **Dashboard**: Units view header gains **⚡ Utility billing** button (superadmin/owner/manager/accountant) → `utilityBillingDrawer()` (💰 Tariffs editor w/ rate+standing+enable per type → `utilitySaveTariffs()`, 🧮 Generate form unit/type/month w/ live `utilityPreview()` inline calc + `utilityGenerate()`, 🧾 bills list `utilityLoadBills()` w/ Pay/Void buttons `utilityBillAction()`); tenant portal gains **My utility bills** panel (`utilityBillPanelHtml(d)` + `utilityBillPay()`). Demo parity (`demoPortalData().utility_bills` UB-100/101 + local preview/generate). `?v=3.17` / SW v25. Tests **38/38 P20 + 354 regressions (392 total)** + rig E2E (drawer, preview live 2,500→2,650=150 kWh ৳1,500, generate, list, staff pay/void, tenant portal pay) + live prod (tariffs, preview, generate UB-100 U-010 2026-08 150 kWh ৳1,500, tenant portal sees it; RBAC 403s). **Live-data note**: first-run billing on live used `no prev → usage = curr` (5120 → ৳51,200) because live had no July reading; regenerated with explicit `prev_reading:4970, curr_reading:5120` to a realistic ৳1,500 demo bill — the override path is also the fixup tool.
- **Phase 19 ✅ (2026-08-02, commit `ef5882e`)** — Org-level Settings + notification-preference enforcement, live. **Backend**: `org_settings` table (k/v) + `ORG_DEFAULTS()` (inv_prefix, org_name, org_tagline, default_lease_months, khajna_calendar, invoice_footer) + `org_cfg()`/`org_cfg_save()` (whitelisted keys, **inv_prefix dash-normalized** — "MARS"→"MARS-", bad chars stripped) + **`invoice_next_id()`** honoring the org prefix (replaces hardcoded `INV-$year-` in BOTH AI `generate_invoice` and app-crud invoice create). New endpoints **`app-org-settings-get`** (GET, owner/manager/superadmin; khajna calendar decoded to array) / **`app-org-settings-save`** (POST, same roles, 403 otherwise, audit-logged). **`notify_ok($pdo,$email,$flag)`** maps recipient email → `user_settings` (`sub:`/`staff:` keys, active only; unknown → **send by default**) and is enforced in ALL senders: rent reminders (`notify_rent`, in `app-reminder-run` loop before `reminder_send_one` — opt-outs **not** stamped in `invoice_reminders` so re-enabling resumes), collections digest (`notify_collections`, per-email group in `app-collections-run`), invoice-email / receipt-email (`notify_docs` → `{ok:true, suppressed:true, reason}` + audit "… suppressed (opt-out)"), renewal-status email (`notify_renewal`, `app-renewal-decide`). `suppressed` counter added to reminder/collections run responses + platform_meta/audit stamps. **Dashboard**: Settings drawer gains **🏢 Organization settings panel** (owner/manager/superadmin only — role gate uses `role().id`/`LIVE.user.role`), fields: org name, tagline, invoice prefix, default lease months, **khajna calendar line-editor** (`P-001 | 30 Jun` per line ↔ JSON array), invoice footer. `?v=3.16` / SW v24. Tests **31/31 P19 + 323 regressions (354 total)** + rig E2E (demo defaults, owner/manager see panel, tenant doesn't, live save→API round-trip incl. prefix normalization + khajna, zero JS errors) + live prod 7/7 (org get/save/restore, RBAC 403, suppressed fields present). **Note**: `execute_code` is blocked in this profile's cron/sandbox — live checks run via script files.
- **Phase 18 ✅ (2026-08-02, commit `e19fd0b`)** — User Profile Management + per-user Settings, live. **Backend**: `user_settings` table (user_key PK = `kind:id`, prefs JSON, ON CONFLICT upsert) + `SETTINGS_DEFAULTS()` (theme light/dark · lang en/bn · notify_rent/collections/renewal/docs · wa_reminders · email_digest — whitelist-keyed, bool/theme/lang coercion, unknown keys ignored). **`app-profile` unified** (was POST-only, now GET→`user_profile()` = name/email/role/kind/avatar + sub-specific phone/org/plan/trial_end + **linked tenant row via sub_email** or staff dept + full settings; POST→ update name/org/phone (subs) or name/dept/avatar (staff) + **tenant-row sync** when a linked subscriber changes name/phone + `old_password`/`new_password` change (min 6, wrong current 403 via record_attempt, **revokes all other sessions**). **`app-settings-get`** (GET) / **`app-settings-save`** (POST) for preference persistence per user. **Dashboard**: Settings drawer (topbar ⚙️ for ALL roles now, was superadmin-only) extended with a **Preferences panel** — theme pickers (🌙/☀️), language pickers (EN/বাংলা), 6 notification toggles (new `.kr-sw` checkbox-switch CSS — `.sw` is already the legend swatch class, don't reuse), Save preferences applies theme+lang immediately; staff gain an **Avatar initials** field; sidebar+topbar avatar/name now render `LIVE.user.avatar`/`name` (was static role().ava). `?v=3.15` / SW v23. Tests **29/29 P18 + 294 regressions (323 total)** + rig E2E (settings drawer, dark/bn apply, live profile save/restore incl. tenant-row sync, server-persisted prefs, zero JS errors) + live prod (owner/tenant/staff profiles, settings round-trip dark→light, tenant link T-002, unauthed 401).
- **Phase 17 ✅ (2026-08-02, commit `bd1458d`)** — Tenant Report Card (renter risk score + landlord vetting), live. **Score engine**: `tenant_scorecard($pdo,$tid)` computes a 0–100 composite from four weighted factors — **payment 40%** (settled-invoice ratio + punctuality vs due date = 1st of invoice month, 3-day grace; on-time rate, avg days late, overdue-count penalties), **stability 20%** (longest lease tenure in months capped 24, approved renewal_requests, −20 if no active lease), **care 20%** (tenant-liability tickets resolved ratio + meter-reading months bonus), **compliance 20%** (handover Completed +20, TPA §107 reg_ok for non-res leases +20/−20, documents on file +10, base 50). Bands: ≥80 Excellent / ≥60 Good / ≥40 Fair / <40 Risky. Endpoints: `app-score-list` (GET, superadmin/owner/manager — all tenants, sorted desc, with band + 4 factors), `app-score-detail` (GET — tenant own OR staff `?t=T-xxx`; returns score/band/factors/stats/tips), `app-vetting-report` (GET — printable HTML: score ring, factor bars, key indicators table, improvement tips; echo+exit like app-invoice-print; tenant own only, staff any tenant). `portal_data()` now returns `score` (full card for the portal panel). **Dashboard**: Tenants view gains a **Score column** (band-colored badge, click → `scoreDrawer(tid)` — donut ring, 4 factor bars, key indicators, tips, 🖨 Print vetting report); tenant portal gains a **My tenant score** panel (ring + band + 3 tips + print). Demo parity via `demoScoreFor(tid)` (JS mirror of the formula) + demo renewals rows. **Class bug fixed (found here)**: staff-role checks compared `role().role` (display label, e.g. "Property Owner") against `['superadmin','owner','manager']` — always false, so the P16 renewal panel NEVER rendered in demo mode; switched to `role().id` at all 4 sites + added `renewalsPanel()` demo parity. `?v=3.14` / SW v22. Tests **27/27 P17 + 267 regressions (294 total)** + rig E2E (8 score chips 25–87, drawer, print, portal panel, renewal panel) + live prod (8 scores, detail, HTML report, tenant RBAC 403, zero JS errors).
- **Phase 16 ✅ (2026-08-02, commit `137ed00`)** — Lease Renewal Requests + Utility Meter Readings + Listings v2 (CSS fix), live. **Renewal flow**: `renewal_requests` table (RR-###, months 1–36, new_rent optional, note, status Pending/Approved/Rejected, decided_by/at); `app-renewal-create` (tenant only, own-lease 403 otherwise, one pending per lease 409, Expired/Terminated 400), `app-renewal-list` (GET, superadmin/owner/manager — joined tenant/unit/property, pending first), `app-renewal-decide` (approve → extends lease end by months + applies new_rent + reactivates Expired/Terminated, creates system notice + sends **renewal_status email template #7**; reject → lease untouched; re-decide 409). **Utility meters**: `meter_readings` (MR-###, UNIQUE(unit,type,month) upsert via ON CONFLICT, back-step guard — reading can't be lower than previous for same unit/type; `app-meter-submit` tenant own-unit/staff, `app-meter-list` staff-all / tenant-own). `portal_data()` now returns `renewals` + `meters` (tenant scoped). **Dashboard**: tenant portal lease cards gain 🔄 Renew my lease drawer + status chips (pending/approved/rejected) + 📟 My utility meters panel (type/month/reading/**usage diff vs previous month**); staff Leases view gains a Renewal requests panel (approve/reject inline, pending-first) + Units view 📟 Meter readings drawer; `render()` hooks `renewalsPanel()` for leases view; `demoPortalData()` parity. **Listings v2 + CSS fix (user-reported)**: listings.html referenced `css/landing.css` + `js/landing.js` — **both 404 on prod** (only style.css/main.js/i18n.js exist; i18n.js was also missing from deploy_landing.py) → page rendered unstyled; rewritten to load `css/style.css` + `js/main.js` + `js/i18n.js` + `i18n-dict.js` + sw registration; new toolbar (🔍 search, type chips + **city chips** Dhaka North/South/Chattogram, sort rent↑↓/size/name), **rent-first unit cards** (asking rent per vacant unit from new `units.rent` — /api/listings returns it), per-unit WhatsApp deep-link with rent, USP strip (verified+managed, digital agreement+receipt, bKash/Nagad, no broker fee), richer meta/OG/JSON-LD, `kri18n` re-render on language toggle. `?v=3.13` / SW v21. Tests **40/40 P16 + 227 regressions (267 total)** + rig E2E (renewal UI submit RR-100, meter drawer, zero JS errors) + live prod (10/10 endpoint checks, listings styled with rents, zero JS errors).
- **Phase 13 ✅ (2026-08-02, commit `f4eedee`)** — Document Vault + Email Delivery + Print Preview + Handover Checklists, live. **Document vault** (Documents module revamp): `documents` table gains `p` (property) + `cat` columns (categories: utility / legal / tax & khajna / community / agreement & lease / other); `app-doc-vault` returns scoped docs + properties + cats (tenant sees own-lease AND own-property docs; staff all); `app-doc-view` streams inline (PDF/images/text) with corrected allow-logic (ref-in-scope OR p-in-own-properties); upload accepts p+cat and auto-derives p from lease ref. **Email delivery**: `app-invoice-email` + `app-receipt-email` — one-click branded invoice/receipt emails to the tenant via SMTP using 2 new email templates (invoice #5, receipt #6 — amount in words EN+বাংলা); `inv_context()` helper must live at TOP LEVEL (functions inside the switch are unreachable — PHP skips them when jumping to a case). **Print preview**: `previewDoc()` drawer (iframe srcdoc + Print button) wired into `printInvoice`, `tplPrintFor`, `tplPrintCurrent` — no more window.open. **Handover checklists** (`handover_checklists`, HOV-###): 12-item move-in + 10-item move-out defaults, `app-hando-list/get/create/save/delete`, auto-Completed when all checked, ✅ Handover button on every lease row with move-in/move-out tabs, checkboxes + notes, live progress. 📧 email buttons on Invoices/Payments/Receipts. `?v=3.10` / SW v18. Tests 42/42 P13 + 71/71 P12 + 33/33 P11 + 50/50 P8-10 (196 total) + browser E2E (vault filter, PDF viewer, print preview, handover create→check→save) + live round-trip (upload→view→delete on prod, invoice/receipt email sent, handover create→complete→cleanup).
- **Phase 14 ✅ (2026-08-02, commit `f4eedee`→P14)** — Tenant Portal v2, live. **`app-portal`** (GET, module `portal` — tenant role; superadmin/owner/manager pass `?t=` to preview any tenant): aggregate of tenant row, leases enriched with unit/property + **expiry countdown** (`days_left`, `total_days`, `pct` tenancy progress, `reg_pending` from TPA §107 gate), invoices with `paid`/`due` (Success payments only — refunded rows don't count), payments, receipts, docs (own-lease refs OR own-property `p`), handover checklists, tickets, latest notices, and `stats` (outstanding, paid_total, next_due, tickets_open, min_days_left). **`app-portal-agreement`** (GET `?lease=`): renders the LSE-STD tenancy agreement as printable HTML (echo + exit like `app-invoice-print`) — tenant only for own lease (403 otherwise), staff with leases/templates module org-wide, partner 403; falls back to `seed_templates()` if the doc_templates row is missing. **Dashboard tenant() rewritten** from hardcoded T-002/U-010 to a live-data **Tenant Portal v2**: stats row (Outstanding, Paid total, Next due, Lease countdown, My tickets), per-lease cards with countdown progress bar + days-left badge (green ≥90 / orange 30–89 / red <30 / Expired), TPA §107 gate badge, 📄 Agreement button (in-app print preview), ✅ handover status chip, My documents (view/download), payment history table (net/paid/due, Pay now, 🖨 invoice print, 📎 receipt), payments list, notices. Demo mode keeps parity via `demoPortalData()` (SEED → same shape as the API). **Staff 👁 Portal preview**: Tenants view gains a Portal button (owner/manager/superadmin) that opens any tenant's portal in a drawer (`viewTenantPortal()` → `app-portal?t=` → `portalHtml`). **Pitfall fixed (class-level)**: an async loader called from a renderer MUST `await new Promise(r=>setTimeout(r,0))` before reading its target element — `loadPortal()` read `#portalLive` before `render()` wrote the skeleton; the live path survived (first `await apiCall` yields) but the SYNC demo path silently bailed → portal stuck on "Loading…". Also: helper functions must be declared OUTSIDE object literals (DASH/renderers) — a bare `async function` inside an object literal is a syntax error. `?v=3.11` / SW v19. Tests 31/31 P14 + 42/42 P13 + 71/71 P12 + 33/33 P11 + 50/50 P8-10 (227 total) + rig E2E (live tenant portal, agreement download drawer, demo-mode portal, owner portal preview) + live prod (portal w/ countdown + Sep invoice ৳40k + handover HOV-001 + lease doc, Pay drawer, zero JS errors).
- **Phase 15 ✅ (2026-08-02, commit `e3cc9ba`→P15)** — Rent Reminder Scheduler (day 1/7/15 escalation), live. **Escalation engine**: due date = 1st of invoice month; `reminder_days_overdue()` + `reminder_tier_for()` map days → tier (1: 0–6d gentle, 2: 7–13d follow-up, 3: 14d+ final, configurable ranges). `invoice_reminders` table (invoice_id PK, tier, sent_at, via) = the escalation gate — a run only sends when `target tier > last sent tier`, so each tenant is emailed at most once per tier as days pass. **Endpoints**: `app-reminder-config` (GET, recon OR `X-Service-Key`; returns config + last_run + 20-row send history), `app-reminder-save` (POST recon; persists `reminder_config` JSON in platform_meta — `reminder_config_save()` whitelists fields, NOTE: saving `{}` sets enabled=false, restore by deleting the platform_meta row), `app-reminder-summary` (GET; per-invoice plan with days_overdue/tier/last_tier + `by_tier` counts — MUST cast `(object)$byTier` because PHP json_encodes numeric-string keys as a JSON ARRAY), `app-reminder-run` (POST recon/service; dry-run default, `{send:true}` sends tiered emails via `email_render('rent_reminder')` — template upgraded with `{{tier}}/{{days_overdue}}/{{tier_note}}/{{late_fee}}` placeholders, refreshed in seed_templates only when the DB body lacks `{{tier_note}}` — stamps `last_reminder_run`). **Dashboard**: Reconciliation view gains a **Rent reminders panel** (⏰): scheduler status pill, tier-range chips, live plan table (invoice/tenant/month/due/past-due/current tier/last-sent/email), recent sends, ⚙️ Configure drawer (enabled toggle, late-fee note, 3 tier label/day-range/note editors), Preview + Send now actions. **Cron** `859146bbd940` (no_agent watchdog, 03:00 UTC = 9am Dhaka, `krtaker_reminder_scheduler.py` — service-key `{send:true}` run; silent when nothing escalated, summary + tier counts when sent, exit 1 on failure). `?v=3.12` / SW v20. Tests 36/36 P15 + 31/31 P14 + 42/42 P13 + 71/71 P12 + 50/50 P8-10 + 33/33 P11 (263 total; suite ORDER matters — P11's refund test re-refunds PAY-002/003, so P14 must run before P11) + rig E2E (panel, config drawer save, preview) + live prod (2 real tier-1 Day-1 emails sent to INV-2026-0011/0012, second run silent via gate, history stamped, zero JS errors).
- **Phase 12 ✅ (2026-08-02, commit `b48404c`)** — Document + Email Templates, live. **Document templates** (`doc_templates` table, seeds LSE-STD tenancy agreement / SRV-STD service contract / RCP-RENT·ADV·SVC·GEN money receipts): merge-field engine (`{{tenant_name}}`, `{{rent}}`, `{{amount_words_en/bn}}` — amount in words EN + বাংলা with proper 21–99 words, BD lakh grouping), `app-tpl-list/get/save/dup/delete/reset/render`. **Email templates** (`email_templates`: otp/welcome/collections/rent_reminder) redesigned with branded gradient headers + CTA; OTP/welcome/collections/reminder sends now render through the table (customizable); `app-email-tpl-list/get/save/reset` + `app-email-preview` (sample vars). **Advanced wireframing**: `app-tpl-render` returns vars + `links` map (placeholder → module/id); dashboard Templates module shows clickable linked-field chips + inline wireframe highlighting that `jumpTo()` the live record's detail view; record selector (lease/partner/payment) renders with real data. Print buttons: Leases (Agreement), Payments (Receipt), Receipts (Print/Advance). KR tool **#23 `gen_document`** (EN+বাংলা intents moved BEFORE the generate-invoice intent so "generate agreement L-001" wins). `templates` module in ROLE_MODULES (superadmin/owner/manager/legal/accountant). `?v=3.9` / SW v17. Tests 71/71 P12 + 50/50 P8-10 + 33/33 P11 (154 total) + browser E2E (wireframe jump, zero JS errors); live verified on krtaker.com (6 templates seeded, lease+receipt+service renders with real refs, OTP preview, save→reset, 23 tools).
- **Phase 11 ✅ (2026-08-02, commit `646300d`)** — Payments & Collections Autopilot + Reconciliation, live. **Collections autopilot**: `app-collections-summary` (GET; unpaid/overdue rollup with tenant/unit/property/due — auth'd recon-role OR `X-Service-Key` header for server-to-server) and `app-collections-run` (dry-run by default; `{send:true}` emails each tenant a grouped overdue summary via SMTP and stamps `last_collections_run` in platform_meta). Daily **collections-digest cron** (`89cbea204a0e`, 00:30 UTC → 6:30am Dhaka, no_agent watchdog: silent when paid up, digest to Telegram when unpaid exist, alert on failure). **Reconciliation**: `app-payment-recon` (payments vs receipts vs refunds vs gateway_tx-by-status, orphan payments, overpaid invoices, stale pending >24h), `app-refund` (→ Refunded, double-refund 400), `app-gateway-cleanup` (pending>24h → expired). New **Reconciliation dashboard module** (finance group, superadmin/owner/manager/accountant): KPIs, live unpaid-invoices panel with WhatsApp/Print actions, gateway sessions table, mismatch list with one-click cleanup, refund drawer; "Send overdue reminders" button in Invoices view. **KR tools 19→22**: `remind_overdue` (dry-run default; "send them" emails), `payment_recon`, `refund_payment` + EN/বাংলা intents. `SERVICE_KEY` const (deploy-only, not in git). Bug found by digest: `collections_data()` must sum only `status='Success'` payments (refunded ৳1 was shrinking outstanding). Fixed `loadRecon()` render race (element read before `render()` wrote it → setTimeout 0 yield). `?v=3.8` / SW v16. 33/33 local tests, live verified (service-key summary, dry-run, refund PAY-010 + double-block, 22 tools).
- **Phase 8/9/10 ✅ (2026-08-02, commit `206434c`)** — Tenant operations + Analytics + Growth, live. **Phase 8**: maintenance ticket thread (`ticket_thread` table, `app-ticket-create/comment/thread` — tenant own-unit only, partner own-job only, thread bubbles in ticket drawer), document vault (`app-doc-upload/list/download/delete`, multipart, files in `/home/krtaker/krtaker_files` (DATA_DIR outside webroot), tenant sees own-lease docs / partner own-job docs, 8MB + extension allowlist), notice board (`app-notice-list/create/delete`, seed NTC-001..3, staff-only post), printable invoice (`app-invoice-print` — auth'd HTML, TDS + payment history, Print/PDF button in invoice drawer). **Phase 9**: Analytics module (client-side from bootstrap data: 12-mo issued/collected SVG charts, occupancy per property, receivables aging, next-month collection forecast) + KR `monthly_digest` tool (issued/collected/outstanding/new leases/tickets/renewals/top outstanding, EN+বাংলা). **Phase 10**: WhatsApp reminders (wa.me deep links in `send_reminder`, WhatsApp button on unpaid invoices, share buttons), referral program (`app-referral-generate/list/update`, `register?ref=` attribution, KR `referral_link` tool, ৳5,000/referral), public listings (`listings.html` + public `/api/listings` — published properties + vacant units, filter chips, WhatsApp enquiry; register.js carries `?ref=`). RBAC extended for 4 new modules per role; bootstrap returns scoped `ticket_thread/documents/notices/referrals`. **19 KR tools** (was 15). Fixed `list_vacancies` ambiguous `ORDER BY id` (units+properties both have `id`). `?v=3.7` / SW v15, 52 deploy files. 50/50 local tests, live verified.
- **Phase 3 ✅ (2026-06-12)** — Full CRUD + drawer-sheet detail views live. Backend: `app-crud` endpoint (create/update/delete on properties/units/tenants/leases/invoices/tickets/partners/staff/support) with field whitelists, auto-ID generation (P-00X/INV-2026-000X…), server-side RBAC (tenant = own-unit ticket create only; partner = own-job update only; delete blocked for tenant/partner), lease status auto-derivation (TPA §107), invoice net derivation, audit-logged. Front-end: slide-in drawer-sheet from right — click any row/card opens full detail (KV grid + related records: property→units, tenant→leases, lease→unit/tenant/invoices, invoice→payment status + Record payment, ticket→unit/contractor + Update status), inline edit forms (text/number/date/month/select/checkbox/textarea, auto-computed net, required validation), create via ＋ buttons, delete with confirm, all mutations re-bootstrap + re-render. Demo data verified end-to-end on prod.
- **Phase 2 ✅ (2026-06-12)** — Dashboard backend + RBAC live on krtaker.com. `api/index.php` extended with app layer: SQLite app tables (properties/units/tenants/leases/invoices/receipts/payments/tickets/partners/staff/support/plan_catalog/app_users/app_tokens/audit_log), endpoints `app-setup`, `app-login`, `app-logout`, `app-me`, `app-bootstrap`, `app-subscribe`, `app-ticket-status`, `app-payment` (Bearer tokens, 7-day TTL, role-based module gating server-side). Registration now stores password_hash (plan stays `Trial` — chosen in dashboard). Dashboard V2 wired to live API: auth screen, role-isolated data (tenant sees only their leases/invoices), in-dashboard **subscription manager** (Starter/Business/Enterprise upgrade via `app-subscribe`, persists), demo-preview role switching with banner, logout. login.html now does real auth → dashboard. Demo accounts: owner@/sultana@/kabir@krtaker.com (demo123). Cache-bump v3.1 / SW v9.
- **V2.1 Dashboard (2026-06-12)** — `docs/dashboard-v2.html`: added global search (units/tenants/leases/invoices/receipts/payments/tickets/partners/staff/support, keyboard nav, jump-to-row highlight), mobile hamburger + backdrop, mobile search bar, bottom mob-bar now mobile-only (was leaking to desktop via inline style), desktop-only sticky footer (breadcrumb · zoom 60–150% · fullscreen · © credits). Live: krtaker.com/dashboard-v2.html.
- **V2 Dashboard (2026-06-12)** — `docs/dashboard-v2.html` (V1 kept as `docs/design-prototype.html`): completely redesigned dashboards for all 10 subscriber roles (Super Admin, Owner, Manager, Tenant, Partner, Service Manager/QC, Legal, CRM, Accountant, HR) in the landing-page design system (same tokens, fonts, dark mode, bilingual EN/বাংলা). Role switcher gates modules; all 12 module views; KR AI widget; pure-SVG charts; deployed live at krtaker.com/dashboard-v2.html.
- **Phase 1 ✅ (2026-08-01, commit `76b6dc7`)** — PWA prototype v1.0 (`docs/design-prototype.html`, SW `krtaker-v1-0`): 12 modules (Dashboard, Properties, Units, Tenants, Leases, Invoices, Receipts, Payments, Taxes, Maintenance, Compliance, AI Assistant); legal engines verified (TPA §107 registration gate, PRCA §10/§23 advance cap + §18 eviction, TDS split §109/§128, holding-tax Net Annual Value pipeline); bKash sandbox 4-step pay flow; AI caretaker chat with tool actions; zero JS errors, tunnel preview live.

---

## 1. Analysis of the Technical Documentation

The document describes an **enterprise-grade multi-tenant property management SaaS** for the Bangladesh market with an autonomous AI agent as the "digital caretaker". Five core domains:

| Domain | What it does | Key requirements from doc |
|---|---|---|
| **AI Orchestration Layer** | NLP chat + autonomous workflows | LLM + RAG over BD statutes (TPA 1882, PRCA 1991, Income Tax Act 2023), function-calling, permission-inherited tool execution |
| **CRM + Project Mgmt** | Tenant/lead pipeline, task & milestone tracking | Lead nurturing, sales forecasting, renovation project oversight |
| **Legal & Compliance Engine** | Programmatic enforcement of BD law | Lease >12mo → "Pending Registration" gate; PRCA rent-hike/advance/eviction constraints; e-Porcha/khatian verification |
| **Financial & Taxation Engine** | Rent, holding tax, TDS, capital gains | Net Annual Value formula, wealth surcharge brackets, TDS schedules (Sec 109–131), minimum tax |
| **Payment Orchestration** | Local + NRB payments | bKash Tokenized Checkout (4-step), SSLCommerz IPN, NRTA/NITA remittance dossiers |

**Architecture mandates:** PostgreSQL Row-Level Security (multi-tenant isolation), microservices + event-driven async, TLS 1.3 / AES-256, JWT auth, PgBouncer.

**Phased rollout in doc:** P1 infra+CRUD (wk 1–4) → P2 legal+financial+payments (wk 5–8) → P3 AI+RAG (wk 9–12) → P4 UAT+go-live (wk 13+).

### ⚠️ Reality check (engineering judgment)
- The doc is a **whitepaper-grade vision** — full microservices + PG-RLS + RAG + agent stack is a multi-month enterprise build.
- Our proven REM ERP approach wins here: **single-file PWA prototype → Flask backend → deploy**, iterating fast with visible results each phase.
- PG-RLS and microservices are **deferrable** — start with SQLite + tenant_id column (app-level isolation), migrate to PostgreSQL+RLS when real tenants arrive. The schema and logic stay identical.
- AI agent = **LLM API + function calling** (no self-hosted models needed at first); RAG = law texts chunked + embedded in SQLite FTS / lightweight vector store.

---

## 2. What we reuse from REM ERP (reference)

- **Design system:** `docs/design-reference-v10.html` (copied) — light theme, blue `#2F80ED`, SW versioned caching, manifest, mobile toolbar. Rebrand to KRTaker.
- **Backend pattern:** `app.py` (Flask + auth + CSRF), `db.py` (SQLite), `seed.py` (rich demo data), `portal.html` (customer portal with separate token auth).
- **Payment adapters:** bKash/Nagad adapter + sandbox checkout + shared `applyPaymentRipple` helper — bKash adapter maps directly to KRTaker's bKash Tokenized Checkout requirement.
- **Report generation:** reportlab (PDF invoices) + openpyxl (XLSX) endpoints.
- **Deploy stack:** gunicorn systemd unit + nginx reverse proxy + certbot (sslip.io hostnames) + Cloudflare tunnel for preview.

---

## 3. KRTaker Modules (prototype scope, mirroring REM's 53-module style)

Core groups (v1 — ~20 modules):
1. **Property Portfolio** — Properties, Units, Khatian/e-Porcha records, Jurisdiction (city corp)
2. **Tenancy** — Tenants, Leases (with registration-status engine), Security Deposits, Rent Schedules
3. **Financials** — Invoices, Rent Receipts (signed), TDS Ledger, Holding Tax, Capital Gains, Net Worth/Surcharge
4. **Compliance** — PRCA Checker, Eviction Workflow, Registration Tracker, Statutory Calendar
5. **Maintenance** — Tickets, Contractors, Liability Resolver, Repair Cost Ledger
6. **CRM** — Leads, Pipeline, Follow-ups, Sales Forecast
7. **Projects** — Renovation Tasks, Milestones, Resources, Cost Tracking
8. **Payments** — bKash Sandbox, SSLCommerz (later), Payment History
9. **AI Assistant** — Chat UI, Lease Q&A, Tax Answers, Autonomous Actions (issue ticket, send reminder, generate invoice)

---

## 4. Execution Plan (REM-ERP-style, 7 phases)

### Phase 0 — Scaffold & Brand ✅ (done: reference files copied)
- `/root/KRTaker` created; `docs/design-reference-v10.html` + backend reference files in place.

### Phase 1 — KRTaker PWA Prototype (SW v1.0)
- Fork REM V10 design → rebrand "KRTaker" (Key Responsibility Taker), blue theme retained.
- Build modules: Properties → Units → Tenants → Leases → Invoices → Receipts → Maintenance → Compliance → Taxes → Payments → AI Chat.
- **Lease registration gate** (doc requirement): lease >12 months → status "Pending Registration", ledger inactive until registration metadata uploaded.
- **PRCA hard validations** (doc): advance ≤ 1 month rent (residential) w/ manual override; rent-hike flags; eviction flow gated on 3-month unpaid detection.
- **TDS split on invoices** (doc): corporate tenant → net rent + TDS line (Sec 109 / 115 / 126 rates table).
- **Holding tax calculator** (doc): Gross Annual Rent → −2mo maintenance allowance → −mortgage interest → Net Annual Value → city corp rate.
- SW version `krtaker-v1-0`; serve on port 8878 + Cloudflare tunnel preview.

### Phase 2 — Backend Core (Flask + SQLite)
- Auth + RBAC (Owner/Admin/Agent/Tenant roles), JWT-ish tokens (reuse REM pattern).
- CRUD APIs for all core entities; tenant_id column on every table (app-level isolation now; PG-RLS later).
- `seed.py` — realistic BD demo: 5 properties (flat/plot/commercial), 8 units, 10 leases (mix ≤1yr & >1yr to demo registration gate), invoices, TDS cases, maintenance tickets.
- Reports: PDF rent receipt (crypto-signed header), holding tax statement, TDS certificate.

### Phase 3 — Compliance & Financial Engines (server-side)
- PRCA rule engine: rent revision window (2yr), advance cap, eviction grounds checker.
- Holding tax engine: Net Annual Value pipeline per doc formula.
- TDS ledger: auto-split, PSR tracking, +50% TDS rate if docs missing (doc mandate).
- Wealth surcharge bracket calculator (4Cr–50Cr+ table from doc).

### Phase 4 — Payment Gateways (sandbox first) ✅ LIVE
- `GATEWAYS()` config in `api/index.php` (bKash / SSLCommerz / Nagad, sandbox=true, placeholder live creds with swap instructions).
- New endpoints: `app-gateways`, `app-payment-init` (creates `gateway_tx` session), `app-payment-confirm` (atomic payment+receipt+invoice ripple via shared `record_payment()`), `app-payment-cancel`.
- In-dashboard simulated merchant checkout (sandbox page) → confirm → signed receipt; tenant scope enforced (own invoices only); partial-payment aware (due = net − paid).
- Dashboard "Pay online / Pay rent / Pay now" wired; billing history shows payments + gateway sessions.
- All Add buttons activated: payments record (invoice picker), rent-roll report, TDS challan, capital-gains estimator, partner quote, assign job, legal cases CRUD (`cases` table), staff invite/directory, support tickets, superadmin quick-add + ops tiles.
- Go-live: paste real bKash App Secret / SSLCommerz Store Password in `GATEWAYS()` → sandbox=false.

### Phase 5 — AI Agent (chat + function calling) ✅ LIVE
- KR widget wired to `app-ai-chat` (RBAC-gated, permission inheritance — tools run AS the calling user).
- **Dual mode**: DeepSeek function-calling when `KRT_DS_KEY` is set (6 tools: generate_invoice, create_ticket, check_lease_liability, calc_holding_tax, send_reminder, ask_legal); **offline mode** otherwise — rule-based intents (EN + বাংলা) + legal knowledge base, fully functional demo.
- RAG-lite: `legal_docs` + FTS5 (LIKE fallback) seeded with 18 curated entries (PRCA 1991 §10/§13/§18/§23, TPA §107/§108, IT Act 2023 §109/§128, holding tax GAR→NAV, khatian/e-porcha, NRB NRTA/NITA).
- Chat UI: suggestion chips, typing indicator, mode badge, `**bold**` markdown rendering, auto-refresh after actions; `ai_log` audit table.
- Go live: `export KRT_DS_KEY=sk-...` on the host (or paste in `AI_CONFIG()`) → KR upgrades to DeepSeek automatically.

### Phase 6 — Multi-tenant Hardening (PostgreSQL + RLS) ✅ delivered (adapted)
- **Finding**: shared cPanel host ships `pdo_pgsql` but runs **no PostgreSQL server** (probe: 5432 refused; cPanel doesn't provision PG). True PG+RLS is a VPS capability → Phase 7's backend move.
- **Delivered on the live SQLite stack** (app-layer RLS = the plan's *primary filter*):
  - Connection hardening: WAL, `synchronous=NORMAL`, `foreign_keys=ON`, `busy_timeout=5000`, `temp_store=MEMORY` (live: `journal_mode=wal`, `quick_check=ok`).
  - Auth hardening: login lockout (10 fails/email or 40/IP per 15 min → 429), OTP cap (5 wrong → code invalidated), register ≤8/IP/hr, resend ≤3/10 min; `auth_attempts` table + opportunistic prune.
  - Tokens **hashed at rest** (sha256 in `app_tokens`; lookup by hash — existing sessions invalidated once, users re-login).
  - **KR AI row-scoping** (closed live cross-tenant leaks): tenant sees only own leases/units/invoices/tickets (EN + বাংলা intents + LLM system prompt); partner sees only own jobs (financial intents politely declined); owner/staff org-wide. `ai_scope()`/`ai_q()` helpers.
  - `app-crud` row-scope guards: tenant update/delete blocked up-front; partner restricted to tickets.
  - New superadmin endpoints: `app-backup` (VACUUM INTO snapshot stream — DB sits outside FTP jail), `app-export` (23-table JSON, feeds PG import), `app-audit` (paged audit trail); `app-health` (unauthenticated integrity/journal/counts); security headers on API responses (nosniff/SAMEORIGIN/Referrer-Policy).
  - **PG migration artifact** ready for Phase 7: `docs/pg-migration.sql` (full DDL, `owner_id` + `app.current_tenant`, RLS policies on all 13 tenant tables via `krtaker_app`/`krtaker_staff` roles, FTS5→tsvector+GIN, PgBouncer hygiene notes) + `tools/pg_import.py` (idempotent JSON→PG importer).
- Verified: local E2E suite (lockout, OTP cap, token hashing, backup/export/audit, health) + live prod checks (tenant AI scoping, 403s, superadmin export, single-fail no false lockout). Commit `16f44ea`; assets `?v=3.5` / SW v13 unchanged (server-only deploy).

### Phase 7 — Deploy
- gunicorn systemd `krtaker-backend` + nginx (`krtaker.18.142.98.150.sslip.io`) + certbot; PWA on `app.…sslip.io`; Cloudflare tunnel for interim HTTPS. (Reuse REM deploy recipes.)
- **PG cutover path**: install PostgreSQL 14+ on the VPS → `app-export` from cPanel → `tools/pg_import.py` → apply `docs/pg-migration.sql` → point backend at PG with `SET app.current_tenant` after each auth (RLS backstop; keep API WHERE-clause scoping primary).
- **Status (2026-08-02)**: migration pipeline **validated locally** — PG 14, full import of the real export (23/23 tables), RLS proven (org isolation, cross-org INSERT blocked, staff BYPASSRLS). Verified snapshot at `/root/krtaker-backup/phase7-pre/krtaker_pg_dump_*.sql`. **VPS execution blocked on SSH** to `18.142.98.150` (keys denied) — see `docs/phase7-deploy.md` runbook + exact pubkey ask. Daily DB backup cron `934e56ba8240` live (00:00 UTC, 14-day rotation, watchdog).

---

## 5. Acceptance criteria per phase
- **P1:** Prototype renders all modules; registration gate + PRCA validations + TDS split + holding tax calc work in-browser; zero JS errors (browser verify).
- **P2:** All CRUD via API; seed loads; PDF/XLSX exports valid.
- **P3:** Engine unit cases pass (doc examples: ৳2.2Cr invoice, TDS 5–10%, holding tax formula).
- **P4:** bKash sandbox end-to-end payment + callback idempotency (reuse PAY-013/014 test methodology).
- **P5:** Chat answers legal Qs from RAG; agent actions complete with correct RBAC denial on cross-role tools.
- **P6:** RLS policy test: tenant A cannot read tenant B rows via direct SQL.
- **P7:** HTTPS live, boot-enabled, DB backed up.

---

## 6. Deliverables
- `/root/KRTaker` — repo (github.com/deshiklab/KRTaker), PWA prototype + backend + docs
- Phase-by-phase zips to Telegram (≤50 MB)
- Demo script + walkthrough like REM ERP

### Demo-complete batch (2026-08-02, commit `06d4245`, `?v=3.6` / SW v14)
- **KR AI: 6 → 15 tools** — added `rent_roll` (per-property/portfolio expected·collected·outstanding), `portfolio_summary` (props/units/occupancy/asset value/MRR/unpaid), `upcoming_renewals` (N-month lookahead), `collection_summary` (issued vs collected per month), `capital_gains` (IT Act §128 + surcharge brackets), `tds_summary` (by-month), `property_details`, `lease_details`, `tenant_details`. Offline intents expanded EN+বাংলা with `ai_month()` (YYYY-MM / English / Bengali month names → current year); intents reordered so month/analytics queries win over generic arrears; ভাড়াটে/ভাড়া regex disambiguated.
- **`app-profile`** — update name/org/phone (subs) or dept (staff) + password change (old-pw verified, sessions invalidated, forces re-login). Wired into a new **Settings drawer** (profile · password · gateway status) from the Org admin panel.
- **Real gateway adapters** — `gateway_init`/`gateway_verify` for SSLCommerz (init + validator API), bKash (token grant → create → execute), Nagad (DFS init/complete); active only when credentials are set in `GATEWAYS()` (otherwise simulated demo checkout). `app-payment-confirm` verifies via the gateway when `gw_ref` exists. Dashboard: real checkouts redirect to the gateway and auto-confirm on return (`gatewayReturn()` parses `?gw=&sid=&val_id/paymentID/order_id`).
- Verified locally + live (owner: rent roll ৳22.7L expected/৳17.6L collected; June collection ৳13.37L/৳13.69L; renewals; Bengali intents; profile flow; simulated gateway PAY-009/RCP-0010; 15 tools in `app-ai-meta`).

## 7. Open questions for Belal
1. **Repo:** push to `deshiklab/KRTaker` (public) or private `kabirswe/KRTaker`?
2. **AI provider:** DeepSeek (cheap, current) or OpenAI-compatible endpoint for the agent?
3. **Scope of Phase 1 prototype:** full 20-module build, or start with 8 core modules (Portfolio + Tenancy + Financials + AI Chat)?
4. **bKash sandbox creds** — need App Key/Secret from the bKash developer portal (or reuse REM sandbox pattern with placeholder creds).

### Demo-complete batch P25–P29 (2026-08-02, commits `eded233`→`6354407`, `?v=3.23` / SW v31, 559/559)
- **P25 per-property rent config + amenities** — `property_rent` (service charge %, utility advance, parking, escalation %, advance months, due day, late fee %, rent/sqft) + `amenities` (AM-### per property/unit, icon+category); `app-rent-config-get/save` RBAC (tenant → own property only); rent mix = base + service + parking; units.rent editable; idempotent live seed. Live proof: Dhanmondi mix ৳44,100 (40k + 1,600 + 2,500).
- **P26 tab systems** — ⚙️ Settings 5 tabs (Profile/Preferences/Security/Billing/Data-GDPR) + 🏢 Property drawer 4 tabs (Overview/Units/Rent/Amenities); fixed `krTabBar` missing `data-p`.
- **P27 move-out wizard** — `app-moveout`: prepare (10-item handover checklist auto-created + settlement snapshot + open tickets) → close (single transaction: settlement → checklist Completed → tickets closed → lease Terminated + unit Vacant → notice → `move_out` email #9). Live settlement: ৳41,500 due / balance ৳1,500 after 40k deposit.
- **P28 premium billing cycle** — `caretaker_invoices` (CI-###); `app-premium-billing` list/run/pay; **run uses X-Service-Key** (cron-friendly) and advances from sub's `next_invoice` (fixed loop-forever-from-today bug); monthly cron created (1st, silent when nothing).
- **P29 tenant insurance / rent guarantee** — `insurance_plans` (RG ৳499 / DS ৳299 / DC ৳199) + `insurance_policies` (POL-###); plans (score-aware premium 5–15% off) / apply (one per plan) / claim / decide (staff approve→paid / reject→active); hit and fixed PHP switch trap + GET action. Live E2E: apply IP-002 → claim ৳3,000 → approve → paid.
- **Live**: 9 email templates confirmed; all 4 new endpoint groups + rent-config live-tested; DB backup `/root/krtaker-backup/phase25-29/predeploy-20260802-152243.db`.

### Demo-complete batch P30–P31 (2026-08-02, commits `16b2a77`, `?v=3.24` / SW v32, 613/613)
- **P30 maintenance & repairs workflow** — `maintenance_requests` (MR-###; category/priority/status/assigned_to/vendor/cost_estimate/actual_cost/charge_to); `app-maintenance` list (tenant scoped to own lease unit, staff full, enriched names) / create (tenant auto-scopes unit) / assign (staff, Open→Assigned) / status / cost (auto Resolved when actual_cost set). **Bug fixed**: SQL CASE saw stale column value in same UPDATE → bind the new cost as a param. Frontend: Maintenance page = 5-column board (Open/Assigned/In Progress/Resolved/Closed) + stat cards; report-issue form; staff drawer actions; tenant-portal panel. Live E2E: Sultana create MR → blocked assign → manager assign/cost → auto Resolved ৳5,500.
- **P31 leasing pipeline (leads)** — `leads` (LD-###; status New→Contacted→Viewing→Applied→Leased→Lost); `app-leads` **public submit** (no auth, IP rate-limited 5/min — feeds from listing inquiries) + staff list/status/assign+notes (superadmin/owner/manager/crm). Frontend: 6-stage kanban pipeline + stat cards, add-lead form, detail drawer with stage transitions, nav/RBAC/mobile-bar wired (leads added to ALL + crm mods). Live E2E: public submit LD- → owner list 6+ → LD-001→Contacted → LD-002 assign.
- **Live**: ?v=3.24 / SW v32; API 411KB; seed idempotent; DB backup `/root/krtaker-backup/phase30-31/predeploy-20260802-164849.db`.
- **Regression**: 613/613 (all 21 suites; run_all.py now counts `PASS n/n` format + includes test_p28).


### Demo-complete batch P32–P33 (2026-08-02, commit `99e8c8a`, `?v=3.25` / SW v33, 679/679)
- **P32 owner statements & P&L** — `statement_payouts` (PK prop+month; amount/status/method/ref); `app-statements` **list** (per-property monthly: gross, collected, tds, service = gross×service_charge_pct, expenses = resolved/closed owner-charged maintenance in month, net = collected−tds−service−expenses; payout merged) / **detail** (per-unit line items + expense items + totals) / **payout** (upsert ON CONFLICT; superadmin/owner/accountant). RBAC: superadmin/owner/manager/accountant view, manager cannot record payouts. Frontend: Statements module (💰 Finance group, month picker 2026-05..08, 4 stat cards, per-property cards with payout chips, full-statement drawer with rent lines + expenses, record-payout form). Live proof: P-001 2026-06 net ৳22,150 (25,000 collected − 2,850 service); maintenance cost → expenses flow (P30 tie-in).
- **P33 compliance & expiry tracker** — `compliance_items` (CPL-###; entity_type/entity_id/item/label/ref_no/issue_date/expiry_date/last_reminded); `app-compliance` **list** (auto-syncs from live data: lease expiry ← active leases, insurance expiry ← policies, trade license + fire safety ← each property, NID ← tenants; status computed valid / expiring_soon ≤45d / expired; entity_label + days_left resolved) / sync / create / update / delete (owner only) / **remind** (ONE digest email listing all due items — avoids N×20s SMTP timeout; updates last_reminded). Frontend: Compliance upgraded from static stub → live tracker (Valid/Expiring/Expired stat cards, full table w/ days-left + status chips, add-document form, edit/delete). RBAC view: superadmin/owner/manager/svc_mgr/legal.
- **Bug fixed (live)**: remind originally sent 1 mail per due item → N×20s smtp_send hang on shared host → digest single mail.
- **Also**: `refreshData()` hardened with try/catch (demo/offline fetch rejection was throwing console errors).
- **Live**: ?v=3.25 / SW v33; API 430,740 B (probe: app-statements/app-compliance → 401 not 404); seed idempotent; DB backup `/root/krtaker-backup/phase32-33/predeploy-live.db` (618KB via app-backup API); live E2E 25/25 (statements list/detail/payout upsert NPSB-LIVE32 → Scheduled; compliance create/update→expiring_soon/remind/delete; tenant 403 both; bootstrap carries new collections; audit entries).
- **Regression**: 679/679 (23 suites; test_p32 29/29 + test_p33 37/37 added to run_all; p32 resets non-seed maintenance rows, p33 deletes non-seed compliance + policies).


### Demo-complete batch P34–P35 (2026-08-02, commit `34f65a5`, `?v=3.26` / SW v34, 737/737)
- **P34 utilities & service billing** — meter readings (app-meter-submit upsert, prev-reading guard) + tariffs (app-utility-tariff-get/save) existed from P20; added **app-utility-run** (batch: every leased unit × enabled tariff, deduped by unit via GROUP BY subquery, reading present → calc+upsert; returns generated/updated/skipped/total + bills; prop filter) and **app-utility-summary** (billed/paid/unpaid per month; tenant-scoped). Frontend: new **Utilities** module (🔌 ops group; superadmin/owner/manager/accountant + tenant own-unit view) — tariff editor, Run-billing month button, bills board with pay/void, tenant submit-reading + pay (upgraded legacy Phase-20 utilityBillPay to be context-aware + demo-mutating; removed my duplicate). Live proof: 2026-08 U-010 electric (4300−4125)×10 = ৳1,750; tenant paid own bill.
- **P35 lease renewals & rent escalation** — renewal_requests extended with **Offered** status: **app-renewal-offer** (staff; escalation = explicit param > property_rent.escalation_pct default > 0; new_rent = rent×(1+escal/100) unless explicit; notice + email; 409 if open Pending/Offered) + **app-renewal-respond** (tenant accept/decline; accept applies via shared **renewal_apply** helper — extends end by months, sets rent, reactivates, notice+email); app-renewal-decide now accepts Pending **or** Offered. Frontend: new **Renewals** module (🔄 workspace group) — pending/offered/recent pipeline, offer form with escalation prefill (L-007 → 7% → ৳42,800), approve/reject; tenant portal chips now show **Accept/Decline** for offers (demo accept also updates lease). Live proof: offer L-002 +6% → ৳33,920; default L-001 8% → ৳27,000; Sultana accepted RR-102 → L-007 ৳42,800, end 2027-12-14.
- **Bugs fixed**: escalation sentinel `max(0,-1)` clamped before property-default lookup → kept raw sentinel; utility batch double-processed units with 2 active leases → GROUP BY.
- **app-setup?reset=1** (key-guarded): wipes meter_readings/utility_bills/renewal_requests + restores demo leases (L-007 40k/2026-12-14, L-002 32k, L-001 25k) so canonical seeds land even after E2E pollution (live had P16-era leftover renewals/meters that blocked seed_app's count==0 guards).
- **Live**: ?v=3.26 / SW v34; API 447,766 B (probes: app-utility-run/app-renewal-offer/app-renewal-respond/app-utility-summary → 401 not 404); reset+seed run; DB backup `/root/krtaker-backup/phase32-33/predeploy-p3435.db` (667KB via app-backup API); live E2E 24/24; bootstrap now carries utility_tariffs/meter_readings/utility_bills/renewal_requests (staff full, tenant scoped, partner empty) + export lists.
- **Regression**: 737/737 (25 suites; test_p34 30/30 + test_p35 28/28; run_all resets tariffs to defaults for test_p20 parity).

### Demo-complete batch P36–P37 (2026-08-02, commit `49d0f4b`, `?v=3.27` / SW v35, 801/801)
- **P36 vendor management & partner payouts** — new tables `partner_invoices` (PI-###; Submitted→Approved→Rejected→Paid, decided_by/at) + `vendor_payouts` (VP-###; UNIQUE(partner,month) upsert, Scheduled/Paid, method/ref) + `qc_by`/`qc_at` columns on maintenance_requests (ALTER). **app-vendors** (10 actions): list (per-vendor computed open_jobs + approved_total + paid_total; partner sees ONLY own row), jobs (staff all · partner WHERE vendor=own name), job-status (partner own jobs only → 403 foreign; Assigned|In Progress|Resolved; Closed locked), job-qc (superadmin/owner/manager/svc_mgr sign-off Resolved→Closed + qc_by/qc_at), invoice-submit (partner own job, amount>0; staff may submit on behalf with partner param), invoice-list (scoped), invoice-decide (**verdict** param — `action` collides with dispatch), invoice-pay (owner/accountant, Approved→Paid, 409 double-pay), payout-record (upsert keeps id), payout-list. ROLE_MODULES: vendors for superadmin/owner/manager/svc_mgr/accountant + **partner**. Frontend **Vendors** module (🧰 ops group): staff → stats + vendor directory cards + job board w/ QC buttons + invoice review/pay + payout ledger (record form); partner → own stats (My open jobs/Awaiting payment/Paid to me) + My jobs (Start/Mark resolved) + My invoices (submit drawer, own-job select) + My payouts. Demo scoping: partner branch filters jobs by vendor name + invoices/payouts by partner id. Live proof: Rahim (SP-01) moved MR-001→In Progress, invoiced ৳9,500, manager approved, accountant paid; owner recorded SP-02 payout ৳4,200 bKash BK-LIVE-01.
- **P37 NRB remittance & payout tracking** — new table `remittances` (RM-###; owner_email/owner_name/prop/month/amount BDT/rate/amount_fx/currency/method/ref/form_c/status Sent|Confirmed/confirmed_at; UNIQUE(owner_email,prop,month) upsert). **app-remit** (list w/ month filter + owner self-scope, create → fx=round(amount/rate), owner-name resolved from subscribers/tenants, create roles superadmin/owner/accountant — manager list-only 403, status Sent→Confirmed, summary = GROUP BY month,status,currency,method + totals bdt/fx/count/confirmed). Frontend **Remittances** module (🌍 finance group; superadmin/owner/manager/accountant) — stats (Sent BDT / FX eq / count / Confirmed) + ledger table (FX + Form C + Confirm button) + **Money sent home** panel (per-month BDT/FX/records/status); record form (owner email, property, month, amount, rate, currency USD/EUR/GBP/AED/SAR/MYR, method SWIFT/bKash/Remitly/Western Union/Other, ref, Form C). Live proof: RM-100 owner@ P-003 2026-07 ৳46,000 @115 → 400 USD, upsert kept id, confirmed; summary total ৳12,96,600, confirmed 3.
- **Bugs fixed**: invoice-decide `action` param collided with dispatch action (→ `verdict`, matches app-insurance); demo partner view initially unfiltered (jobs/invoices/payouts now scoped by partner); splice-helper re-appended anchors on whitelist/bootstrap/export edits (5 spot-fixes after batch insert, php -l clean).
- **Live**: ?v=3.27 / SW v35 (28 files); API 474,452 B (probes: app-vendors/app-remit → 401 not 404); dashboard 506,703 B; app-setup?reset=1 canonical reseed (new tables count=0 → seeded); DB backup `/root/krtaker-backup/pre-p36_20260802_182054.db` (675,840 B via app-backup API); live E2E 26/26 (vendor lifecycle + remit fx/upsert/confirm/summary, tenant 403 both, manager remit-create 403); live browser verified Vendors (owner + partner scoping) + Remittances ledger/money-sent-home; pre-existing empty-message JS exception identical on deployed version (benign).
- **Regression**: 801/801 (27 suites; test_p36 39/39 + test_p37 25/25; p36 preamble resets MR-001..005 + cleans MR-01x, p37 resets remittances).

### Demo-complete batch P38–P39 (2026-08-02, commit `2ff6f18`, `?v=3.28` / SW v36, 857/857)
- **P38 tenant onboarding & e-KYC** — new table `onboarding_apps` (OB-###; lead/name/email/phone/nid/unit/prop/rent/adv/months/start/status Started→Submitted→Verified→Rejected→Completed, verified_by/at, tenant_id/lease_id/invoice_id). **app-onboarding**: start (from lead LD-### prefill or manual; superadmin/owner/manager/crm), submit (Started/Rejected→Submitted), verify (owner/manager; verdict approve|reject; only Submitted), complete (owner/manager; only Verified + unit must be **Vacant** (409 occupied) → creates tenant T-### (sub_email=applicant email → portal login), lease L-### (start/months/rent/adv, Active), move-in handover HOV-### (HOVO_ITEMS), first invoice INV-#### (start month, gross=net=rent), unit→Leased, lead→Leased, app→Completed+ids; double-complete 409). ROLE_MODULES: onboarding for superadmin/owner/manager/crm (crm list/start/submit only — verify 403). Frontend **Onboarding** module (📋 ops group): stats + pipeline table (lead name, NID, unit/property, rent/adv/months, actions per status: Submit KYC / Verify-Reject / Complete move-in; Completed shows T/L/INV chips) + start form (lead select prefill, vacant-unit select w/ rent, advance, months, start). Demo complete mutates DB (tenant/lease/handover/invoice + unit Leased). Live proof: OB-003 (Rima Sultana) completed → tenant/lease/invoice created, U-007 Leased, LD-004 Leased.
- **P39 maintenance SLA + vendor ratings** — tables `sla_config` (priority PK; urgent 4/24, high 24/72, medium 72/168, low 120/240) + `vendor_ratings` (VR-###; UNIQUE(partner,job) upsert; rating 1-5/comment/rated_by; **rolling avg onto partners.rating**). Helpers `sla_state` (created ts → response_due/resolve_due; status: done if Resolved/Closed → breached if past resolve OR past response while Open/Assigned → at_risk in last 25% of resolve → on_track) + `sla_summary`. **app-sla**: config-get/config-save (owner/manager; clamp 1–720, whitelist 4 priorities)/summary (counts on_track/at_risk/breached/done + per-item state; superadmin/owner/manager/svc_mgr/accountant). **app-vendors**: job-qc accepts optional rating+comment → vendor_rating_upsert when vendor maps to a partner (ElectroCool BD not in partners → skipped); new `rate` action (owner/manager/svc_mgr); list adds avg_rating+rating_count. Frontend: Maintenance gets **SLA panel** (4 mini-stats On track/At risk/Breached/Done + ⚙️ SLA config drawer with per-priority response/resolve hours) + SLA chips on each card; Vendors QC button opens **star-rating drawer** (5 stars + comment → job-qc with rating) + directory shows ★avg (count). Seed sets MR ts (-20d/-4d/-6d) so live shows breached=2/at_risk=1; demo SEED maintenance rows got ts fields.
- **Bugs fixed**: P36/P37 ROLE_MODULES splice left a DUPLICATE block (old entries won for duplicate keys — remit/vendors silently missing from require_module paths) → rewrote clean; splice anchor-re-append duplicated app-vendors list $rows[] push + job-qc tail + final error (2×SP-01 rows!) → cleaned; reset=1 now restores units U-003/U-007→Vacant + deletes onboarding leases/invoices/handover + restores MR-001..005 statuses/ts so live E2E is re-runnable.
- **Live**: ?v=3.28 / SW v36 (28 files); API 495,642 B (probes: app-onboarding/app-sla → 401 not 404); dashboard 528,669 B; DB backup `/root/krtaker-backup/pre-p38_20260802_184800.db` (729,088 B); reset+seed run; live E2E 26/26 (onboarding lifecycle incl. occupied-unit 409 + double-complete 409, SLA config/defaults/summary, QC+rating roll-up SP-02 4.5, tenant 403s); live browser verified Onboarding (4 rows + actions), Maintenance SLA panel + chips, Vendors ★ ratings; 0 JS errors.
- **Regression**: 857/857 (29 suites; test_p38 32/32 + test_p39 24/24; p38 cleanup deletes non-seed tenants/leases/invoices/handover + restores units/leads, p39 resets MR-001..005 statuses+ts + deletes vendor_ratings/sla_config).

### Demo-complete batch P40–P41 (2026-08-02, commit `9301192`, `?v=3.29` / SW v37, 898/898)
- **P40 AI Caretaker v2 — WhatsApp channel + proactive alerts** — tables `wa_channels` (user_key/phone/provider/status, UNIQUE(user_key,phone)) + `kr_alerts` (AL-###; user_key/type sla|compliance|arrears|renewal/severity critical|warning|info/title/body/ref/status open|dismissed/voice_note/resolved_at) + `kr_wa_msgs` (direction out|in, voice_note flag). **app-kr-alert**: `run` (**service-key only** → kr_alert_scan fans out to ALL active owners: SLA breach/at-risk ← sla_summary, compliance expired/expiring ≤45d ← compliance_items, rent arrears ← unpaid invoices older than 7 days, lease renewals ≤60d; upsert dedupes open + **24h cooldown on dismissed** so re-scans don't re-create) / list (own open only) / dismiss / dismiss-all. **app-kr-wa**: status (channel + recent msgs) / bind (validates phone ≥9 chars, re-bind reactivates) / unbind / send (validates, auto-creates channel, logs outbound; production note routes Twilio) / webhook (simulated inbound → KR reply). New KR tools + offline intents (EN+বাংলা): tenant_score (T-### → scorecard factors), vendor_status (SP-### → rating/jobs/approved/paid), remit_status (owner/manager/accountant; owner self-scoped), list_alerts (+dismiss-all command), wa_send (phone+message → channel+log), portfolio_forecast. WhatsApp intent placed BEFORE Bengali intents (ভাড়া regex was shadowing it). Settings prefs added: wa_phone, alert_sla/compliance/arrears/renewal. Frontend: AI module rebuilt (🤖 assist) — Proactive alerts panel (severity chips + dismiss ✓/dismiss-all + Run alert scan for staff), WhatsApp channel panel (bind input, message send, recent messages in/out), expanded capabilities (score/vendor/remit/forecast/WhatsApp); Settings drawer gets alert toggles + WhatsApp number row; KR quick chips +6.
- **P41 portfolio analytics & forecasting** — `board_reports` (BR-###; month/kind/payload markdown/created_by). **app-analytics** (superadmin/owner/manager/accountant/svc_mgr; tenant/partner 403): pnl (per-property gross/collected/tds/service/expenses/net + totals via statement_calc), trends (N-month issued=SUM(net)/collected=SUM(payments on inv month) + occupancy), aging (current/30/60/90+ by invoice month age + total), vacancy (Vacant units × market rent monthly/annual loss), forecast (12-month: avg issued × collection rate; risk = vacancy loss or renewals due), board (generates markdown report → BR-### + returns payload), boards (history). KR tool portfolio_forecast. Frontend Analytics rebuilt (📈 finance) — 5 stat cards (net/collected/occupancy/vacancy loss/12-mo forecast), per-property P&L table with totals row, issued-vs-collected 6mo bar, 12-month forecast bar, aging kv list, vacancy loss panel, **Board report** drawer (monospace markdown + Download .md). Demo SEED carries kr_alerts/wa_channels/kr_wa_msgs/board_reports.
- **Bugs fixed**: `service_key_ok()` didn't exist (→ `service_authed()`); alert `run` demanded user token before svc-key check (reordered); tenant_score used wrong scorecard keys (factors/tips); wa send auto-created junk channels without phone validation (added ≥9 check); re-bind didn't reactivate unbound channel (INSERT OR IGNORE skip → UPDATE reactivate); wa_channel_for returned junk short channels (LENGTH(phone)>=9 filter); stale "23 tools" assertions (→ >=23); patch-tool escape mangling on SQL (line-surgery fixes, php -l clean).
- **Live**: ?v=3.29 / SW v37; API 526,523 B (probes: app-kr-alert/app-kr-wa/app-analytics → service-key 401/401 not 404); dashboard 542,122 B; DB backup `/root/krtaker-backup/pre-p4041_20260802_193751.db` (2,117,632 B); live E2E 24/24 (wa bind/send/inbox, alert run svc-key + no-key 401 + dismiss, pnl totals, trends 6mo, aging, vacancy, forecast 12mo, board BR-###, tenant 403, KR forecast/vendor/WhatsApp/score); live browser verified AI Caretaker v2 (alerts + WhatsApp panels) + Analytics (P&L/forecast/aging/vacancy/Board drawer) logged in as owner, 0 JS errors.
- **Regression**: 898/898 (31 suites; test_p40 33/33 + test_p41 34/34; run_all resets kr_alerts/wa_channels/kr_wa_msgs/board_reports; p11/p12 "23 tools" → >=23).

### Demo-complete batch P42 — Contractor Marketplace v2 (2026-08-02, commit `f894438` + `06e40c6` + `f44ba29`, `?v=3.32` / SW v40, 943/943)
- **P42 vetted contractor marketplace + pay-on-signoff + job evidence** — table `job_media` (JM-###; job/kind before|after|memo|other/name/fname/size/mime/uploaded_by). MR gains `memo` (digital-memo text), `pay_release`+`pay_release_at` (QC release), `pay_paid`+`pay_paid_at` (invoice paid). **app-job-media** (staff/assigned-partner/own-unit-tenant scoped): list (media + per-kind counts + MR memo/pay meta), upload (multipart, images+PDF, 8MB cap, Closed jobs 409), memo (text), remove (unlink + row), view (bytes, scoped). **app-vendors** additions: `market` (Active pros w/ avg rating + rating_count + open_jobs + avg_cost + verified=sub_email, staff+tenant), `request-quote` (tenant own-unit scoped / staff any; creates Open MR pre-assigned to vendor; partner 403), `job-qc` gains `release_payment` (requires >=1 before + >=1 after + memo file/text else 409; closes job + marks Approved invoices `Payable` + MR pay_release), `invoice-pay` accepts Approved|Payable (paying a released job sets MR pay_paid), jobs list enriched with ev_before/ev_after/ev_memo/ev_total. Frontend: vendor directory cards gain Vetted badge + avg job cost + one-tap Request quote; job board gains Evidence column (count -> drawer) + released/paid chips; QC drawer shows evidence summary + "Release payment" toggle (enabled only when evidence ready, only when Approved invoice exists); evidence drawer (list + upload Before/After/Memo + memo textarea + remove + View links); partner "My jobs" rows gain Evidence button + pay chips; tenant maintenance gains "Find a vetted pro" -> marketplace drawer (pros + Quote); badge() maps Approved/Payable/Submitted. Demo SEED carries job_media + PI-004 Approved.
- **Bugs found & fixed**: foreach($rows as &$r) reference loop corrupted arrays before json_encode in PHP 8.4 (counts loop wrote through lingering reference -> duplicated first row; fixed with index-based cast); **SQLite bound-param trap `?=1` / `? > 0` never true** — PDO execute(array) binds TEXT, and `'1'=1` is FALSE in this SQLite build (type ordering, no affinity for literals) -> `CAST(? AS INTEGER)` for all numeric literal comparisons (also fixed pre-existing P30 latent bug: maintenance cost `? > 0` auto-Resolve never fired).
- **Live**: ?v=3.30 / SW v38; API 541,299 B (probes: app-job-media/app-vendors market -> 401 not 404); dashboard 558,804 B; DB backup `/root/krtaker-backup/pre-p42_20260802-202429.db` (876,544 B); live E2E 22/22 (market fields, tenant quote scoped, partner uploads before/after/memo + memo text + other-job 403, list counts, view bytes, jobs enriched, no-evidence 409, invoice submit/approve, QC release -> Payable -> Paid, pay-again 409); live browser verified Vendors v2 as owner (Request quote buttons, Vetted badges, Avg cost, Evidence column, MR-007 released + paid chips, evidence drawer w/ 3 files + View links), 0 JS errors.
- **Regression**: 943/943 (32 suites; test_p42 45/45; run_all resets job_media + MR pay/memo flags + Payable->Submitted rollback).

### Demo-complete batch P43 — Trust Engine v2: NID verification + Thana TIF (2026-08-03, commit pending, `?v=3.33` / SW v41, 1005/1005)
- **P43 Trust Engine v2** — tables `nid_verifications` (NV-###; tenant/nid/dob/status verified|unverified|mismatch/method/checksum_ok/age_ok/notes/verified_by/verified_at) + `thana_forms` (TF-###; tenant/unit/prop/thana/district/status Draft|Submitted|Verified/payload JSON/created_by/submitted_at). `nid_validate()` = **pure BD 17-digit check-digit algorithm** (weights 2–9 ×2 over first 16 digits, check=(11−sum%11)%11, 10→0; zero external deps, no API key) + legacy 10-digit format-only pass. **app-trust** (12 actions): summary (staff full / tenant own / partner 403), nid-validate, nid-list (staff all · tenant own via `trust_tenant_id` sub_email match), nid-save (staff only; **upsert one active row per tenant** — old rows superseded in place; syncs tenants.nid), nid-status (staff manual override verified|unverified|mismatch), tif-list / tif-create (staff or tenant self) / tif-get (own-or-staff, 403 foreign) / tif-save (Draft|Submitted editable, **Verified locked 409**; `tif_save_payload` writes whitelisted keys) / tif-submit (Draft→Submitted) / tif-verify (staff approve|reject) / **tif-print** (bilingual printable HTML, echo+exit). ROLE_MODULES: `trust` for superadmin/owner/manager/svc_mgr/legal/accountant/tenant; GET whitelist; bootstrap branches (staff full, tenant scoped, partner empty); export list; reset=1 wipe; demo seeds use **valid check-digit NIDs** (T-003 `19870123456789040`, T-004 `19880108990011031`) + NV-003 corrupted for mismatch demo. KR AI tool `nid_check` + offline intent (EN+বাংলা) before legal-KB fallback.
- **Frontend** (dashboard 583,433 B): new 🪪 **Trust Engine** module (Operations group) — 4 stat cards (NID verified / NID records / Forms verified / Forms pending), NID verification ledger (check-digit valid/fail badges, Mark-verified + mismatch actions, verified-by chips), Thana forms table (status badges, ✏️ edit, verified lock), Verify-NID drawer (tenant select + NID + DOB), 26-field bilingual TIF drawer (present/permanent addresses, employer, thana/district), Submit-to-thana + Verify approve/reject, 🖨 Print → `previewDoc` iframe (fetch+Bearer for live; demo builds HTML from payload). Demo parity: SEED rows NV-001..003/TF-001..002, tenant self-service (create my form), demo mutations.
- **Bugs found & fixed**: `tfSave` spread bug — `...payload` carried a stale `undefined` id and dropped the `id` key in JSON.stringify (new-form path broke live); demo tenant-scoping — `x.sub_email===LIVE.user?.email` with `LIVE.user=null` matched the FIRST tenant lacking `sub_email` (all demo tenants lack it → scoped to T-001) → guard on truthy email then fall back to Sultana; **P41 aging flake made canonical**: Aug 2026 ৳35k invoice now seeded in run_all reset on **L-006** (`INV-2026-010`) — original fix put it on L-007 which broke P14's `invs[0]` Paid assertion (seed idempotency: DELETE stale INV-2026-009/010 first); smoke `mk_nid` generator needed 6+10+1 digits (was 6+5+1=12).
- **Live**: ?v=3.33 / SW v41; API 571,880 B (probe app-trust → 401 not 404); dashboard 583,433 B; DB backup `/root/krtaker-backup/pre-p43_20260803_065738.db` (892,928 B); app-setup?key=kr-phase2-2026 (schema+seed, nid_verified 2 / nid_total 3 / forms 2); live E2E **38/38** (unauth 401, partner 403, staff+owner+tenant summaries, validate valid/corrupt/legacy-10/13-digit/400, nid-list staff≥3 + tenant own-only, nid-save upsert+sync+404+tenant 403, nid-status mismatch+bad+403, tif list/create/get/save/submit/verify/lock 409/print HTML/tenant 403s/tenant self-service); live browser verified Trust Engine as owner demo (3 NID rows + badges, forms table) 0 JS errors; demo rig re-verified (Verify NID drawer saves new row, TIF drawer 26 fields, print preview renders bilingual HTML, tenant scoping → Sultana only).
- **Regression**: 1005/1005 (33 suites; test_p43 62/62; run_all P43 reset: wipe non-seed NV/TF + restore tenant nids + NV-003 unverified + TF statuses).

### Demo-complete batch P44 — Legal Engine v2 (2026-08-03, commit pending, `?v=3.34` / SW v42, 1065/1065)
- **P44 AI-Powered BD Legal & Compliance Engine** (USP-D) — tables `legal_notices` (LN-###; ntype eviction|rent_hike|termination|tds_alert, lease/tenant/unit/reason/body, notice_days, effective_date, served_on/by, status Draft|Served|Void) + `case_events` (EV-###; case_id/ev_type note|stage|hearing|doc/body/actor) + **`cases` upgraded** with stage (Notice|Negotiation|Legal Notice|Hearing|Resolved), lawyer, next_hearing, updated_at (PRAGMA-guarded ALTERs). Config lives in `org_settings` under `legal_*` keys (eviction 90d, rent_hike 60d, termination 30d, TDS commercial 10% / residential 2%, deposit caps res 2× / com 3×). **`app-legal`** (module `legal` for superadmin/owner/manager/svc_mgr/legal/accountant + tenant own-view, partner 403): summary (staff org-wide / tenant scoped), config-get/save (owner/manager; clamp 1–720), notices-list/create/get/serve/void/print (`legal_notice_build` computes days + effective date from config; `legal_notice_html` bilingual printable with statutory references; echo+exit), audit (`lease_audit` rule engine: deposit > cap → high, term > 12mo without reg → high TPA §107, commercial missing reg_office → med, commercial TDS → info, end within 60d → med; score = 100 − 25×high − 10×med), audit-all (portfolio avg/high), tds-due (commercial Active/Pending leases × rate via `tds_due` — joins property through **units.p** since leases have no property FK), cases-list/create/get/update/event (stage timeline). **KR tools** gen_notice/lease_audit/tds_due/case_status + offline intents placed **before the generate-invoice intent** (`generate` regex would steal "generate an eviction notice for L-007") and **before the tds catch-all** ("what is the TDS due this month?" was matching tds_summary).
- **Frontend** (dashboard 611,270 B): new 📜 **Legal Engine** module (Operations group, after Compliance) — 4 stat cards (staff: Open notices/Active cases/Audit avg/TDS due; tenant: My notices/Active cases/My lease/Rent), Statutory notices table (type EN/BN + badge + 🖨/Serve/Void), Lease compliance audit panel (per-lease score ring + findings codes; ⚖️ drawer with severity chips + full portfolio run), TDS obligation table (per commercial lease + total), Dispute cases docket (stage badges, counsel, next hearing, 👁 timeline drawer with stage-update form + add-event). Demo parity: SEED legal_notices LN-001/002 + cases CASE-001/002 + case_events EV-001..003; demo audit/TDS computed client-side (JS mirrors of the PHP rules); tenant demo scoped to T-002 rows only.
- **Bugs found & fixed**: renderer must be keyed by module id (`legal(){` not `legalEngine(){` — `render()` dispatches `renderers[state.view]`, so a mismatched method name silently falls back to the role dashboard); missing `legalCaseBadge` (node --check passes but runtime throws — the demo loader caught it); `tds_due` joined `p.id=l.p` but **leases has no property column** (units carry `p`) → 500; P14/P41 flake from test_p44's first (pre-fix) run leaving an AI-generate-invoice `INV-2026-0011` (L-007, 2026-08, 40k, Unpaid) — run_all reset now deletes `l='L-007' AND m='2026-08'` leftovers.
- **Live**: ?v=3.34 / SW v42; API 608,960 B (probe app-legal → 401 not 404); dashboard 611,270 B; DB backup `/root/krtaker-backup/pre-p44_20260803_095400.db` (937,984 B); app-setup?key=kr-phase2-2026 (schema + seeds: notices 2, cases 2, events 3); live E2E **42/42** (unauth 401, partner 403, summaries, config round-trip, notice create/get/serve/print/void + tenant 403s, audits incl. L-004 TDS finding, tds-due, cases lifecycle + tenant 403s, 4 KR intents); live browser verified Legal Engine as owner demo (stats, LN-001/002, TDS ৳1.4L, cases) 0 JS errors; demo rig re-verified (generate → LN-591, print iframe, case stage → Hearing, tenant scoping + tenant stats).
- **Regression**: 1065/1065 (34 suites; test_p44 60/60; run_all P44 reset: wipe non-seed notices/events/cases + restore seed stages/statuses + drop legal_% org_settings).

### Demo-complete batch P45 — Land/Plot Guard (2026-08-03, commit `87ee7c0`, `?v=3.35` / SW v43, 1211/1211)
- **P45 anti-encroachment land/plot monitoring (USP-C, NRB owners)** — tables `land_parcels` (LP-###; owner_email/name/district/upazila/mouza/khatian/dag/area/lat/lng/monitor none|quarterly|semi_annual|annual/status Secure|Needs Review|Encroached|Under Monitoring/risk/notes/created_by) + `land_visits` (LV-###; parcel/agent/visit_type field|drone/scheduled_for/completed_at/status Scheduled|In Progress|Completed|Missed|Cancelled/outcome/findings/checklist JSON/completed_by) + `land_media` (LM-###; visit/kind photo|video|drone/name/fname/size/mime/geo lat,lng/captured_at/uploaded_by) + `land_events` (LE-###; parcel/ev_type created|visit|status|encroachment|note/body/actor). **`app-land`** (module `land` for superadmin/owner/manager/svc_mgr; tenant/partner/legal/accountant 403; owner self-scoped by email + org '' parcels): summary (total/secure/review/encroached/monitored/due_soon/overdue/coverage%/alerts), list (parcels+visits+media+events), get/create/save/status (Encroached → risk high + encroachment event)/delete (cascades visits/media/events), visit-list/visit-schedule (YYYY-MM-DD validated)/visit-complete (Scheduled|In Progress only 409; outcome auto-updates parcel + event; proof entries → LM rows)/visit-cancel (Completed 409)/visit-proof (Completed only)/media-list, report (bilingual `land_report_html` echo+exit), alerts (computed: encroached/overdue/due_soon ≤30d), config-get/save (org_settings `land_*`: default_monitor/due_soon_days clamp 1–90/visit_reminder_days). `land_enrich()` computes next_due (pending scheduled visit WINS → missed visits read as overdue; else monitor interval from last completed/created) + due_days + media/events counts. **KR tools** land_status/land_visit_due/report_encroachment/land_summary; intents fire on `(LP-\d+)` ALONE (the LP- prefix is unambiguous — requiring a 'land|plot' keyword made "status of LP-001" fall through to the legal KB) + land+visit-due + land-guard-summary; **kr_alert_scan** gains a `land` pass (email-owned parcels only → fan to that owner's sub:N; Encroached → critical, overdue → warning, due ≤14d → info; deduped by (user_key,type,ref)).
- **Frontend** (dashboard 649,758 B): new 🛰️ **Land Guard** module (Operations group, after Trust Engine) — 4 stat cards (Parcels/Secure/Needs review/Encroached), **Land alerts panel** (🚨 Critical / ⛔ Overdue / 📅 Due soon), parcels table (name/owner-email, location, area, monitor badge, last visit + outcome, next due badge red/orange/blue, status badge, 👁/📅/🖨 actions), Upcoming visits + Visit history panels (complete/cancel actions), Register-parcel drawer (name/district/upazila/mouza/khatian/dag/area/lat/lng/monitor/notes), parcel detail drawer (status selector + update, timeline with color-coded events, visits list, proof media with geo + captured-at), schedule-visit drawer, complete-visit drawer (outcome + findings + proof entries `kind | name | geo | captured_at`), printable bilingual Land Guard Report via `previewDoc` (live fetch+Bearer; demo builds HTML client-side). Demo parity: SEED carries land_parcels/visits/media/events (LV-004 +85d / LV-005 −5d dynamic so overdue+due-soon always demo), `landEnrichDemo()` mirrors the PHP math, demo owner fallback `LIVE.user?.email || 'owner@krtaker.com'` (null-user demo scoping, P43-class fix).
- **Bugs found & fixed**: (1) **P40 latent audit corruption** — `substr($msg, 0, 60)` in BOTH wa-send audit calls truncates multibyte Bengali mid-codepoint → invalid UTF-8 in audit_log → `app-export` json_encode returns false → **empty HTTP 200** (the P45 export test caught it; 15+ bad rows accumulated from every P40 wa test run). Fix: `mb_substr($msg, 0, 60, 'UTF-8')` at both sites + **`json_out` hardened with `JSON_INVALID_UTF8_SUBSTITUTE`** (server never empty-200s on bad DB content again); rig audit_log wiped (all audit assertions are create-then-count, safe). (2) `openDrawer(title, body, foot)` — land drawers initially passed ONE HTML string → escaped as drawerTitle text; rewrote to 3-arg signature (same as legal/vendors drawers). (3) Demo owner scoping matched only org parcel (LIVE.user null in demo) → fallback email. (4) scope-vs-404 ordering in get/delete/report/visit-schedule: fetch row first (404) THEN scope check (403).
- **Live**: ?v=3.35 / SW v43 (53 files); API 655,892 B (probe app-land → 401 not 404); dashboard 649,758 B (byte-identical to repo); DB backup `/root/krtaker-backup/pre-p45_20260803_104342.db` (1,028,096 B, integrity ok); app-setup?key=kr-phase2-2026 (schema + seeds: 4 parcels/5 visits/4 media/8 events); live E2E **34/34** (RBAC, summary totals, list, create/get/save/status, visit schedule+complete+409s, report HTML, config round-trip, 3 KR intents, bootstrap scoping, export tables, delete); live browser verified Land Guard as owner demo (4 parcels, stats 4/2/1/1, Overdue+Critical alerts, visit panels) 0 JS errors; demo rig verified full lifecycle (schedule LV-985 → complete with proof → media/events/status sync, parcel drawer, report preview, tenant has no land nav).
- **Regression**: 1211/1211 (35 suites; test_p45 120/120; run_all P45 reset restores parcel/visit/media/event seed state + deletes land kr_alerts + land_% org_settings).

### Demo-complete batch P46 — NRB Hub (2026-08-03, `?v=3.36` / SW v44, 1317/1317)
- **P46 NRB Hub (USP §3 — tax & repatriation, end-to-end vacancy management, virtual dispute resolution)** — tables `nrb_tax_returns` (TX-###; owner_email/prop/fy/rental_income/tds_deducted/tax_estimate auto `max(0, round(income*0.13)-tds)`/status Draft|Filed|Paid/ca_firm/ca_ref/filing_date/paid_date/notes; Paid terminal) + `nrb_repatriations` (RP-###; amount_bdt/amount_fx `round(amount/rate,2)`/rate/currency/method SWIFT|bKash|Remitly|Western Union|Other/bank_channel NRP_Account|BB_Approval|Overseas_Transfer/documents JSON/status Requested→Docs_Submitted→Submitted→Approved→Completed (+Rejected; forward-seq only, Completed/Rejected locked)/submitted_at/approved_at/completed_at/ref) + `nrb_vacancies` (VC-###; owner_email/prop/unit/status Vacant|Marketing|Showing|Vetting|Awaiting_Approval|Filled|Cancelled/started_at/tenant_left/target_rent/channels JSON/listing_url/candidate JSON/vet_status none|pending|passed|failed/approved_at/notes) + `nrb_showings` (SH-###; vacancy/prospect/phone/scheduled_at/status Scheduled|Done|No_Show|Cancelled/feedback/rating 1–5) + `nrb_disputes` (DS-###; owner_email/prop/unit/tenant/dtype eviction|damages|arrears|other/advocate/fee/status Open|Notice_Served|In_Progress|Escalated|Resolved|Closed/notice_ref/last_update/next_action/timeline JSON). **`app-nrb`** (module `nrb` = superadmin/owner/manager/accountant/svc_mgr/legal; tenant/partner/crm/hr 403; owner self-scoped by email + org '' rows): summary (tax/repat/vacancies+enrichment+pending_approval/showings/disputes+timeline/config — enrichment: unit_name/prop_name/showings/candidate/channels parsed, days_in_status), tax-list/create/save/status (Draft→Filed→Paid with date stamps; Paid locked 409), repat-list/create/docs (→Docs_Submitted)/status, vacancy-list/create/save/candidate (candidate object + vet)/status (**Awaiting_Approval requires candidate name + vet passed → 400**)/**approve** (the USP killer flow: only Awaiting_Approval; auto-creates tenant T-### with sub_email, Active lease L-### via DateTime +months, move-in handover HOV-###, first invoice INV-#### via invoice_next_id, unit→Leased, vacancy→Filled, system notice; double-approve 409)/cancel (Filled 409), showing-list/create (date validated)/save/status (only Scheduled can change; Done sets rating+feedback), dispute-list/create (default advocate from config)/save/status (notice_ref settable on Notice_Served; timeline event appended per transition; Resolved/Closed locked)/event (append timeline + last_update), config-get/save (org_settings `nrb_*`: default_ca_firm/default_advocate/vacancy_alert_days/dispute_alert_days clamp 1–120; superadmin/owner/manager only). 404-before-403 (fetch row → unknown 404, then owner-scope 403). **KR tools** nrb_tax_status/nrb_repat_status/nrb_vacancy_status (single VC-### or pipeline + pending count)/nrb_dispute_status (single DS-### w/ timeline or list); intents placed **before generate-invoice**: `(DS-\d+)`/`(VC-\d+)` prefix-gated (no keyword needed), `(TX-\d+)|tax return|ট্যাক্স রিটার্ন|কর রিটার্ন`, `(RP-\d+)|repatriat|রেমিট্যান্স|দেশে পাঠানো`, dispute keyword `dispute|বিরোধ|মামলা|evict|উচ্ছেদ`, vacancy keyword **`vacanc|ভ্যাক্যান্সি|approval|অনুমোদন|লিজ সই`** (deliberately NOT `vacan`/`খালি` — those steal the old occupancy intent "which units are vacant"/"কোন ইউনিট খালি"). **kr_alert_scan** gains an `nrb` pass: vacancy Awaiting_Approval >2d → warning "Vacancy awaiting your approval", Vacant/Marketing/Showing/Vetting >30d → info, dispute open >14d (non-Resolved/Closed) → warning; fanned to the owning subscriber's sub:N (email match). Bootstrap staff full / tenant+partner empty; export list + reset=1 wipe; seed TX-001/002, RP-001/002, VC-001..003 (VC-002 = Awaiting_Approval with passed vet + Farhana candidate), SH-001..003, DS-001/002 (eviction w/ 3-event timeline + damages).
- **Frontend** (dashboard 708,461 B): new 🌐 **NRB Hub** module (Finance group, after Remittances) — 3 sub-tabs (🏠 Vacancies / 🧾 Tax & Repat / ⚖️ Disputes) with a tab switcher; **Vacancies tab**: 4 stat cards (Tracked/Awaiting approval/Showings/Filled), **⏳ Awaiting-your-approval banner** (amber panel listing lease-ready vacancies with candidate name, rent, vetting, start date + big ✅ Approve button + 👁), vacancy pipeline table (unit/property/target rent/channels chips/showings/candidate+vet badge/status/👁+📅), upcoming showings panel (✅/✕ per scheduled showing); vacancy detail drawer (candidate card with set-candidate form incl. NID + vet select, showings list w/ complete/no-show actions, cancel-vacancy), **Approve & sign lease** button → confirm → toast with created T/L/INV ids; **Tax & Repat tab**: tax returns table (FY/income/TDS/est/CA firm/status + ✏️/File/Paid actions), repatriation ledger (amount/FX/channel/docs chips/status + 📎 Docs / → next-stage), create drawers (tax: auto est. preview; repat: live FX preview), docs attachment drawer; **Disputes tab**: 4 stat cards (tracked/open/evictions/legal fees), disputes docket (tenant/type/advocate/notice ref/next action/status/👁), dispute drawer (advocate card, last-update + next-action, **timeline** with events, stage-advance button + edit + add-event; notice-ref input on Notice_Served), open-dispute drawer (default advocate from config); ⚙️ settings drawer (CA firm/advocate/alert days). Demo parity: SEED carries all 5 nrb tables (VC-002 pending-approval demo), `demoNrbEnrich()`/`demoNrbData()` mirror the PHP enrich + status counts, **all 17 mutators have DEMO_MODE branches** (client-side DB mutation mirroring the API — demo approve creates tenant/lease/invoice rows, flips unit Leased, vacancy Filled), owner demo scope falls back to `LIVE.user?.email || 'owner@krtaker.com'`.
- **Bugs found & fixed**: (1) **PHP functions nested inside another function (class-level, splice hazard)** — the `after()` splice inserted the nrb helpers BETWEEN `function kr_alert_next_id($pdo) {` and its body → nested declarations only exist after kr_alert_next_id RUNS → "Call to undefined function nrb_tax_rows" 500s. Fix: insert BEFORE the anchor line (rep with anchor re-appended). (2) `x ?: y ?: z` unparenthesized ternary (PHP 8 fatal) in vacancy-save channels decode → parens. (3) `datetime('now')` inside a PHP single-quoted string needs `\'` escape (seed dispute INSERT). (4) **Intent collision (P8910 regression)**: P46 vacancy intent regex `vacan|খালি` stole "Which units are vacant?" (occupancy) — `vacan` matches "vacant" and `খালি` is the occupancy Bengali word; narrowed to `vacanc|ভ্যাক্যান্সি|approval|অনুমোদন|লিজ সই` (matches "vacancy" not "vacant"). (5) **P41 aging landmine**: test_p46's KR invoice-intent check (`generate invoice for L-001`) left INV-2026-0011 (L-001, 2026-08, 25k) → aging total 1297000/current 60000; test self-cleans + run_all P46 reset deletes `l='L-001' AND m='2026-08'`. (6) Frontend `tax.map(t=>…)` shadowed the global `t()` translator → "t is not a function" → renamed callback to `x`. (7) Demo mutators called apiCall (404 on static server) → DEMO_MODE branches added to all 17.
- **Live**: ?v=3.36 / SW v44 (28/28 web files, sw.js krtaker-site-v44; dashboard has no version string — the file itself is the bump); API 712,081 B (probe app-nrb → 401 not 404); dashboard 708,461 B (byte-identical to repo, nrbLoad present, health ok); DB backup `/root/krtaker-backup/pre-p46_20260803_122838/` (integrity ok); app-setup?key=kr-phase2-2026 (schema + seeds); live E2E **43/43 core + approve flow verified live** (RBAC 401/403, summary totals incl. showings key, GET whitelist, tax create→Filed→Paid + Paid-lock 409, repat create→docs→Submitted→Completed + backward 409, vacancy create→candidate→Awaiting_Approval→**APPROVE → T-106/L-102/HOV-005/INV-2026-0016, VC-100 Filled, double-approve 409** (needed U-003 temporarily Vacant via app-crud — no vacant units on live), showing create→Done + 409, dispute create→Notice_Served→event→Resolved + lock 409, config round-trip, 2 KR intents, bootstrap scoping/seed); live browser verified NRB Hub as owner (4 tracked / 1 awaiting approval banner with Farhana VC-002 / 1 Filled, tax + disputes tabs with live rows incl. smoke DS-100 Resolved) **0 JS errors**; demo rig verified all 3 tabs + approve flow (VC-002 → Filled, U-002 → Leased, tenant created) + drawers + timeline.
- **Regression**: 1317/1317 (36 suites; test_p46 106/106; run_all P46 reset restores nrb seed rows/statuses/candidate/vet + deletes non-seed nrb_* + nrb kr_alerts + nrb_% org_settings + approve-flow leases/invoices/handovers/tenants + U-002/U-003 → Vacant + L-001 Aug invoice cleanup).

### Demo-complete batch P47 — Legal Concierge (2026-08-03, `?v=3.37` / SW v45, 1440/1440)
- **P47 Legal Concierge (USP VAS-B — namjari/mutation, e-Porcha/Khatian extraction, holding-tax payments tracked via CRM)** — tables `concierge_requests` (CR-###; owner_email/parcel LP-###/prop/service namjari|e_porcha|khatian|holding_tax|registration/district+upazila+mouza+khatian+dag (prefilled from land_parcels)/status Submitted→Under_Review→Docs_Requested→In_Progress→Awaiting_Fee→Completed (+Rejected/Cancelled; forward-only, terminal locked, **Completed requires fee paid when fee>0**)/assigned_to/est_days/fee/fee_status unpaid|paid/fee_paid_at/service_ref/awaiting_fee_at/completed_at/notes/timeline JSON) + `concierge_docs` (CD-###; request/kind application|porcha|khatian|mutation_cert|holding_bill|nid|other/name/fname/size/mime/uploaded_by; files stored in DATA_DIR) + `holding_taxes` (HT-###; owner_email/parcel/prop/city_corp/ward/holding_no/fy/annual_value/rate_pct/tax_amount auto `round(NAV×rate/100)`/paid_amount/status Due|Paid (+effective **Overdue** when past due_date)/due_date/paid_date/receipt_no). **`app-concierge`** (module `concierge` = superadmin/owner/manager/svc_mgr/legal/accountant/**crm**; owner self-scoped by email + '' rows; tenant/partner 403): summary (requests by_status/by_service + fees_due, holdings enriched + due_total, config), request-list/create (fee/est default from config, parcel prefill)/get (404-before-403)/update (owner only while Submitted; staff any non-terminal)/status (forward-only + fee gate + service_ref set)/event/fee (pay + double 409), doc-upload (multipart images+PDF 8MB)/list/download (scoped bytes echo+exit)/remove, holding-list/create (tax auto)/save (Paid locked)/pay (Due|Overdue→Paid + receipt; double 409), config-get/save (org_settings `concierge_*`: agent + 5 service fees + 5 est days + docs_stuck_days + holding_alert_days; **fees clamp 0–100000, days 1–365, alert days 1–120** — a single `min(365,…)` clamp accidentally capped fees at 365 in v1, caught by the rig test). **KR tools** concierge_status (CR-### w/ timeline or list)/holding_tax_status (HT-### or ledger)/concierge_summary; intents `(CR-\d+)`/`(HT-\d+)` prefix-gated + `namjari|মিউটেশন|e-porcha|ই-পর্চা|khatian|খতিয়ান|holding tax|হোল্ডিং ট্যাক্স|concierge|কনসিয়ার্জ` keyword, all BEFORE generate-invoice. **kr_alert_scan** concierge pass: Awaiting_Fee unpaid >7d → warning, Under_Review/Docs_Requested/In_Progress stale >docs_stuck_days → info, holding tax overdue → warning, due ≤holding_alert_days → info (owner-email → sub:N).
- **Frontend** (dashboard 741,262 B): new 🗂️ **Legal Concierge** module (Operations group, after Land Guard) — 2 sub-tabs via `state.conciergeTab` (🗂️ Requests / 🏛️ Holding Tax) + ⚙️ Settings (owner/manager): Requests tab 4 stat cards (Tracked/Active/Awaiting fee ৳/Completed) + requests table (service badge, parcel/property, status, agent, est, fee+paid chip, 👁), Holding tab 4 stats (Holdings/Due/Overdue/Due total) + city-corp table (NAV @ rate → tax, status Due/Overdue/Paid, 📥 Pay or receipt); drawers: new request (service select with **fee/est auto-preview from config**, parcel select filtered by owner scope), request detail (badges, location, notes, **timeline**, docs list + upload/remove, **status advance select** (8 statuses), mark-fee-paid, add-note), add holding (live tax preview `NAV×rate/100`), pay holding (receipt no), config (agent + 5 fees + 5 days + 2 alert days). Demo parity: SEED carries concierge_requests (CR-001 In_Progress w/ 3-event timeline + ref, CR-002 Completed, CR-003 Awaiting_Fee unpaid) + concierge_docs (CD-001/002) + holding_taxes (HT-001 Due +28d, HT-002 Paid w/ receipt, HT-003 due 2026-07-15 → **Overdue demo**); `demoConciergeEnrich()`/`demoConciergeData()` mirror PHP (effective Overdue + scoping by `LIVE.user?.email || 'owner@krtaker.com'`); **all 10 mutators have DEMO_MODE branches** (create/advance/fee/event/doc upload+remove/holding create/pay/config — demo upload appends meta row, no file).
- **Bugs found & fixed**: (1) config clamp applied `min(365,…)` to FEE keys too → 5500 clamped to 365 (config-save returned namjari_fee=365); split fee/day/alert clamps. (2) Test bugs: timeline count (create adds 1 event not 2), summary text lacks service words, export nests under `_tables`, alert scan ran after HT-003 was already paid in the holding section (restore seed first). (3) Frontend `৳`+money() double-taka in the Awaiting-fee stat (money() already prefixes ৳).
- **Live**: ?v=3.37 / SW v45 (53/53 files); API 755,822 B (probe app-concierge → 401 not 404); dashboard 741,262 B (byte-identical to repo); DB backup `/root/krtaker-backup/pre-p47_20260803_141512/`; app-setup?key=kr-phase2-2026 (schema + seeds: 3 requests/2 docs/3 holdings/13 config keys); live E2E **51/51** (RBAC, summary, request lifecycle incl. fee-gated completion + locks, parcel prefill from LP-004, doc upload/download/remove bytes, holding create/pay/locks, config round-trip + tenant 403, 3 KR intents, bootstrap scoping, export, GET whitelist); live browser verified Legal Concierge as owner (5 tracked / 3 active / ৳1,500 awaiting fee / 2 completed; holdings 4 tracked with HT-003 Overdue + smoke HT-100 Paid receipt) **0 JS errors**; demo rig verified all tabs + drawers + create/advance/fee demo mutations + role gating (owner/crm/accountant/legal/svc_mgr ✓, tenant/partner ✗).
- **Regression**: 1440/1440 (37 suites; test_p47 123/123; run_all P47 reset wipes non-seed CR/CD/HT + restores seed statuses/fee fields + HT-003→Due + deletes type='concierge' kr_alerts + concierge_% org_settings).

### Demo-complete batch P48 — Smart Home & Security (VAS-D) + Property Health Check (VAS-C) (2026-08-03, `?v=3.38` / SW v46, 1550/1550)
- **P48 Smart Home & Security (VAS-D)** — tables `smart_locks` (SL-###; owner_email/prop/unit/lock_name/model/code 6-digit/status active|revoked/purpose showing|broker|maintenance|other/grant_for/**showing SH-### link**/valid_from/valid_until/used_count/used_at) + `cctv_cameras` (CV-###; prop/name/location/status online|offline/feed_url/last_seen). **`app-smarthome`** (module `smarthome` = superadmin/owner/manager/svc_mgr/**crm** — accountant/legal 403; owner self-scoped): summary (locks by_status + expiring, cameras by_status), lock-list/create (auto 6-digit code or provided; valid_until defaults now+lock_valid_hours)/save/revoke (double 409)/use (used_count++, revoked 409), **`showing-code`** (THE VAS-D flow: SH-### → vacancy → auto-creates active lock tied to the showing, code valid showing-day 08:00–20:00, unit from vacancy, grant_for = prospect; **active lock already exists for showing → 409**), camera-list/create/save/status (online|offline stamps last_seen), config-get/save (org_settings `smart_*` lock_valid_hours clamp 1–720). **KR tools** smart_lock_status (SL-### or list)/camera_status (CV-### or list)/**showing_code** (SH-### → generates, the phone flow); intents `(SL-\d+)`/`(CV-\d+)` prefix-gated + `(SH-\d+)+(lock|code|access|স্মার্ট লক|কোড|প্রবেশ)` + keyword `smart lock|স্মার্ট লক` / `camera|cctv|ক্যামেরা|সিসিটিভি`. **kr_alert_scan** smart pass: camera offline 24h+ → warning, active lock expiring ≤48h → info (owner → sub:N).
- **P48 Property Health Check (VAS-C)** — table `health_plans` (HP-###; owner_email/prop/unit/season pre_monsoon|pre_summer|quarterly/service ac_service|roof_waterproof|drainage_clear|deep_clean|pest_control/scheduled_for YYYY-MM/status Planned→Scheduled→In_Progress→Completed|Skipped (terminal locked)/assigned_to/cost/paid/**checklist JSON** `[{task,done}]` auto-seeded per service via `health_checklists()`/findings/completed_at). **`app-healthcheck`** (module `health` = superadmin/owner/manager/svc_mgr/crm/**accountant**; **endpoint named app-healthcheck NOT app-health — `app-health` collides with the built-in health-check case and PHP switch dispatches to the FIRST match, making a later case dead code**): summary (by_status/by_season/by_service + overdue/upcoming vs current month), plan-list/create (default vendor from config, checklist auto)/save (Completed/Skipped locked)/status (terminal locked)/checklist-save (index+done or full array; locked when terminal), config-get/save (org_settings `health_*` health_alert_days clamp 1–720 + health_default_vendor). **KR tools** health_plan_status (HP-### or list)/health_summary; intents `(HP-\d+)` prefix-gated + `health check|health plan|health summary|হেলথ চেক|হেলথ প্ল্যান|স্বাস্থ্য পরীক্ষা`. **kr_alert_scan** health pass: non-terminal plan scheduled < current month → warning overdue, ≤ next month → info due (owner → sub:N).
- **Frontend** (dashboard 778,870 B): 🔐 **Smart Home** module (Operations group after Legal Concierge; superadmin/owner/manager via ALL + svc_mgr/crm mods) — 2 sub-tabs (🔐 Locks / 📹 CCTV): locks stats (total/active/expiring-48h/uses) + codes table (unit, purpose badge, granted-to, code w/ letter-spacing, valid window, status, 🔑 use + ✕ revoke) + **🔑 Grant showing code** drawer (Scheduled-showing select → auto-generate); CCTV stats (total/online/offline/coverage%) + cameras table (🔴/🟢 toggle); 🌦️ **Health Check** module (superadmin/owner/manager via ALL + svc_mgr/crm/accountant mods) — stats (plans/active/overdue/upcoming) + plans table (season badge, service, property, scheduled month, vendor, cost, progress n/task · pct, status, 👁) + new-plan drawer (**service → task-list preview** via `healthSvcPreview`) + plan drawer (checklist with Done/Undo toggles, status advance, cost edit). **nrb integration**: 🔑 button added to BOTH the Upcoming-showings panel AND the vacancy-detail showings list → `smarthomeCodeFor(id)` (VAS-D: broker/owner presses 🔑 on a scheduled showing → temporary lock code). Demo parity: SEED smart_locks (SL-001 active broker code expiring 08-04 = **expiring-48h demo**, SL-002 revoked showing code for SH-001) + cctv_cameras (CV-001 online / CV-002 offline 2d) + health_plans (HP-001 Scheduled pre-monsoon 4-task, HP-002 Completed 4/4, HP-003 Planned quarterly); `demoSmartEnrich/Data` + `demoHealthEnrich/Data` mirror PHP; all mutators DEMO_MODE-branched.
- **Bugs found & fixed**: (1) **endpoint name collision** — `app-health` already exists (health check) → switch dispatched to the OLD case and my module was dead code (probe showed the health-check JSON); renamed `app-healthcheck`. (2) **seed SL-001 tied to SH-001 with status active blocked the showing-code flow** (409 "active code already exists") — restructured: SL-001 = active broker code (no showing), SL-002 = revoked showing code for SH-001 (history demo). (3) expiry window math: seed valid_until 08-05 20:00 was ~53h out → outside the 48h alert window → moved to 08-04 20:00. (4) nested template literal in healthPlanDrawer button labels (would render `${t(...)}` literally) → precompute `lbl`. (5) test preamble now restores seed state (SL/CV/HP rows + used_count + org_settings) so the suite is re-runnable.
- **Live**: ?v=3.38 / SW v46 (53/53 files); API 801,803 B (probes app-smarthome + app-healthcheck → 401 not 404); dashboard 778,870 B (byte-identical); DB backup `/root/krtaker-backup/pre-p48_20260803_150254/`; app-setup?key=kr-phase2-2026 (schema + seeds: 2 locks/2 cameras/3 plans/3 config keys); live E2E **61/61** (both modules' RBAC incl. accountant smarthome-403/health-200, lock lifecycle + showing-code SH-001→U-002 + double 409, camera toggles, plan lifecycle + checklist + locks, config round-trips, 5 KR intents, bootstrap scoping, export, GET whitelist); live browser verified Smart Home (5 locks incl. smoke codes, expiring 2) + Health Check (4 plans) **0 JS errors**; demo rig verified all tabs/drawers/mutations + grant-code flow (SL-101 for SH-003) + nrb 🔑 button + role gating (smarthome: owner/svc_mgr/crm ✓, accountant/tenant/partner ✗; health: + accountant ✓).
- **Regression**: 1550/1550 (38 suites; test_p48 110/110; run_all P48 reset wipes non-seed SL/CV/HP + restores seed statuses/counts + CV-002 offline + deletes smarthome/health kr_alerts + restores smart_/health_ org_settings).


### Demo-complete batch P49 — Build Watch: Construction & Renovation Monitoring (VAS-E) (2026-08-03, `?v=3.39` / SW v47, 1632/1632)
- **P49 Build Watch (VAS-E — first phase beyond the USP doc; NRB owners build from abroad with zero visibility on progress/budget/milestones; Land Guard watches the land, Build Watch watches the building)** — tables `build_projects` (BP-###; owner_email/prop/title/kind construction|renovation|repair/status Planning→In_Progress→On_Hold→Completed|Cancelled (terminal locked)/budget_total/start_date/target_end/contractor/architect/site_address/notes) + `build_milestones` (BM-###; project/phase foundation|structure|electrical|plumbing|finishing|handover/target_date/status Pending→In_Progress→Completed|Skipped (terminal locked)/cost/paid) + `build_expenses` (BX-###; label/amount/category material|labour|permit|design|other/spent_on/paid) + `build_media` (BD-###; photo|video|doc, files in DATA_DIR like land_media). **`app-build`** (module `build` = superadmin/owner/manager/svc_mgr ONLY — accountant/legal/crm/tenant/partner 403; owner self-scoped by email + ''; 404-before-403 everywhere): summary (stats by status + total_budget/total_spent + `alerts` (milestone overdue / budget overrun / past-target) + config), list/get/create (owner forces own email)/save/status (forward-only map, terminal 409)/delete (cascades milestones/expenses/media + unlinks files), milestone-list/create/save/status (Pending→In_Progress|Skipped, In_Progress→Completed, terminal 409), expense-list/add (label + amount>0 → 400)/delete (double 404), media-upload (multipart `?action=media-upload` GET param; images/mp4/pdf; move_uploaded_file → `bmd_<id>.<ext>`)/list/delete/view (scoped bytes echo+exit), report (GET `?id=` → `build_report_html` bilingual printable echo+exit), config-get/save (org_settings `build_*`: milestone_alert_days clamp 1–120, budget_overrun_pct clamp 1–50, default_contractor; owner/manager save only). **`bp_enrich()`**: spent = SUM expenses, milestones done/overdue (non-terminal target < today — a still-In_Progress milestone with a past target counts overdue by design)/due-soon (≤ today+alert_days), progress % = done/total, budget_used_pct, **over_budget** = spent > budget×(1+overrun_pct/100), days_left, media_count, property_name. **KR tools** build_status (BP-### or list w/ progress+spend)/build_budget/build_milestone_due; intents BEFORE generate-invoice: `(BP-\d+)`/`(BM-\d+)` prefix-gated FIRST, then `milestone|মাইলস্টোন`, then `budget|বাজেট` AND build-word (both required), then generic `build|construction|renovation|বিল্ডিং|নির্মাণ|সংস্কার`. **kr_alert_scan** build pass: milestone overdue → warning, budget overrun → warning (owner-email → sub:N, deduped refs `BP-xxx-od`/`BP-xxx-bg`). Bootstrap staff full / tenant+partner empty; export list + reset=1 wipe; seed 3 projects/6 milestones/6 expenses/2 media + 3 build_% org_settings keys.
- **Frontend** (dashboard 778,870 → 812,044 B): new 🏗️ **Build Watch** module (Operations group, after Health Check; ALL + svc_mgr mods; MOD_ICON 🏗️ / MOD_TITLE / BN 'নির্মাণ মনিটরিং') — renderer `build()` (exact module-id name), `buildLoad` (live apiCall summary → also refreshes `DB.build_projects` so drawers stay fresh; demo `demoBuildData`), `buildHtml` (4 stat cards: Projects/Active/Spent (৳, warn when over)/Overdue ms. + projects table (title+prop, kind, budget, spent w/ ⚠️ over-budget badge, progress bar colored red when over, target + 'past' chip, status badge, 👁) + Build alerts panel), `buildDrawer(id)` (budget gauge with % + Over-budget badge, milestone count, contractor/architect/site/timeline kv, buttons: ➡️ Advance status / 🖨 Print report / ＋ Milestone, milestones table with phase/target (overdue chip)/cost/status + ➡️ advance, expenses ledger with paid chip + ✕ delete + **inline add-expense form**, 📷 site-media strip with geo), `buildNewProject`/`buildCreate`, `buildNewMilestone`/`buildMsCreate`, `buildStatus(id,dir)` (seq map + terminal check), `buildMsStatus`, `buildExpenseAdd`/`buildExpenseDel`, `buildConfigDrawer`/`buildConfigSave` (alert days/overrun %/default contractor), `buildReport(id)` (live fetch API + Bearer → previewDoc like tif-print; demo builds HTML client-side). Demo parity: SEED carries build_* (BP-001 renovation In_Progress 2M w/ BM-003 07-20 Pending = **1-overdue demo** + spent 1.85M; BP-002 construction 8M w/ **BX-005 4M → spent 9M = 112.5% over-budget demo**; BP-003 Completed 150k; BD-001/002 site photos w/ geo); `demoBuildEnrich()` mirrors the PHP math (incl. over_budget threshold + overdue by past target); **all 8 mutators DEMO_MODE-branched**; owner demo scope `LIVE.user?.email || 'owner@krtaker.com'`.
- **Bugs found & fixed**: (1) **ROLE_MODULES splice op forgotten** → every `require_module($u,'build')` 403'd ("Access denied — build not available for owner"); roles line edit was planned but missing from the 12-op list — added as direct patch. (2) **`audit()` signature is `audit($u['name'], $action, $module, $entity, $details)`** and **`org_cfg($pdo)` is a whole-config getter, NOT keyed** — P49 first used `audit($pdo,$u,…)` + `org_cfg('build_x')` (wrong args + wrong semantics) → regex-patched to `audit($u['name'],'build-…','build',…)` + dedicated `build_cfg($pdo,$key,$def)` helper. (3) **ALERT_PASS block missing its closing `}`** (the rep consumed the anchor's `}`) → "Unclosed '{' on line 5170" — every inserted block must carry its own structural close. (4) **Python `\'` escape casualty**: `datetime(\'now\')` in a Python triple-quoted string unescapes to `datetime('now')` → terminates the PHP single-quoted SQL → parse error; fix = `datetime("now")` (SQLite accepts double-quoted string literal). (5) **Live seed drift after deploy**: BX-005 3.5M→4M and BM-002 07-10→08-10 were fixed in the rig DB + splice script but the deployed API was FTP'd BEFORE those fixes → live seeded old values (total_spent 10,498,000, BP-002 not over budget, BP-001 2 overdue); the seed block is `if count==0` so app-setup won't re-seed a populated table → re-FTP'd the corrected API then fixed live rows via API (`expense-delete` × 6 + app-setup reseed; `milestone-save` to bump BM-002 target) → 44/44. (6) `len(src)` counts characters not bytes (multibyte Bengali) — splice "+30257" vs wc -c +33,174 is decode-vs-bytes, not a bug.
- **Live**: ?v=3.39 / SW v47 (53/53 files; web/sw.js krtaker-site-v47; 28/28 web files at v=3.39, 0 stale); API 845,399 B (byte-verified via FTP; probe app-build → 401 not 404); dashboard 812,044 B (byte-identical to repo, buildLoad present); DB backup `/root/krtaker-backup/pre-p49_20260803_153728/` (integrity ok); app-setup?key=kr-phase2-2026 (schema + seeds); live E2E **44/44** (RBAC 401/403, summary totals 10,998,000 + both alerts, get 404/403, create→Planning→In_Progress→Completed + terminal 409, milestone create/advance/lock 409, expense add/double-delete 404, report HTML content, config clamps (1,50) + restore + svc_mgr 403, 4 KR intents incl. generate-invoice + vacant-unit intact, cleanup delete verified 404); live browser verified Build Watch as owner (3 projects / 2 active / ৳1,09,98,000 spent / 1 overdue ms., alerts panel, BP-002 drawer Over-budget badge + milestones + expenses, typeof buildLoad = new SW bundle) **0 JS errors**; demo rig verified full lifecycle (stats, drawer, milestone advance Pending→In_Progress, expense add, status advance, new project create, config drawer, report preview ppFrame) + role gating exact (owner/svc_mgr ✓, accountant ✗).
- **Regression**: 1632/1632 (39 suites; test_p49 82/82; run_all P49 reset restores BP/BM/BX/BD seed rows + statuses + BX-005 amount + deletes type='build' kr_alerts + build_% org_settings).

### Demo-complete batch P50 — Gate Watch: Visitor & Vehicle Control (VAS-F) (2026-08-03, `?v=3.40` / SW v48, 1711/1711)
- **P50 Gate Watch (VAS-F — beyond-the-doc again; Smart Home/CCTV watches the unit's digital security, Gate Watch watches who physically enters the building; NRB owners get the guard's notebook as a live board + daily bilingual register)** — tables `gate_visits` (GV-###; **owner_email** (scoping lives on the row — `properties` has NO owner_email column, land/build pattern)/prop/vtype visitor|delivery|worker|vendor|vehicle/name/phone/vehicle_no/unit/purpose/host_name/check_in/check_out/status Inside|Out/flagged/notes/photo (DATA_DIR file)/created_by) + `resident_vehicles` (RV-###; prop/unit/vehicle_no/vtype car|bike|cycle|other/label/owner_name/sticker_no/status active|inactive) + `gate_watchlist` (GW-###; vehicle_no/name/reason/active). **`app-gate`** (module `gate` = superadmin/owner/manager/svc_mgr ONLY — tenant/partner/legal/accountant/crm 403; owner self-scoped by `owner_email` + ''; 404-before-403): summary (inside_now + inside_count/today_in/today_out/by_type/flagged_open/watchlist_active/recent + config), visit-list (filters prop/date/status/vtype)/visit-get/visit-create (**auto-flag**: vehicle_no (UPPER) or name matches active watchlist → flagged=1 + returns watchlist reason; owner role forces own email)/visit-out (Inside→Out stamps check_out; double-out 409)/visit-delete (superadmin/owner/manager only; unlinks photo)/visit-photo (multipart `?action=visit-photo` GET param; images only; `gv_<id>_<ts>.<ext>`)/visit-photo-view (scoped bytes echo+exit), vehicle-list/create (plate required 400)/save/delete (404), watchlist-list/create (plate or name 400)/save (active toggle)/delete (404), report (GET → `gate_report_html` bilingual printable with ID column + signature blocks, echo+exit), config-get/save (org_settings `gate_*`: auto_flag_hours clamp 1–120, report_title; owner/manager save only). **`gate_enrich()`**: property_name, inside_min (minutes since check_in for Inside rows), resident (vehicle in active registry). **KR tools** gate_inside (who's inside)/gate_visit_status (GV-###)/gate_vehicle_check (plate → watchlist ⚠️ / resident ✅ / unknown + last visit); intents BEFORE generate-invoice: `(GV-\\d+)` prefix-gated FIRST, then `vehicle|গাড়ি|রেজিস্ট্রেশন` + plate `([A-Z]{2,12}(?:[- ][A-Z]{2,12})?[- ]?\\d{4,6})` (the {2,4} version captured "ETRO-9999" from DHAKA-METRO-9999 — plates are two hyphenated words + 4–6 digits), then **`inside now|who.*inside|কে কে আছে|ভেতরে|গেট|gate|visitor|দর্শনার্থী|আগন্তুক` — placed BEFORE the build generic intent** because "building" contains "build" and the `(build|construction|…)` catch-all stole "who is inside the building right now". **kr_alert_scan** gate pass (per `gate_visits.owner_email` → sub:N): flagged Inside → **critical** 'Watchlist visitor on site' (ref `-wl`), Inside > auto_flag_hours → **warning** 'Long-stay visitor' (ref `-ls`). Bootstrap staff full / tenant empty / partner none; export list + reset=1 wipe; seed GV-001 (Inside −6h long-stay demo)/GV-002 (delivery Out)/GV-003 (worker Out)/GV-004 (vehicle Inside **flagged** watchlist demo) + RV-001/002 + GW-001 (DHAKA-METRO-9999) + gate_auto_flag_hours=4 + report_title.
- **Frontend** (dashboard 812,044 → 842,382 B): new 🚪 **Gate Watch** module (Operations group after Build Watch; ALL + svc_mgr mods; MOD_ICON 🚪 / MOD_TITLE / BN 'গেট ওয়াচ') — renderer `gate()` (exact module-id), `gateLoad` (live apiCall summary; demo `demoGateData` — overrides Inside-row check_in to now−6h/−30m so the demo board reads realistically), `gateHtml` (4 stat cards Inside now/Today in/Flagged/Watchlist + `kr-tabs` sub-tabs via `state.gateTab`: 🟢 Live board (inside cards w/ FLAGGED + resident badges + Check-out) · 📋 Log (full table w/ check-out + ✕) · 🚗 Vehicles (registry table) · 🚨 Watchlist (entries + active badge) + 🖨 Daily report), `gateCheckIn`/`submitGateCheckIn` (drawer: prop/type/name/phone/vehicle/unit/purpose/host/notes; demo computes watchlist flag client-side), `gateCheckOut`, `gateDeleteVisit`, `gateVehicleForm`/`submitGateVehicle`/`gateVehicleDelete`, `gateWatchlistForm`/`submitGateWatchlist`/`gateWatchlistDelete`, `gateConfigDrawer`/`gateSaveConfig` (auto-flag hours + report title; **demoGateData must read `window.__gateCfg`** or gateLoad overwrites a saved config with the hardcoded 4), `gateReport` (live fetch API?action=report + Bearer → previewDoc; demo builds HTML client-side). Demo parity: SEED carries gate_* (GV-001..004 incl. flagged + RV-001/002 + GW-001); `gateEnrich()` mirrors PHP (inside_min/resident/property_name); **all mutators DEMO_MODE-branched**; bottom bar still 5 items (gate is sidebar-only).
- **Bugs found & fixed**: (1) **`properties` has NO `owner_email` column** — first cut scoped owners via `SELECT id FROM properties WHERE owner_email=…` → "no such column" 500; moved scoping to `gate_visits.owner_email` (build/land pattern), visit-create forces owner's own email, alert pass reads the row's owner_email (not a properties lookup). (2) **Placeholder mismatch**: watchlist seed `VALUES (?,?,?,?,1)` with 5 bound values → "column index out of range" 500 mid-seed (visits+vehicles seeded, watchlist+config missing) — dropped the trailing bound value. (3) **`/* generate invoice */` marker count = 1** (not 2) — the n=2 assert caught it before write. (4) **rep() consumed the app-build case close** — anchor `json_out(…Unknown build action…);\n}\n\ndefault:` got fully replaced (anchor text not re-included) → `case 'app-gate'` landed INSIDE the app-build block → "unexpected token case"; fix = anchor on `default:` alone and keep the anchor in the replacement. (5) **KR "building" collision**: gate inside-intent placed after the build generic `(build|construction|…)` — "who is inside the building right now" matched "build" inside "building" → moved the inside-intent BEFORE the build generic. (6) **Vehicle plate regex {2,4}** broke DHAKA-METRO-9999 → ETRO-9999 — widened to `{2,12}(?:[- ][A-Z]{2,12})?`. (7) **Latent P49 seed drift**: deployed API seed still had BM-002 target 07-10 (P49 closeout fixed only live rows via API, never the file) — P50's reset=1 re-seeded it and broke test_p49 "BP-001 overdue 1" (2 overdue); fixed the API seed to 08-10 + run_all P49 reset now forces `target_date='2026-08-10'`. (8) **test_p50 reset call**: `call('app-setup',{key,reset})` PUT the key in the JSON body but app-setup reads `$_GET` → 403 Invalid setup key — raw GET with query string. (9) **alert-scan test preamble** must restore GV-001 to status='Inside' (the checkout test left it Out → no long-stay warning).
- **Live**: ?v=3.40 / SW v48 (53/53 files; web/sw.js krtaker-site-v48; 28/28 web files at v=3.40, 0 stale); API 877,665 B (FTP-verified; probe app-gate → 401 not 404, app-healthcheck still 401); dashboard 842,382 B (byte-identical to repo); DB backup `/root/krtaker-backup/pre-p50_20260803_163650/` (90 tables, 1,253,376 B db + export); app-setup?key=kr-phase2-2026 (schema + seeds: inside 2 / flagged 1 / watchlist 1); live E2E **30/30** (RBAC tenant+crm 403 / owner 200, summary cumulative-safe, create watchlist-flag 200 + normal, checkout + double 409, delete, vehicle CRUD, watchlist CRUD, report HTML w/ গেট রেজিস্টার, config save + restore, 2 KR intents, export gate_visits); live browser verified as owner (Gate Watch nav + render: Inside now 2 / Today 5 / Flagged 1 / Watchlist 1, Mahmudul Karim inside, FLAGGED badge, check-in → GV-102 live → check-out → delete via API, typeof gateLoad = new SW bundle, sidebar nav('gate')) **0 JS errors**; demo rig verified full lifecycle (stats, live board, FLAGGED, check-in GV-101 + watchlist auto-flag GV-102, check-out, vehicles tab + add, watchlist tab + add, config save persists 8h, report preview iframe, role gating tenant ✗ / owner ✓, bottom bar 5 items).
- **Regression**: 1711/1711 (40 suites; test_p50 79/79; run_all P50 reset restores GV/RV/GW seed rows + statuses/flagged + check_in −6h + deletes type='gate' kr_alerts + gate_% org_settings + BM-002 target 08-10).



### Demo-complete batch P51 — Fire Safety & Emergency Response (VAS-G) (2026-08-03, `?v=3.41` / SW v49, 1799/1799)
- **P51 Fire Safety & Emergency Response (VAS-G — beyond-the-doc, VAS alphabet continues A→G; Build Watch watches construction, Gate Watch watches who enters, Fire Safety watches whether the building can burn safely — extinguisher expiry, detector health, incident response, evacuation drills, emergency contacts; Dhaka high-rise fire risk is a massive NRB pain point)** — tables `fire_assets` (FA-###; **owner_email on the row** (properties has NO owner_email — land/build/gate pattern)/prop/asset_type extinguisher|detector|sprinkler|alarm|exit_light|hose/location/model/serial_no/install_date/expiry_date/status active|faulty|decommissioned/last_inspected/next_inspection/notes) + `fire_incidents` (FI-###; incident_type fire|gas_leak|electrical|medical|water|security|other/severity low|medium|high|critical/status Reported→Assessing→Responding→Resolved→Closed forward-only terminal-locked/reported_by/occurred_at/location/description/**timeline JSON** (appended per status change + event)/resolved_at stamped on Resolved via **`CASE WHEN CAST(? AS INTEGER)=1`** — the bare `?=1` bind is TEXT and never fires (class-level P42 trap, re-hit + fixed here)) + `evacuation_plans` (EP-###; assembly_point/route/last_drill/next_drill/status active|draft) + `emergency_contacts` (EC-###; contact_type fire|police|ambulance|electric|gas|water|building_manager|owner|other/name/phone/notes). **`app-firesafety`** (module `firesafety` = superadmin/owner/manager/svc_mgr ONLY — tenant/partner/legal/accountant/crm 403; owner self-scoped email+''; 404-before-403): summary (assets_total/by_status/expired/expiring/inspection_overdue via `fire_asset_enrich` (days_to_expiry vs expiry_alert_days; **expired wins over expiring**), incidents_open/by_severity, plans_active/drill_overdue, contacts, config), asset-list/create (location required 400; unknown prop 404; owner forces own email; new asset starts active + last_inspected=today + next_inspection=today+inspection_days)/asset-save/asset-status (active|faulty|decommissioned)/asset-inspect (stamps last+next, audit)/asset-delete (double 404), incident-list/create (location required 400)/incident-save (Resolved/Closed locked 409)/incident-status (forward map, terminal 409, timeline append + resolved_at)/incident-event (note required 400; locked when closed)/incident-delete, plan-list/create (name required 400)/plan-save (active|draft)/plan-drill (only active 409; stamps last+next=+drill_days)/plan-delete, contact-list/create (name + phone required 400)/contact-save/contact-delete, report (`fire_report_html` bilingual printable **with expired/expiring flags + signature blocks**, echo+exit), config-get/config-save (org_settings `fire_*`: inspection_days clamp 1–365, expiry_alert_days 1–365, drill_days 1–730; owner/manager save only). **KR tools** fire_safety_status (FA-### or summary)/fire_incident_status (FI-### or open list)/fire_asset_check (FA-###); intents BEFORE generate-invoice: `(FA-\d+)`/`(FI-\d+)` prefix-gated FIRST, then `fire safety|fire extinguisher|অগ্নি|ফায়ার|আগুন|fire|safety drill|ড্রিল`, then `incident|accident|দুর্ঘটনা|ঘটনা` — no keyword collisions this time (fire/incident vocab is disjoint from build/gate/legal). **kr_alert_scan** firesafety pass (per `fire_assets.owner_email` → sub:N): **expired → critical** 'Fire asset expired' (ref `-ex`), expiring ≤alert → **warning** (ref `-ew`), **faulty → warning** (ref `-fy`), inspection overdue → **info** (ref `-in`), open **high/critical** incident → **warning** (ref FI id), active-plan drill overdue → **info** (ref `-dr`). Bootstrap staff full (4 tables) / tenant empty; export list + reset=1 wipe (4 tables + `fire_%` org_settings); seed FA-001 (extinguisher +45d = **expiring demo**)/FA-002 (expired −20d + inspection overdue = **critical demo**)/FA-003 (detector healthy)/FA-004 (alarm **faulty** demo) + FI-001 (fire/high/**Responding** open demo w/ 2-event timeline)/FI-002 (electrical low Resolved history) + EP-001 (active, next_drill −10d = **drill overdue demo**)/EP-002 (draft) + EC-001..005 (fire 999/police 999/ambulance 199/DESCO 16106/building manager) + fire_inspection_days=30/expiry_alert_days=60/drill_days=180.
- **Frontend** (dashboard 842,382 → 880,114 B): new 🧯 **Fire Safety** module (Operations group after Gate Watch; ALL + svc_mgr mods; MOD_ICON 🧯 / MOD_TITLE / BN 'ফায়ার সেফটি') — renderer `firesafety()` (exact module-id — the P44 renderer-keying trap), `fireStaff()` (role gate), `fireLoad` (live apiCall summary → also refreshes DB.fire_* so drawers stay fresh; demo `demoFireData` reads `window.__fireCfg` so config saves persist), `fireHtml` (4 stat cards Assets/Expired/Expiring/Open incidents + `kr-tabs` sub-tabs via `state.firesafetyTab`: 🧯 Assets (table w/ EXPIRED + expiring + due badges, ✎/🔎 inspect/✕) · 🔥 Incidents (cards w/ severity + status badges + last-3 timeline events + ➡️ advance/✚ event/✕) · 🏃 Evacuation (plans table w/ overdue chip + 🏃 drill button) · ☎️ Contacts (cards w/ type badges + 📞) + 🖨 Report), `fireAssetForm`/`submitFireAsset` (drawer w/ type select + date fields), `fireAssetInspect`, `fireAssetDelete`, `fireIncidentForm`/`submitFireIncident` (severity + type selects), `fireIncidentStatus` (demo: forward-map + resolved_at), `fireIncidentEvent`/`submitFireIncidentEvent`, `fireIncidentDelete`, `firePlanForm`/`submitFirePlan`, `firePlanDrill`, `firePlanDelete`, `fireContactForm`/`submitFireContact`, `fireContactDelete`, `fireConfigDrawer`/`fireSaveConfig` (3 clamped intervals), `fireReport` (live fetch API?action=report + Bearer → previewDoc; demo builds HTML client-side w/ অগ্নি নিরাপত্তা header). Demo parity: SEED carries fire_* (4 assets incl. expired/expiring/faulty + 2 incidents + 2 plans + 5 contacts); `fireAssetEnrich()` mirrors PHP (expired/expiring/days_to_expiry/inspection_overdue + property_name); **all mutators DEMO_MODE-branched**; bottom bar still 5 items (sidebar-only). **`fireCap()` module-local helper added** — the dashboard has NO `ucfirst` (hits would have been runtime TypeError in renderers; node --check passes but the drawer/report renderers throw — always exercise demo mode).
- **Bugs found & fixed**: (1) **`?=1` SQLite bind trap re-hit** — `resolved_at=CASE WHEN ?=1 THEN datetime("now")` bound TEXT '1' never equals INTEGER 1 → resolved_at never stamped; fixed `CAST(? AS INTEGER)=1` (rig test caught it: "incident Resolved stamps resolved_at"). (2) **frontend `ucfirst` doesn't exist** — demo incidents/plans/contacts renderers threw `TypeError: ucfirst is not defined` at render time (node --check passes — runtime-only); added `fireCap()`. (3) **smoke-script bugs (not API)**: summary block read `d` from the no-token 401 call (empty dict → all summary asserts None) — re-fetch summary first; incident advance var mapping was off-by-one (create→Reported, so 1st status call = Assessing — original smoke made 4 status calls but checked wrong vars → "to Resolved" saw Closed). (4) **frontend ALL/svc_mgr splice count=2** (dashboard has ONE ALL + ONE svc_mgr mods list — the API has 4; the n=4 assert failed first try, corrected to 2).
- **Live**: ?v=3.41 / SW v49 (53/53 files; web/sw.js krtaker-site-v49; 28/28 web files at v=3.41, 0 stale); API 926,536 B (FTP-verified; probe app-firesafety → 401 not 404, app-healthcheck still 401); dashboard 880,114 B (byte-identical to repo); DB backup `/root/krtaker-backup/pre-p51_20260803_175551/` (integrity ok); app-setup?key=kr-phase2-2026 (schema + seeds: 4 assets/2 incidents/2 plans/5 contacts + 3 fire_% keys); live E2E **43/43** (RBAC tenant+crm+accountant 403 / owner 200, summary cumulative-safe, asset lifecycle incl. inspect + faulty + double-delete 404, incident full chain to Closed + locked 409 + delete, plan drill + draft 409 + delete, contact CRUD, report HTML w/ অগ্নি নিরাপত্তা, config save/restore, 3 KR intents + generate-invoice + vacant intact, bootstrap scoping); live browser verified as owner (nav('firesafety') renders: Assets 4 / Expired 1 / Expiring 1 / Open incidents 1, FA-002 EXPIRED + due badges, incidents tab FI-001 Responding + timeline, live create FA-100 → board → delete via API → back to 4, typeof fireLoad = new SW bundle) **0 JS errors**; demo rig verified full lifecycle (4 tabs, EXPIRED/expiring/faulty/due/overdue chips, add asset FA-101, inspect stamps, incident advance + event + resolved_at, drill logs +180d, config persists 45, report preview, role gating tenant ✗ / owner ✓, bottom bar 5 items). Pre-existing 2 message-less exceptions confirmed identical on P50 bundle (benign, not P51).
- **Regression**: 1799/1799 (41 suites; test_p51 88/88; run_all P51 reset restores FA/FI/EP/EC seed rows + statuses + expiry dates (date('now',±n) so alerts always fire) + deletes type='firesafety' kr_alerts + fire_% org_settings).

### Demo-complete batch P52 — Essential Systems Watch (2026-08-03, `?v=3.42` / SW v50, 1902/1902)
- **P52 Essential Systems Watch (VAS-H — beyond the doc again; the building's mechanical backbone: Fire Safety watches whether it can burn safely, Systems Watch watches whether it can run — lift, generator, pumps, transformer)** — tables `sys_assets` (SA-###; **owner_email on the row** (properties has NO owner_email — land/build/gate/fire pattern)/prop/asset_type lift|generator|water_pump|sewage_pump|transformer|solar|booster_pump|other/location/model/serial_no/install_date/**next_service** + service_interval_days/**cert_expiry** + cert_ref (RAJUK/DESCO)/status operational|service_due|faulty|decommissioned/**fuel_capacity/fuel_level/fuel_burn_rate**/notes) + `sys_services` (SS-###; routine|repair|major|amc/technician/vendor/cost/hours_run) + `sys_fuel` (SF-###; refill_date/**liters/rate_per_litre/amount auto round(liters×rate)**/vendor/hours_run). **`app-systems`** (module `systems` = superadmin/owner/manager/svc_mgr ONLY — tenant/partner/legal/accountant/crm 403; owner self-scoped email+''; 404-before-403): summary (assets_total/operational/faulty/service_overdue/service_due_soon/cert_overdue/cert_expiring/fuel_low/services_total/fuel_refills/**fuel_liters_30d/fuel_cost_30d** via `sys_asset_enrich` (service_overdue = next_service<today and not decommissioned; due_soon ≤ today+alert; cert_overdue/expiring; fuel_pct + fuel_low ≤ threshold; services_count/last_service/property_name)), asset CRUD + status + **asset-service** (stamp: inserts SS row + next_service=today+interval) + **asset-fuel** (refill: inserts SF row + fuel_level = min(capacity, level+liters) clamp), service-list/create (asset 404; date required 400)/delete, fuel-list/create (liters>0 400)/delete, report (`sys_report_html` bilingual printable w/ per-asset flags + signature blocks, echo+exit), config (`sys_*`: service_alert_days 1–365, cert_alert_days 1–365, fuel_low_pct 1–50, fuel_alert_days 1–120; owner/manager save only). **KR tools** systems_status (SA-### or summary)/sys_service_due/sys_fuel_check; intents BEFORE generate-invoice: `(SA-\d+)` prefix-gated FIRST, then **`services? due` → sys_service_due**, then **`fuel|ডিজেল|জ্বালানি|refill|রিফিল` → sys_fuel_check**, then generic `systems watch|system status|lift|লিফট|generator|জেনারেটর|water pump|পাম্প|রক্ষণাবেক্ষণ` → systems_status — **specific tools MUST precede the broad generic regex** ("generator fuel level" contains "generator" → generic stole it, cost one re-run). **kr_alert_scan** systems pass (per sys_assets.owner_email → sub:N): cert expired → **critical** `-ce`, expiring → **warning** `-cw`, service overdue → **warning** `-so`, due soon → **info** `-sd`, faulty → **warning** `-fy`, fuel low → **warning** `-fl`. Bootstrap staff full (3 tables) / tenant empty; export list + reset=1 wipe; seed SA-001 (generator next_service −5d = **overdue demo** + cert +45d expiring + fuel 110/500 = **22% LOW demo**)/SA-002 (lift cert −15d = **EXPIRED demo** + svc due +10d)/SA-003 (pump healthy +20d)/SA-004 (transformer **faulty** +8d) + SS-001..003 + SF-001/002 (30d liters 390 / cost ৳44,100) + 4 sys_% keys.
- **Frontend** (dashboard 880,114 → 919,216 B): new ⚙️ **Systems Watch** module (Operations group after Fire Safety; ALL + svc_mgr mods; MOD_ICON ⚙️ / MOD_TITLE / BN 'সিস্টেম ওয়াচ') — renderer `systems()` (exact module-id — P44 trap), `sysStaff()` gate, `sysLoad` (live apiCall summary → refresh DB.sys_*; demo `demoSysData` reads `window.__sysCfg` so config saves persist), `sysHtml` (4 stat cards Assets/Service due/Certs/Fuel low + kr-tabs sub-tabs via `state.sysTab`: ⚙️ Assets (table w/ OVERDUE + due + EXPIRED + expiring + LOW badges + fuel level bar + ✎/🛠️ stamp/⛽ refill (fuel_capacity>0 only)/✕) · 🛠️ Service log (table + ＋ Log service) · ⛽ Fuel ledger (table + ＋ Log refill) + 🖨 Report). Mutators: sysAssetForm/submitSysAsset, sysStatus, sysAssetService/submitSysService (stamp drawer w/ interval), sysAssetFuel/submitSysFuel (refill drawer w/ rate → level+clamp), sysAssetDelete (cascade), sysServiceForm/submitSysLogService, sysServiceDelete, sysFuelForm/submitSysLogFuel, sysFuelDelete, sysConfigDrawer/sysSaveConfig (4 clamped intervals), sysReport (live fetch API?action=report + Bearer → previewDoc; demo builds HTML client-side w/ সিস্টেম ওয়াচ header). Demo parity: SEED carries sys_assets (4) + sys_services (3) + sys_fuel (2); `sysAssetEnrich()` mirrors PHP; **all 14 mutators DEMO_MODE-branched**; `sysCap()` module-local helper (no ucfirst — P51 trap); bottom bar still 5 items (sidebar-only).
- **Bugs found and fixed**: (1) **intent shadowing class-trap** — broad generic regex (`generator|…`) placed before the fuel regex stole "generator fuel level" → systems_status instead of sys_fuel_check; reordered specific-first (`services? due` then `fuel|…` then generic). (2) **rep() consumed the app-firesafety case close** (P50 trap re-hit) — anchor included the fire case's final json_out + `}` and the replacement dropped the `}` → "unexpected token case"; restored the brace, anchor on `default:` alone. (3) **DDL text-matching fails on escaped bytes** — existing CREATE TABLE lines carry literal `\'`/`\"` from prior Python splices; clean-text anchors count=0; fix = positional insert (find `CREATE TABLE IF NOT EXISTS emergency_contacts (`, then next `);`, insert after). (4) **COALESCE `''` in PHP single-quoted SQL** — needs `\'\'` (Python `\\'` chain: JSON → file → Python value → PHP); php -l catches it. (5) **demoSysData forced values mutators change** (SA-001 fuel_level reset to 110 every load — P50 gateCfg class) → removed the overrides; static seed dates keep badges alive. (6) `confirm()` blocks headless browser console → override before driving deletes. (7) Transformer had no 🛠️ stamp button (arbitrary exclusion) → removed the guard. (8) test-order traps: report test before service/fuel mutations; clamp-test fuel row delete before bootstrap assert; alert-scan preamble restores SA-001 −5d/110.
- **Live**: ?v=3.42 / SW v50 (53/53 files; 28/28 web at v=3.42, 0 stale); API 963,869 B (FTP byte-verified; probe app-systems → 401 not 404, app-firesafety still 401); dashboard 919,216 B (byte-identical); DB backup `/root/krtaker-backup/pre-p52_20260803_185953/` (integrity ok); app-setup?key=kr-phase2-2026 (schema + seeds: 4 assets/3 services/2 fuel + 4 sys_% keys); live E2E **42/42** (RBAC 401/403, summary cumulative-safe, asset lifecycle incl. service stamp + refill + double-delete 404, report bilingual, config clamps 365/1/50/1 + restore, 4 KR intents + generate-invoice + vacant intact, bootstrap scoping, export, GET whitelist); live browser verified as owner (nav('systems') renders: Assets 4 / Service due 3 / Certs 2 / Fuel low 1, SA-001 OVERDUE + LOW + expiring, SA-002 EXPIRED, SA-004 faulty, live create SA-100 → board 5 → delete via API → back to 4, typeof sysLoad = new bundle, sw.js v50 served) **0 JS errors**; demo rig verified full lifecycle (3 tabs, add asset SA-101 → stamp → refill persists 160 → delete, config persists 30/90/15/10, report preview iframe w/ সিস্টেম ওয়াচ, tenant gated no nav, bottom bar 5 items) **0 JS errors**.
- **Regression**: 1902/1902 (42 suites; test_p52 103/103; run_all P52 reset restores SA/SS/SF seed rows + relative dates + SA-001 fuel 110 + deletes type='systems' kr_alerts + sys_% org_settings).

### Demo-complete batch P53 — Staff Watch (2026-08-03, `?v=3.43` / SW v51, 1997/1997)
- **P53 Staff Watch (VAS-I — building staff roster, attendance & payroll; the ops-watch family completes: Build→Gate→Fire→Systems→**Staff** — who actually works there; NRB owners can't verify guards/cleaners show up or that salaries aren't paid for no-shows; "pay only for verified attendance"; gives the long-stub `hr` role its first real module)** — tables `building_staff` (BS-###; **owner_email on the row** (properties has NO owner_email)/name/role guard|cleaner|caretaker|driver|supervisor|security|other/phone/join_date/**monthly_salary**/shift day|night|rotating/prop/status active|on_leave|terminated) + `staff_attendance` (AT-###; staff/**work_date**/check_in/check_out/status present|late|absent|leave, **UNIQUE(staff,work_date)** upsert keeps id) + `staff_payroll` (PY-### — **NOT SP (partners)**; staff/month/salary/overtime/bonus/advance_deduction/**absent_days+daily_rate+net AUTO** via `staff_payroll_calc` (absent_days from month attendance, daily=round(salary/30), net=max(0, salary+ot+bonus−advance−absent×daily)), **UNIQUE(staff,month)** upsert, Draft|Paid terminal). **`app-staffwatch`** (module `staffwatch` = superadmin/owner/manager/svc_mgr/**hr** ONLY — tenant/partner/legal/accountant/crm 403; owner self-scoped email+''; 404-before-403): summary (staff_total/active/on_leave/on_duty_today/absent_today/late_today/no_mark_today/payroll_unpaid_count/attendance_month via `staff_enrich` — today_status, month counts, **absent_pct = absent/total marked**, payroll_unpaid_prev), staff CRUD + status, attendance-list (month filter)/**attendance-mark** (upsert by staff+date)/delete, payroll-list/**payroll-create** (month `^\d{4}-(0[1-9]|1[0-2])$`; auto net; upsert)/**payroll-pay** (double 409)/delete, report (`staff_report_html` bilingual w/ flags + signature blocks, echo+exit), config (`staff_*`: payroll_alert_days 1–120, absent_pct 1–50; owner/manager save only). **KR tools** staff_status (BS-### or summary)/staff_attendance_today/payroll_due; intents BEFORE generate-invoice: `(BS-\d+)` prefix-gated FIRST, then `payroll|salary|বেতন|মাইনে|পে-রোল` → payroll_due, then **`attendance|উপস্থিতি|on duty|duty|ডিউটি` → staff_attendance_today** (MUST precede generic staff regex — 'duty' was stolen first run), then generic `staff watch|staff|কর্মচারী|স্টাফ|guard|গার্ড|cleaner|পিয়ন|রক্ষী` → staff_status. **kr_alert_scan** staffwatch pass: active + no mark today → **info** `-at`, absent_pct ≥ threshold → **warning** `-ab`, prev-month Draft payroll + day-of-month > alert_days → **warning** `-py`. Bootstrap staff full (3 tables) / tenant empty; export + reset wipe; seed BS-001 (guard **100% absent demo** + NO MARK + salary due)/BS-002 (cleaner **present today**)/BS-003 (caretaker **late today**)/BS-004 (driver **on_leave**) + AT-001..008 (relative dates) + PY-001 (Draft)/PY-002 (Paid)/PY-003 (Draft net 16000) + staff_payroll_alert_days=7/staff_absent_pct=30.
- **Frontend** (dashboard 919,216 → 950,326 B): new 👷 **Staff Watch** module (Operations group after Systems Watch; ALL + svc_mgr + **hr** mods; MOD_ICON 👷 / MOD_TITLE / BN 'স্টাফ ওয়াচ') — renderer `staffwatch()` (exact module-id — P44 trap), `staffStaff()` gate (adds hr), `staffLoad` (live apiCall summary → refresh DB; demo `demoSaffData` reads `window.__staffCfg` so config saves persist), `staffHtml` (4 stat cards Staff/On duty/Absent-late/Salary due + kr-tabs via `state.staffTab`: 👥 Roster (table w/ NO MARK red + absent% red badge + ✎/🕐/💰(active)/✕) · 🕐 Attendance (table + ✕) · 💰 Payroll (table + 💵 Pay Draft-only/✕) + 🖨 Report). Mutators: staffForm/submitStaff, staffStatus, staffMark/submitStaffMark (demo upsert by staff+date), staffAttendanceDelete, staffPayrollForm/submitStaffPayroll (prev-month default, demo mirrors absent/net math), staffPayrollPay, staffPayrollDelete, staffDelete (cascade), staffConfigDrawer/staffSaveConfig, staffReport (live fetch + Bearer → previewDoc; demo builds HTML client-side w/ স্টাফ ওয়াচ header). Demo parity: SEED 4 staff/8 attendance/3 payroll; `staffEnrich()` mirrors PHP; **all 14 mutators DEMO_MODE-branched**; `staffCap()` helper (no ucfirst); bottom bar still 5 items.
- **Bugs found & fixed**: (1) **rep() consumed the app-systems case close (3rd hit — P50/P52/P53)** — anchor included the systems final json_out + `}` → dropped → 'unexpected token case'; restore brace, anchor on `default:` alone; **check the junction text first** (P53's was `exit;\n    }` not `}\n\n`). (2) **Intent shadowing (2nd class-hit)** — generic `staff` regex containing `duty|attendance` stole "who is on duty today" → split attendance intent BEFORE the generic. (3) **module id ≠ 'staff'** — 'staff' = app_users CRUD collection (ST-###) + HR & Admin view; module = `staffwatch`, rows BS-###. (4) **PY-### not SP-###** (SP = partners). (5) Test-order: report before mutations; mutations on fresh BS-1xx; alert preamble sets payroll_alert_days=1 then restores; payroll net asserted against post-save salary (10500 = 11000+500+1000−2000).
- **Live**: ?v=3.43 / SW v51 (53/53 files; 28/28 web at v=3.43, 0 stale); API 998,804 B (FTP byte-verified; probe app-staffwatch → 401 not 404, app-systems still 401); dashboard 950,326 B (byte-identical); DB backup `/root/krtaker-backup/pre-p53_20260803_191812/` (**nested subdir** 20260803_191813/krtaker.db — top-level krtaker.db is a 0-byte placeholder; integrity ok, 100 tables, sys present, staff absent); app-setup seeded; live E2E **44/44** (RBAC incl. hr 200, summary ≥, staff lifecycle + double-delete, attendance mark/upsert/delete, payroll auto-net/pay/double-409/delete, report bilingual, config clamps 120/50 + restore, 4 KR intents + generate-invoice + vacant intact, bootstrap, export, GET whitelist); live browser verified as owner (nav('staffwatch'): 4 staff / On duty 2 / Salary due 2, BS-001 NO MARK + 100% absent, BS-002 present, BS-003 late, BS-004 on_leave, live create BS-100 → board 5 → API delete → 4, typeof staffLoad = new bundle, sw.js v51) **0 JS errors**; demo rig verified (3 tabs, add BS-101 → mark → payroll net 9500 → pay Paid → delete cascade, config persists 10/25, report preview w/ স্টাফ ওয়াচ, tenant gated, bottom bar 5) **0 JS errors**.
- **Regression**: 1997/1997 (43 suites; test_p53 95/95; run_all P53 reset restores BS/AT/PY seed + statuses + deletes type='staffwatch' kr_alerts + staff_% org_settings).


### Demo-complete batch P54 — Kalyan Samity (2026-08-03, `?v=3.44` / SW v52, 2045/2045)
- **P54 Kalyan Samity (VAS-J — IDEA 2 §4B Flat Owners' Association: committee registry + monthly service-charge bills per unit + collection ledger; fuel (P52) + payroll (P53) already existed — this is the collection core; VAS alphabet completes A→J)** — tables `samity_members` (SM-###; name/role Chairman|Secretary|Treasurer|Member/phone/since_date/status active|inactive) + `samity_bills` (SB-###; **owner_email on the row** (properties has NO owner_email — land/build/gate/fire pattern)/unit/month/amount/due_date/status Pending|Partial|Paid|Waived) + `samity_collections` (SC-###; bill/amount/method Cash|bKash|Nagad|Bank/collected_at/receipt_no RCPT-SC-###). **`samity_bill_enrich()`**: collected=SUM(collections), outstanding=max(0, amount−collected), computed_status auto (Paid when collected≥amount, Partial when >0), **overdue = due_date<today && status∉{Paid,Waived}** (computed on read, P47 class). `samity_fund()`: total_billed/total_collected/outstanding/collection_rate. `samity_unit_label()`: units table column is **`name`, NOT `label`** — first cut 500'd "no such column: u.label". **`app-samity`** (module `samity` = superadmin/owner/manager/svc_mgr ONLY — hr/tenant/partner/legal/accountant/crm 403; owner self-scoped email+''; 404-before-403): summary (members/fund/outstanding/collection_rate/overdue), member CRUD, bill CRUD (delete cascades collections), collection create (**overpayment → 400**, auto Paid/Partial), collection delete (reverts bill status), config (`samity_alert_days` clamp 1–120, `samity_default_charge` 1–100000; owner/manager save only), bilingual report (committee + per-unit ledger + fund + signature blocks, echo+exit). **KR tools** samity_status (SB-### → includes **bill id in reply** — needed for tests)/samity_bills_due/samity_arrears; intents BEFORE generate-invoice: `(SB-\d+)` prefix-gated FIRST → samity_status, then `charges? due|bill due|বিল বাকি|দেয়` → bills_due, then generic `service charge|সার্ভিস চার্জ|কাল্যান সমিতি|সমিতি|samity|society|kalyan` → samity_status — **NEW CLASS-TRAP (Bengali keyword theft, 3rd instance)**: bare `(arrears|বকেয়া|পাওনা|outstanding)` STOLE test_p11's `বকেয়া রিমাইন্ডার পাঠাও` (rent-reminder) because বকেয়া lives in both domains; fix = require a samity-context keyword: `(arrears|বকেয়া|পাওনা|outstanding) && (service charge|সার্ভিস চার্জ|কাল্যান সমিতি|সমিতি|samity|society|বিল)`; verify test_p11's canonical query still routes — check the FULL reply (100-char truncation hides the "reminder email(s) sent" suffix). **kr_alert_scan** samity pass (per samity_bills.owner_email → sub:N): bill overdue → **warning** `-ov`, due ≤ alert_days → **info** `-sd`. Bootstrap staff full / tenant empty; export list + reset=1 wipe + `samity_%` org_settings; GET whitelist entry; seed SM-001..004 (Chairman/Secretary/Treasurer/Member) + SB-001 (Paid)/SB-002 (Partial 1500)/SB-003 (Pending → **overdue demo**)/SB-004/005 (current month Pending) + SC-001/002 + 2 samity_% keys.
- **Frontend** (dashboard 950,326 → 972,154 B): new 🏘️ **Kalyan Samity** module (Operations group after Staff Watch; ALL + svc_mgr mods; MOD_ICON 🏘️ / MOD_TITLE / BN 'কাল্যান সমিতি') — renderer `samity()` (exact module-id — P44 trap), `samityStaff()` gate, `samityLoad` (live apiCall summary → refresh DB.samity_*; demo `demoSamityData` reads `window.__samityCfg` so config saves persist), `samityHtml` (4 stat cards Members/Fund/Outstanding/Collected + kr-tabs via `state.samityTab`: 👥 Committee (members table w/ role badges + ✎/✕) · 🧾 Bills (per-unit monthly bills w/ OVERDUE/Partial/Paid badges + 💰 Collect/✎/✕) · 💰 Collections (ledger w/ method badges + ✕) + 🖨 Report). Mutators: samityMemberForm/submitSamityMember, samityMemberDelete, samityBillForm/submitSamityBill (month + amount + due_date), samityCollect/submitSamityCollection (drawer: amount + method; demo computes Paid/Partial + overpayment 400), samityCollectionDelete (reverts bill status), samityConfigDrawer/samitySaveConfig (2 clamped values), samityReport (live fetch API?action=report + Bearer → previewDoc; demo builds HTML client-side w/ কাল্যান সমিতি header). Demo parity: SEED 4 members/5 bills/2 collections; `samityBillEnrich()` + `samityFund()` mirror PHP; **all mutators DEMO_MODE-branched**; `samityCap()` helper (no ucfirst — P51 trap); bottom bar still 5 items (sidebar-only). **DRAWER API = `rowItem('key', html)` + `openDrawer(title, body, footer)` + `val('id')` — there is NO `drawerForm()`/`readDrawer()` in dashboard-v2.html** (ReferenceError: drawerForm is not defined only surfaces at runtime); check an existing module's form (staffForm/submitStaff) BEFORE writing new drawers.
- **Bugs found & fixed**: (1) **`units.label` → `units.name`** — first cut joined units on `label` → "no such column: u.label" 500 (the units column is `name`). (2) **`ins_before` on line-TAIL anchors doubles the tail (NEW class-trap, hit twice)** — anchors that END a list/line (`'app-staffwatch'], true)) {`, export `...staff_payroll'];`) must use `rep(anchor, new_tail)`; `ins_before` leaves the old tail in place → doubled text (`'app-staffwatch','app-samity'], true)) {'app-staffwatch'], true)) {`) + php -l fail; `count==1` does NOT catch it (anchor unique, placement wrong). Rule: `ins_before` for mid-line/statement anchors only; line-tail anchors → `rep`. (3) **JS helper block inside `renderers={...}`** — anchoring on `  remit(){` (a renderer method) put functions between object properties → `Unexpected identifier`; insert after the object's closing `};`; **`src.find('\n}', start)+1` off-by-one DROPS the closing brace** — samityReport lost its final `}` (whole-file braces off by one; re-append). (4) **KR intent shadowing (3rd class-hit, Bengali)** — bare `বকেয়া` regex stole test_p11's rent-reminder → require samity-context keyword; verify test_p11's FULL reply. (5) **Rig DB = local mirror** `/home/krtaker/krtaker_landing.db` (NOT test.db) — rig API must point there (run_all reset + suites' direct sqlite connects target it); test.db desyncs run_all from the suites → phantom totals. (6) **test_p54 output format** — bare `PASS 48` is INVISIBLE to run_all's parser (matches `N passed, N failed` or `PASS n / m`); print `PASS {p} / {p+f}` or the suite never enters the total (symptom: TOTAL ≈ previous phase's count). (7) **run_all reset must clean cross-suite leftovers** — test_p24's U-A24/L-A24/T-A24 rows (crash leftovers inflate rent base, break test_p25's exact 85,000) + test_p22's premtest@ subscribers (UNIQUE constraint crash on re-run). (8) Transient login 500s during run_all = concurrent browser tests against the same rig DB — never run live browser verification on the rig while the regression executes; rerun clean afterwards.
- **Live**: ?v=3.44 / SW v52 (53/53 files; web/sw.js krtaker-site-v52; 28/28 web files at v=3.44, 0 stale); API 1,029,983 B (FTP byte-verified; probe app-samity → 401 not 404, app-staffwatch still 401); dashboard 972,154 B (byte-identical to repo); DB backup `/root/krtaker-backup/pre-p54/` (integrity ok — pre-state 14 subscribers, 0 samity tables); app-setup?key=kr-phase2-2026 (schema + seeds: 4 members/5 bills/2 collections + 2 samity_% keys); live E2E **27/27** (RBAC incl. hr 403 / owner 200, summary fund ৳4,500 / outstanding ৳10,500 / 30% collected / 2 overdue, member lifecycle + double-delete, bill create + collect (overpayment 400, auto Paid/Partial), collection delete reverts bill status, report bilingual w/ কাল্যান সমিতি, config clamps 120/100000 + restore, 4 KR intents + generate-invoice + vacant intact, bootstrap scoping, export, GET whitelist); live browser verified as owner (nav('samity'): Members 4 / Fund ৳4,500 / Outstanding ৳10,500 / 30% / 2 overdue, SB-003 OVERDUE, live create member → board 5 → API delete → 4, typeof samityLoad = new bundle, sw.js v52 served, web pages v=3.44) **0 JS errors**; demo rig verified (3 tabs, add member, bill + collect → Paid, collection delete reverts, config persists, report preview w/ কাল্যান সমিতি, tenant gated, bottom bar 5 items) **0 JS errors**.
- **Regression**: 2045/2045 (44 suites; test_p54 48/48; run_all P54 reset restores SM/SB/SC seed rows + statuses + deletes type='samity' kr_alerts + samity_% org_settings).


### Demo-complete batch P55 — Package & Access Management + Menu Reorg + Photos (2026-08-03, `?v=3.45` / SW v53, 2102/2102)
- **P55 Package & Access Management (driven by the Building & Community Management spec: "Role Delegation" + plan-gated access)** — `plan_catalog` gains **`modules`** (per-role module matrix JSON) + **`limits`** (property_limit/unit_limit/seats/kr_ai/api_access/reports JSON) via ALTER guards; new helpers `MODULE_REGISTRY()` (42 built + 6 roadmap slots: parking/bookings/voting/forums/events/insurance), `package_row()`, `package_modules_map()`, `package_limits_map()`, `plan_for_user()` (sub → plan, staff → enterprise), `effective_modules($u,$role)` (**package restricts, role is base**; matrix entry missing → **zero modules** — no more fall-through to full base; empty matrix → full base for legacy), `effective_limits()`. `require_module()` now checks effective modules (403 message carries plan). `user_payload()` returns `modules` + **`role_modules`** (all 10 roles under the user's package — drives role-switch preview) + `limits` + `photo`. Seed: demo subscribers owner@/sultana@/rahim@ → **Enterprise** (full staff-parity; INSERT OR IGNORE so live rows updated via assign endpoint), 3 plans with starter/business/enterprise matrices + limits, backfill guard for pre-existing rows. `app-packages` (superadmin-only via base ROLE_MODULES; owner/others 403): list (packages+registry+roles+subscriber counts), save (create/update, code `^[a-z0-9_]{2,24}$` 400, name required), delete (in-use 409), subscribers (list + plans), assign (subscriber ↔ plan, audit), preview (plan+role → effective modules/limits), limits-usage. Bootstrap/me carry `package` payload; app-subscribe validates against plan_catalog. **Menu reorg**: 5 flat groups → **7** (Overview / Portfolio / Finance / Community / Operations / Safety & Security / Admin); all 41 nav modules present, groups auto-hide when empty (starter sub sees only 4). Frontend `can()` uses `LIVE.user.role_modules` when live (demo falls back to ROLES mods); new **Packages** module (renderers.packages + packagesLoad/renderPackages/packageForm/submitPackage/packageMatrixDrawer/savePackageMatrix/packageLimitsDrawer/savePackageLimits/packagePreview(Run)/packageSubscribers/packageAssign/packageDelete + DEMO_PACKAGES/DEMO_REGISTRY/DEMO_SUBS); Subscriptions page gains "Plan limits & usage" strip (properties/units bars, seats, KR AI, API). Version ?v=3.45 / SW v53 (28/28 web, 0 stale); API 1,047,861 B (FTP byte-verified); dashboard 1,000,689 B; backup pre-p55_20260803/20260803_233848 (14 subscribers, 3 plans, integrity ok); live probes 401; live smoke 15/15; browser 0 JS errors; regression **2102/2102 (45 suites; test_p55 57/57)**.
- **P55.1 Design-system fix + tenant/user photos (user-reported broken forms)** — root cause: `.lb`/`.inp`/`.chk` classes used by EVERY drawer form had **no CSS rules** → all drawer inputs rendered browser-default (2px inset border, 0 radius, 0 padding). Added `.drawer-b .lb/.inp/.chk` (11.5px 600 labels, 10px-radius padded inputs, themed checkboxes) — fixes all drawer forms app-wide; rewrote the Packages drawers (packageForm/packageLimitsDrawer/subscribers select/preview select/matrix checkboxes) onto the standard `rowItem` + lb/inp pattern. **Photos**: `photo` columns on tenants/app_users/subscribers (ALTER guards); `app-photo` endpoint — `?action=tenant-upload` (multipart, tn_<id>_<ts>.<ext>, images only, replaces+unlinks old), `?action=user-upload` (self, us_<uid>_<ts>.<ext>), `?action=view&target=tenant|user&id=` (authed bytes echo, GET-whitelisted); user_payload carries `photo`. Frontend: `avatarMarkup`/`photoBlobUrl`/`attachPhoto`/`loadTenantPhotos` (fetch-with-Bearer → blob URL — `<img>` can't send headers, the 401 trap), sidebar+topbar avatars, tenants list thumbnail + per-row 📷 drawer (tenantPhotoDrawer/submitTenantPhoto), Settings profile photo upload (setPhotoPreview/submitUserPhoto). Demo parity + role gating (tenant photos for superadmin/owner/manager/accountant-ish via tenants module; user photo self-only). **Class-notes (full: `references/p55-packages.md`)**: form classes `.lb/.inp` are bare semantic classes — NEVER rely on them being styled; the design-system CSS lives in the drawer block. Async loaders must guard `if(el)` or defer `setTimeout(...,0)` (packagesLoad null-DOM crash). `<img src>` can't auth — use fetch→blob URL for any authed media (doc-view pattern). Backfill guards for new JSON columns must be reachable on EXISTING DBs (don't scope seed vars inside `if count==0`). Roles missing from a package matrix must get ZERO modules (not base). Regression 2102/2102 (45 suites; test_p55 57/57 incl. 8 photo checks).


### Demo-complete batch P56 — Tab Profile View + Tenant/User Photo in Forms (2026-08-04, `?v=3.46` / SW v54, 2119/2119)
- **P56 (user: "Add user and tenant photo, introduce tab system profile view" + "Where can I find the photo of the tenant?")** — photos existed since P55 but were only reachable via a tiny 📷 button in the tenants-table last column; NOT in the add/edit form and NOT in the detail view. This batch makes the photo first-class and gives tenants a real profile view. **Tenant form (add/edit)**: `tenantPhotoFormBlock(row)` prepended to the form-grid in `openForm` (avatar preview `#fPhotoPrev` + file input `f_photo_file`, existing photo attach-loaded); `saveForm` now photo-aware — after crud success it uploads via `app-photo?action=tenant-upload` with the created id (create path included), then re-bootstraps + nav('tenants'); drawer closes + toast regardless (upload error non-fatal). **Tab profile view** (reuses the Phase-26 `krTabBar`/`switchTab` system, prefix `ten`): `tenantTabsHtml(tenId)` → 📋 Profile / 📄 Lease / 💳 Billing / 🔧 Tickets; `openDetail` for tenants replaces body with tabs (`__KV__`/`__REL__` markers like properties) + `tenantProfileHeaderHtml` (72px photo avatar `#tenPhoto` + name/kind/NRB + inline 📷 Change photo button) + `attachPhoto(...)` after draw; detail footer gains a 📷 Photo button for superadmin/owner/manager. Lease tab = tenant's leases (unit/property/term/rent/adv, click-through); Billing tab = per-invoice paid/due **computed from `DB.payments`** (not the seed's empty `i.paid`/`i.due` fields — first cut showed ৳0 paid on a Paid invoice) + billed/paid/outstanding summary; Tickets tab = tickets on the tenant's units. **Class-notes (full: `references/p56-tabs-photos.md`)**: patch-tool escapes DOUBLE backslashes inside `\'` sequences in JS template literals (the openForm foot line) — verify with python repr, fix via python replace, NOT patch. Rig `php -S` persistent `$pdo` can hold a stale WAL read snapshot between requests → immediate create→upload 404 (fresh server / restart clears it; production FPM is per-request so unaffected). Multipart test helper MUST put the `--boundary\r\n` separator BEFORE every subsequent field — PHP silently drops the `id` field otherwise ("Tenant not found" 404). Same-second `date('Ymd_His')` filenames collide → re-upload overwrites (correct semantics); tests need a 1.1s gap to assert distinct names. Real 1x1 PNG for mime_content_type to report image/png (fake header+zeros → application/octet-stream).
- **Live**: ?v=3.46 / SW v54 (53/53 files; web/sw.js krtaker-site-v54; 28/28 web at v=3.46, 0 stale); API unchanged 1,047,861 B (frontend-only batch); dashboard **1,009,868 B** (FTP byte-identical to repo); DB backup `/root/krtaker-backup/pre-p56_20260804_064123/` (1,511,424 B, integrity ok, 14 subscribers, 3 plans); live probes 401/403; live smoke **9/9** (owner login, bootstrap tenants + photo field, crud create → tenant photo upload → bootstrap carries photo → view roundtrip image/png exact bytes, user self photo, me payload photo, delete); live browser verified as owner (T-001 detail: 4 tabs 📋/📄/💳/🔧, photo header + Change photo button, Billing paid ৳25,000 / outstanding ৳25,000 from real payments, Lease 1 row, Tickets 1 row, edit form photo file+preview, new-form photo input) **0 JS errors**; demo rig verified identical (T-001 tabs, form photo blocks) **0 JS errors**.
- **Regression**: 2119/2119 (46 suites; test_p56 17/17 — form-save sequence create→upload→payload, replace keeps 1 file, user photo, tenant-role view allowed/upload 403, tab data sources lease→invoice→ticket; run_all P56 reset: photo cleanup already wipes tn_*/us_* files + owner photo + smoke tenant rows).

## SA1-fullsite + SA1-fullsite-v2 (2026-08-04)
- **Full-site CMS**: 496 blocks, 19 page tabs (Home…Register + Site) + Footer/SEO; every section of every landing page editable (heroes, cards, FAQ, compare table, footers, CTA band, partners). Defaults == live text (design/data untouched).
- **API Library** (superadmin → Developers): 85-endpoint catalog, live test console (history), web/mobile API key rotation, data stats + table browser (24 tables). Backend: api-keys-get/save, api-stats, api-table.
- **Advanced CMS v2**: section visibility toggles (cms_vis), media library + upload (/assets/cms), Site tab (announcement bar, GA4, custom head/footer code), per-section reset, per-page open links, image picker on image fields.
- Hydrate: page-aware titles (18-page map), data-cms-html, vis hiding, announcement/GA4/custom-code injection; silent fallback.
- Versions: ?v=3.53/v60 → ?v=3.54/v61. Commits f28ce17, a2b3980 → 3 remotes. Regression 2315/2315 (test_superadmin 151/151). Live verified.

## SA1-fullsite-v3 (2026-08-04): CMS redesign + API Library v3 + Global search
- **Global search** (topbar, Ctrl+K): grouped results across subscribers/users/packages/providers/tickets/leads/CMS/finance/audit; ↑↓/↵/Esc keyboard nav; deep-link → nav(view) + row/field highlight (gsHighlight). Backend `global-search` sub-action.
- **CMS redesign (easy to configure)**: collapsible sections + section jump bar; dirty tracking (● + unsaved count, Ctrl+S save); per-page 🟢 Live/🔴 Draft (cms_pub table — draft pages serve defaults on public cms-read, verified); 🔁 global find & replace (dry-run preview, page/section scope, history-backed); 📄 clone page (copies blocks+vis, starts Draft); field-type inputs (color/url/number/image); all-page search with jump-to-block; generic page preview; EN/BN per-section; cmsSyncDom so tab switches don't lose edits.
- **API Library v3**: 📈 Usage tab (api_usage telemetry logs every request via register_shutdown_function — 24h/7d totals, top endpoints, errors, latency, auth mix, recent requests); endpoint rows clickable → detail drawer (how-to-call + Try in console); key lifecycle — web/mobile keys now enforceable (X-API-Key on app-photo/app-job-media), rate limit (api_rate_limit_ok), last-used/last-IP tracking (api_key_meta); 📦 OpenAPI schema export. Catalog → 91 endpoints.
- Backend: api_usage + cms_pub tables; sub-actions global-search, api-usage, cms-pub-get/save, cms-replace, cms-clone, extended api-keys-get/save.
- Versions: ?v=3.54/v61 → ?v=3.55/v62. Commit 086e12a → 3 remotes. Regression 2333/2333 (test_superadmin 169/169). Live verified (login, search 8 items/3 groups, CMS pub+replace+usage tab, 0 JS errors).

## SA1-fullsite-v8 (2026-08-05): Tenant Webhooks (v3.62 / SW v69)
- **User**: "next" after v3.61 — spec gap #2 (Ai Instructions.txt): **Webhooks** (events + destination URL + delivery log).
- **Backend**: `webhooks` + `webhook_logs` tables; `WEBHOOK_EVENTS()` catalog (payment.success, ticket.created, lease.signed, subscriber.registered, test.ping); `webhook_dispatch()` — POST JSON {event,ts,data} to every active subscribed hook, `X-KRTaker-Signature: sha256=HMAC(body,secret)` + Event/Delivery/Hook headers, curl 10s, every attempt logged (ok/status/ms/response), ok/fail counters, never throws. app-admin sub-actions: webhook-events/list/save (validate http(s) URL + known events + existing subscriber; secret auto-gen 32-hex; `regenerate` rotate)/delete (cascades logs)/toggle/test (fires test.ping, returns delivery — bypasses active by design)/logs/log-clear. **4 real dispatch hooks**: app-premium-billing pay → payment.success · app-ticket-create → ticket.created · app-onboarding complete → lease.signed · register → subscriber.registered (note: register reads `$body['pass']`).
- **Frontend**: Webhooks console in **Developers group** (renderNav — the sidebar trap; guarded): summary chips, endpoint table (label/url/subscriber/event chips/click-to-toggle status/last delivery + ok-fail counts), 🧪 test / 📜 logs / ✎ edit / 🗑 delete; form drawer (label, subscriber select, URL, event checkboxes, Enabled + Rotate secret); 🔑 secret-shown-once drawer with copy; 📜 delivery-log drawer with 🧹 Clear.
- **Verify**: rig smokes (HMAC recompute match, fail log, 4 real events, filter, inactive skip) · test_superadmin **339/339** · full **2503/2503** (48 suites) · live API probe **13/13** (postman-echo 200 delivery; httpbin flaky 503 → use postman-echo) · live browser E2E full loop (create → secret → test → delivery log `test.ping 200 228ms` → toggle/delete → 0 endpoints) **0 JS errors** · live DB clean.
- **Versions**: ?v=3.61→3.62 (28 pages), sw krtaker-site-v68→v69. Commit 5271c50 → 3 remotes. Skill ref `sa1-fullsite-v8.md`.

## SA1-fullsite-v9 (2026-08-05): Per-Tenant API Keys (v3.63 / SW v70)
- **User**: "next" after v3.62 — spec gap #3 (Ai Instructions.txt): **per-tenant API keys** (tenant_id + rotate/revoke).
- **Backend**: `tenant_api_keys` table (tenant_id, label, key UNIQUE `krt_`+32hex, active, created_at, last_used_at, last_ip, calls). Helpers `tenant_key_gen()` / `tenant_key_find()` (active only) / `tenant_key_touch()` / `api_key_resolve()` (web/mobile/tenant). Enforcement block (app-photo/app-job-media/app-tenant-me) now resolves tenant keys: per-tenant rate bucket `rl_<kind>_t<tenant>_<min>`, `X-KRTaker-Key: kind[:tenant_id]` header. **`app-tenant-me`** (GET whitelist, tenant-key ONLY): X-API-Key → subscriber profile (id/name/org/email/plan/plan_name/status/trial_end) + key stats (label/created/last_used/last_ip/calls); touches usage. app-admin sub-actions: tenant-keys (masked list + subscriber join), tenant-key-save (create returns full key once / update), tenant-key-rotate (old invalid immediately), tenant-key-toggle, tenant-key-delete (revoke) — all audited.
- **Frontend**: API Library 5th tab **🏢 Tenant Keys** (apiTab 'tkeys'): summary (keys/active/calls), table (subscriber, label, masked key, click-to-toggle badge, last used+IP, calls, 🔄/✎/🗑), ＋ form drawer, 🔑 key-shown-once drawer with copy (create + rotate), 'How tenant keys work' panel; app-tenant-me in endpoint catalog.
- **Verify**: rig smoke full lifecycle · test_superadmin **373/373** · full **2537/2537** (48 suites) · live API probe **17/17** · live browser E2E (tab → create → key-once drawer → live app-tenant-me fetch 200 + 2 calls → rotate → toggle → revoke → 0 keys) **0 JS errors** · live DB clean (enforce untouched).
- **Class-notes**: native confirm() blocks headless E2E (override `window.confirm=()=>true` after login BEFORE any confirm action — a stuck dialog hangs every subsequent browser_console call until reload); select row-action buttons by title attribute, not `textContent.includes('')` (empty needle = first button); strip non-ASCII before returning innerText through browser_console (emoji surrogates break JSON).
- **Versions**: ?v=3.62→3.63 (28 pages), sw krtaker-site-v69→v70. Commit 73a9789 → 3 remotes. Skill ref `sa1-fullsite-v9.md`.

## SA1-fullsite-v10 (2026-08-12): Table Toolkit (v3.64 / SW v71)
User: "Add pagination and advanced filter options in each table view of Super Admin Panel. And then continue to next tasks (CNAME verify → Theming v2)." First half shipped as **v3.64 / SW v71** — pure frontend.
- **Toolkit** (`tblState/tblApply/tblBar/tblTh/tblPanel/tblDraw/tblSetQ/tblSetF/tblClear/tblGo/tblSetPs/tblSort/tblReg`): per-view state `TBLK[key]={q,f,p,ps,s,dir}`; search (multi-col, case-insensitive) + exact-match dropdown filters (auto unique values or explicit `[{v,l}]` opts) + numeric-aware sortable headers (⇅/▲/▼) + pagination (‹ pages › ellipsis, 10/25/50/100, "1–10 of N") + Clear (n). Search re-renders body only (input keeps focus); filter re-renders bar via outerHTML.
- **Applied to 18 tables / 16 views**: Dashboard recent subs, Subscribers, Users, Providers, Packages, Blog, Onboarding (3), MIS ledger, Payments integrations, Finance ledger, Integrations, Tickets, Backup, Modules, Webhooks, Audit, Tenant Keys, API usage recent, AI chat telemetry, API Data drawer.
- **Verify**: test_superadmin **397/397** (373+24) · full **2561/2561** (48 suites) · live browser E2E (dashboard filters → subscribers search/sort/page 2 → audit 20 pages → users/tenant-keys/webhooks filters) **0 JS errors** · live DB untouched. Note: replaced stale `/tmp/krtest/index.php` (old API copy shadowing bare `/sitemap`).
- **Versions**: ?v=3.63→3.64 (28 pages), sw krtaker-site-v70→v71. Commit dd0c69f → 3 remotes. Skill ref `sa1-fullsite-v10.md`.

## SA1-fullsite-v11 (2026-08-12): Custom Domains — CNAME/TXT verify (v3.65 / SW v72)
User: "…continue to next tasks (CNAME verify → Theming v2)." Second half batch, first item shipped as **v3.65 / SW v72**.
- **Backend**: `tenant_domains` table; app-admin `cnames`/`cname-save` (token `krt-verify-`+24hex returned once, domain validation, krtaker.com + dupes rejected, update resets verified)/`cname-verify` (real `dns_get_record` TXT on `_krtaker.<domain>`, audited)/`cname-delete`. **`host-tenant`** public GET: Host (or body host) → verified tenant profile + wl_ branding; port/www normalized.
- **Frontend**: 🌐 Custom Domains view (Developers group) — stats, toolkit table (domain/tenant/status/masked token+note/verified-at/🔎✎🗑), add-form → TXT token drawer with `_krtaker.<domain>` + copy, Check DNS → ✓ or note, delete confirm; `host-tenant` in API catalog.
- **Verify**: rig smoke **19/19** · test_superadmin **428/428** (397+31) · full **2592/2592** (48 suites) · live API probe **11/11** · live browser E2E (add → token drawer → real DNS check fails with note → delete → 0 rows) **0 JS errors** · live DB clean.
- **Versions**: ?v=3.64→3.65 (28 pages), sw v71→v72. Commit c1f408a → 3 remotes. Skill ref `sa1-fullsite-v11.md`.

## SA1-fullsite-v12 (2026-08-12): Theming v2 (v3.66 / SW v73)
User: "…continue to next tasks (CNAME verify → Theming v2)." Final item of the batch shipped as **v3.66 / SW v73**.
- **Backend**: whitelabel defaults + `wl_secondary_color`/`wl_accent_color`/`wl_logo_url`; **`app-theme`** public GET → theme {site_name, logo_text, primary, secondary, accent, logo_url, favicon, theme} from wl_% keys (no auth — needed pre-login).
- **Frontend**: White Label 🎨 Brand colors (type=color pickers + hex text sync + live preview swatches + logo URL) + `applyAdminTheme()` (panel CSS vars after login/boot); dashboard `applyBrandTheme()` (fetch app-theme → CSS vars + logo mark + favicon + title, fires every load). ⚠️ Name collision pitfall: pre-existing `applyTheme()` (dark toggle) shadows later declarations — used `applyBrandTheme`.
- **Verify**: rig smoke **9/9** · test_superadmin **446/446** (428+18) · full **2610/2610** (48 suites) · live E2E (set purple via UI → app-theme reflects → panel re-themes → dashboard auto-themes on load → restore → PRISTINE) **0 JS errors**.
- **Versions**: ?v=3.65→3.66 (28 pages), sw v72→v73. Commit d834bda (amended) → 3 remotes. Skill ref `sa1-fullsite-v12.md`.

## SA1-fullsite-v12.1 (2026-08-12): Web CMS bulk edit (v6 follow-up, panel-only)
User: "Next" after v3.66 closeout — first item from the offered v6 follow-ups (bulk-edit multiple fields at once / apply one value to several matching fields). Panel-only (superadmin.html, no SW → **no version bump**; live stays ?v=3.66/SW v73).
- **Frontend only** (docs/superadmin.html): in the Web CMS ⚙️ filter results view, every field now gets a ☑ bulk checkbox (`cmsFieldHtml(b,sel)`, `.cms-sel`); a **Bulk edit toolbar** (`#cmsBulkBar`) renders in the results header — Select all / None / "N selected" / ✏️ Apply to selected… (disabled at 0). `CMS.sel` (Set) + `CMS.fltM` (last match tags) state; selection survives re-renders, cleared on filter clear/jump (`cmsClearFilter`/`cmsFilterJump`).
- **Bulk drawer** (`cmsBulkDrawer`): type-aware input — color picker+hex sync for color fields, number input for num, text otherwise; "mixed field types" note when selection spans types; lists first 8 selected fields. `cmsBulkApply` writes ONE value into every selected block, marks them dirty (`CMS.dirty`), clears selection, re-applies filter — then the existing 💾 Save all persists via `cms-save` (history/restore still apply).
- **Verify**: rig smoke (select-all 13 URL matches → drawer → apply → dirty 13 → save → DB rows updated → restore) · test_superadmin **452/452** (446+6) · live browser E2E (login → Web CMS → url filter 13 matches → 13 checkboxes → select 3 → apply `https://bulk-live-e2e.local` → save → live DB shows new value → restored → PRISTINE; select-all/none, clear, mixed-type note) **0 JS errors**.
- **Commit** 4977840 → 3 remotes (origin/backup/mirror-kabirswe). Deployed: targeted FTP superadmin.html only. Skill ref `sa1-fullsite-v13.md`.

## SA1-fullsite-v12.2 (2026-08-12): 👁 View-details drawers (v6 follow-up, panel-only)
User: "in the superadmin.html, Subscribers, Service Providers, Onboarding & CRM & User Management add view details (drawer open)". Panel-only + 1-line API extend → **no version bump** (live stays ?v=3.66/SW v73).
- **Backend** (1 line): `subscribers` admin action now also SELECTs `verified_at, last_login` (were missing → detail drawer for subs would be thin).
- **Frontend** (docs/superadmin.html): generic `detDrawer(icon,title,row,spec)` + `detVal(v,k,row)` — formatted read-only detail drawer: `badge()` for status/role/plan, Active/Suspended for users, ⭐ rating, ৳ money for reward/rent/adv, skips empty fields, Close button. Per-view functions: `subscriberDetail(id)`, `userDetail(id)`, `providerDetail(id)`, `leadDetail(id)`, `onbDetail(id)`, `refDetail(id)`.
- **👁 buttons** added to every row: Subscribers (before 🔀), Users (before 🔀, after existing buttons), Providers (first action — was edit-only), Onboarding **all 3 tables** (Leads/Applications/Referrals gained a new actions column + empty-state colspan bump). ⚠️ Onboarding IDs are strings (`LD-001`/`OB-001`/`REF-…`) → onclick must quote them (`leadDetail('${esc(x.id)}')`); unquoted = JS ReferenceError on click.
- **Verify**: rig API smoke (subscribers has verified_at/last_login) · test_superadmin **458/458** (452+6) · full **2622/2622** (48 suites) · rig browser (all 6 drawer types, real data, badges/money) · live browser E2E (Subscriber #124 / User #1 / Provider SP-01 / Lead LD-006 / Onboarding OB-100 / Referral REF-A1B2C3 — all open, correct fields, ৳/⭐/badges) **0 JS errors** · live DB untouched.
- **Commit** af46696 → 3 remotes. Deployed: API (ftp_api_p45.py) + superadmin.html (targeted FTP). Skill ref `sa1-fullsite-v14.md`.


## SA1-fullsite-v15 (2026-08-12): Onboarding & CRM Kanban + full CRM CRUD + Subscriber profile (panel+API)
User: "In Onboarding & CRM (superadmin.html) add kanban view. Also add more detailed and advanced features. Add more details information in Subscribers (in add, edit & also in the view)." Panel + API extension → **no version bump** (live stays ?v=3.66/SW v73).
- **Backend**: subscribers schema +8 columns (address/city/industry/company_size/website/notes/tags, idempotent ALTER); subscribers list returns per-sub tenant/partner counts; subscriber-save persists new fields. 10 new app-admin endpoints: lead-save/delete/status/convert (→ OB app, lead→Applied), onboard-save/delete/status (verified_at COALESCE), referral-save/delete/status. All audited.
- **Frontend**: per-section 📋 Table ⇄ 🪟 Kanban toggle; 8-stage lead + 5-stage onboarding pipelines with HTML5 drag&drop status moves, age badges, card action buttons (👁 ✎ 🔄 ➡️ 🗑); add/edit/delete drawers for leads/apps/referrals; quick ➡️ pipeline advance; lead → onboarding conversion; Subscribers form + detail extended (address/city/industry/size/website/tags/notes + 🏠 Tenants / 🛠️ Providers stats footer).
- **Verify**: test_superadmin **480/480** (452+28) · full **2644/2644** (48 suites) · rig browser E2E (kanban render 13 cols/87 cards, simulated drag New→Qualified persisted, UI lead create → convert OB-100 → onboard edit w/ verified_at, referral create, subscriber extended form+detail) **0 JS errors** · live probes (subscribers stats keys, lead-save/convert, cleanup) + live browser kanban/subscriber form **0 JS errors** · test data cleaned, live DB untouched.
- **Pitfall**: rig router needs `/tmp/krtest/api/index.php` (dir), not `/tmp/krtest/index.php` — stale copy = "Unknown admin action" on new endpoints.
- **Commit** 23110df → 3 remotes (origin/backup/mirror-kabirswe). Deployed: API (ftp_api_p45.py) + deploy_landing.py 59/59. Skill ref `sa1-fullsite-v15.md`.


## SA1-fullsite-v16 (2026-08-12): Colour-coded kanban + advanced fields on every superadmin form (panel+API)
User: "make the kanban stages colour coded. Update and get advanced field in all forms (for create, view, edit, update) For all pages / menu in superadmin.html." Panel + API → **no version bump** (live stays ?v=3.66/SW v73).
- **Kanban colours**: `KB_COLORS` map (13 stages + gray fallback) → coloured column top-borders, tinted headers + count badges, coloured card left-borders (blue/cyan/violet/amber/green/orange/emerald/red).
- **Backend**: idempotent ALTER loop over 12 tables (users phone/title/employee_id/joined_at/address/notes; providers phone/email/address/city/hourly_rate/specialties/notes; packages description/billing_cycle/trial_days/popular/sort_order/color; blog author/meta_title/meta_desc/cover_image/category; tickets category/due_at/tags; webhooks description/max_retries; integrations description; ledger note/payee; leads budget/move_in; onboarding occupation/employer/reference; referrals referred_phone; subscribers country/source). All save handlers persist new fields; **ticket-save now supports edit**.
- **Frontend**: all 12 forms extended; new detail drawers packageDetail/blogDetail/ticketDetail/integrationDetail/webhookDetail + 👁 buttons on Packages/Blog/Tickets/Integrations/Webhooks rows; detVal handles arrays/billing_cycle/popular/money.
- **Verify**: test_superadmin **511/511** (480+31) · full **2675/2675** (48 suites) · rig runtime smoke all 12 entities + rig browser (coloured kanban, advanced forms, packageDetail populated) **0 JS errors** · live probes (users/providers/subs new cols, package advanced save/delete, ticket create+edit+delete) + live browser (9 coloured columns, user form) **0 JS errors** · live DB untouched.
- **Pitfalls**: integrationSave shared by Payments+Integrations views → view-aware re-render; webhook event ids are payment.success etc (not payment.received); new cols empty on existing rows → detail hides empty fields.
- **Commit** ef6c0fc → 3 remotes. Deployed: API (ftp_api_p45.py) + deploy_landing 59/59. Skill ref `sa1-fullsite-v16.md`.


## SA1-fullsite-v17 (2026-08-12): Real-life lifecycle testing + invoice net bugfix (API fix + tests)
User: "start real life testing of the platform from onboarding to registration to role based users to managing properties to tenant's payments and receipts. Generate multiple scenarios (minimum 12) and test on the based of each scenarios. Give me test cases." → **no version bump** (API fix + test suite only).
- **BUGFIX (deployed live + verified):** app-crud invoice update zeroed `net` on status-only edits → net re-derived only when gross/tds present. Symptom: "Invoice already fully paid." after marking Overdue. Rig + live verified (net preserved 22,500).
- **New suite test_lifecycle.py** — 14 scenarios / 149 checks: landing forms → register/OTP/login → duplicate semantics → owner property→unit→tenant→lease chain (+TPA §107 Pending Registration gate) → full RBAC matrix (7 roles) → tenant own-scope → partner own-job → readback/analytics → invoicing (net derive/print/update) → gateway pay lifecycle (init/confirm/double-confirm/404/cancel/receipt-sig/invoice-Paid) → tenant pays own invoice + collections → statements/payouts RBAC → maintenance ticket lifecycle (tenant→mgr→vendor→svc_mgr) → profile/password/session invalidation → 12 UI wiring guards. **Wired into run_all** → full regression **2824/2824** (49 suites).
- **Report:** docs/LIFECYCLE_TEST_REPORT.md (scenario + test-case tables, findings).
- **Findings:** U-011 orphan unit (p=P-006 missing); INV-2026-0009 zero-pad; Trial plan = full base modules + unlimited limits (no trial catalog row).
- **Rig traps:** TWO API copies — `/tmp/krtest/api/index.php` (router /api/*) AND `/tmp/krtest/index.php` (bare-path directory index, what helpers.api hits) — sync BOTH; stale 8899 process may survive kill → verify ss -tlnp; permission-probe with update {} not create {} (create auto-fills leases status / invoices net).
- **Commit** → 3 remotes. Deployed: API (ftp_api_p45.py). Skill ref `sa1-fullsite-v17.md`.

### SA1-fullsite-v17 round 2 (2026-08-12): fixes for all findings + full re-test
User: "if any test found any bugs / issues, fix it and then continue / start over the testing again, after completion of the tests report give me the test reports."
- **FIX-02** U-011 orphan → seed U-011→P-005 + run_all fixture insert (test_p56 hardcodes U-011); live already clean.
- **FIX-03** invoice pad → run_all normalizes INV-2026-0010 (4-digit) + restores INV-2026-0009 (July 28k; was being deleted → P41 aging regression caught by re-test).
- **FIX-04** Trial ≈ Starter: effective_modules/effective_limits map 'trial'→starter catalog; package.code stays 'trial'.
- **NOTE-02** plan limits display-only (no create enforcement) — per-subscriber enforcement deferred to Phase 7.
- **NULL trap:** dangling cleanup needs `IS NULL OR NOT IN` (NULL silently skips NOT IN).
- Re-test: lifecycle **157/157** (SC-02 +2, SC-15 +6) · run_all **2832/2832** · live redeployed + probe cleaned.
- Commits → 3 remotes. Skill ref `sa1-fullsite-v17.md` (round 2 section). Report: docs/LIFECYCLE_TEST_REPORT.md (Round 2).

## SA1-fullsite-v18 (2026-08-12): Per-subscriber plan limits enforced (panel+API, no version bump)
User: "Start the recommended one" (per-subscriber limit enforcement).
- `sub_email` scoping on properties/units (idempotent migration + backfill v1 → seeded portfolio → Enterprise owner).
- Create-time enforcement in `app-crud`: Trial/Starter capped at 1 property / 5 units (`403 Plan limit reached: 1 of 1 properties. Upgrade to lift limits.`); cross-account unit blocked; create stamps `sub_email`, update strips it for subs; staff/superadmin unaffected; dashboard error toast.
- **Real bug found live:** `plan_catalog.limits='[]'` — old backfill only fixed `'{}'`/empty, and `seed_app()` never ran on live → per-plan caps were inert (Trial users had 9999/99999). Fixed with boot-time self-heal in the schema block.
- Lifecycle **166/166** (SC-16: 9 checks) · regression **2841/2841** · superadmin **511/511** · live curl E2E (prop2/unit6/cross → 403, Enterprise → 200; live DB pristine 5 props/10 units).
- Commit `203c48a` → 3 remotes. Skill ref `sa1-fullsite-v18.md`.

## SA1-fullsite-v19 (2026-08-12): PWA installable dashboard + Web Push notifications (no version bump; SW v73→v74)
User: "Ok next" → PWA + push (DeepSeek/GA4/re-auth all blocked on user keys).
- **Assets:** `web/manifest-dash.json` (start_url=dashboard-v2.html, standalone, shortcuts); `web/sw.js` v74 adds `push` + `notificationclick` + manifest-dash to STATIC; dashboard `<link rel="manifest">` + SW registration + auto-update reload + beforeinstallprompt → Install app button (Settings→Preferences).
- **API:** `push_subs` table; VAPID ES256 keypair embedded; actions `push-state/save/remove/test/send` (Bearer auth, endpoint https + key validation, 10 devices/user cap, upsert on endpoint); `push_to_user()` helper; **real triggers**: `app-ticket-create` → push property owner (unless creator is owner), `app-payment-confirm` → push invoice owner (tenant pays → owner alert).
- **Crypto (RFC 8291/8292), all validated:** `openssl_pkey_derive` needs PEM (raw DER rejected → SPKI→PEM armor); `hash_hkdf` NOT RFC 8291-compatible → manual HKDF-Expand; DER→raw r||s for JWT; aes128gcm framing with AAD=header; cURL h2; 404/410 → dead-sub cleanup.
- **Tests:** Node decrypt round-trip PASS (incl. ৳); JWT signature verified; `test_push.py` **22/22** on rig (FCM 404 on fake token proves full chain reaches Google — 403 would mean bad VAPID); wired into run_all (50 suites). Fake sub keys must be REAL curve points (openssl rejects off-curve).
- **Deploy:** deploy_landing.py + manifest-dash.json; ftp_api_p45.py; rig both-copies sync. Skill ref `sa1-fullsite-v19.md`.

## SA1-fullsite-v20 (2026-08-12): Team / sub-accounts + seat enforcement (panel+API, no version bump)
User: "Next" → last display-only limit (`seats`) closed via subscriber team members.
- **Auth:** token kind `'team'`; `current_user()` resolves member → parent subscriber row overlaid (kind='sub', team_member, team_id, member name/role) so plan/limits/scoping/modules all inherit. **Critical fix:** skip the trailing `$row['kind']=$tok['kind']` overwrite for team tokens or members get Enterprise/full-base (escalation).
- **API:** `team_members` table; actions team-list/invite/reset/update/remove (owner or superadmin only); seats = owner + active members (team_limit = seats−1); invite validates email uniqueness across all identity tables + enforces cap (403); password shown once; role change / disable / remove revokes tokens.
- **Self-service safety:** `app-profile` routes members to their OWN row (team_members, team_id, token kind 'team') — member password/name edits can't touch the owner's account or sessions.
- **Dashboard:** Settings → Team tab (owner/superadmin): seats card, add-member form (name/email/role), member list with 🔑 reset / ⏸ toggle / 🗑 remove; team members get a normal role-driven workspace.
- **Tests:** `test_team.py` **46/46** (overlay, RBAC, scoping, role-change revocation, disable/enable, reset, seat cap on business plan, duplicate email, self-service isolation, remove). Pitfalls: PHP functions inside switch cases aren't hoisted (move helpers to global area); rig IP 127.0.0.1 trips 429 lockout across repeated runs → clear `auth_attempts` in tests + run_all reset.
- **Deploy:** ftp_api_p45.py + ftp_dash.py; rig both-copies sync; wired into run_all (51 suites). Skill ref `sa1-fullsite-v20.md`.

## SA1-fullsite-v21 (2026-08-12): Application security hardening (panel+API, no version bump)
User: "Take the necessary steps to improve the application security".
- **Audit** (1.29 MB API): already strong — bcrypt, 100% prepared statements, no eval/shell_exec/unserialize, random_bytes tokens hashed at rest, login/OTP/register/resend throttling, admin-only impersonation (30-min TTL), upload whitelist + random names, REMOTE_ADDR-only client_ip, redacted payloads.
- **API fixes:** (1) **CRLF/header injection** — smtp_send/mail_fallback interpolated user input into SMTP headers; new `mail_hdr()` strips CR/LF/NUL + caps length at the send boundary; stored contact/register names also stripped (log-poisoning). (2) **newsletter (≤6/IP/10m) + contact (≤5/IP/10m) throttles** — were unthrottled spam amplifiers to admin inbox. (3) **register cap was INERT** (used recent_fails = failed-rows-only, but register only records successes); new `recent_any()` counts all attempts by kind → register ≤8/IP/hr + ≤4/email/hr; resend-otp cap made real (≤3/email/10m). (4) Security headers: `CSP default-src 'none'; frame-ancestors 'none'; sandbox`, `X-Permitted-Cross-Domain-Policies: none`, `Cross-Origin-Resource-Policy: cross-origin`, `Cache-Control: no-store`. (5) Expired-token pruning (1/64 mints). (6) `app-setup` key compare → hash_equals. (7) Body-size cap 2MB → 413.
- **Dashboard XSS:** `rowItem()` + `kv()` helpers interpolated user data raw (ticket desc/subject, staff names, payment refs). Helpers now esc() title/sub/k/v by default (`raw` opt-out for HTML callers — 4 kv callers fixed, inner data escaped); unwrapped 4 pre-escaped rowItem args (double-escape); fixed `x.reported`/`x.prio` sinks in `right` args; audit of remaining `${}` interpolations (numbers/static/translated — safe).
- **Pitfalls:** `recent_any` with email_max=0 = always blocked (`0>=0`) — non-positive max must mean "no limit"; sqlite3 CLI + PHP WAL → SQLITE_BUSY silent failure → `-cmd '.timeout 5000'` + returncode check; kv raw callers missed on first scan (4th HTML caller `Total outstanding` found via balanced-paren scan).
- **Tests:** `test_security.py` **26/26** (headers ×7, CRLF contact/register, length cap, throttles via pre-seeded auth_attempts — no SMTP hammering, body 413, expired token 401, app-setup 403, no user enumeration, login still works) → regression **2935/2935, 0 failed** (52 suites).
- **Live:** API 1,293,147 B + dashboard 1,016,671 B deployed; curl probes: headers ✓, app-setup 403 ✓, 413 ✓, OPTIONS 204 ✓; browser E2E: home/compliance/taxes render, 0 JS errors, no literal `<b>` in TDS. Live DB untouched (non-mutating probes only). Skill ref `sa1-fullsite-v21.md`.
## SA1-fullsite-v21b (2026-08-12): HTML/static-site security audit (round 2, same session)
User: "What about html files?" → audited ~40 static pages + .htaccess + landing JS.
- **Clean:** no http:// mixed content; no exposed sensitive files (.git/.env/config.php/backups all 404; api/index.php.bak 405 = router not leak); landing JS escapes user input (chat.js escapeHtml, tools.js numeric-only); robots/sitemap fine.
- **Fixed:** (1) **CSP for HTML pages** via .htaccess (non-API only) — default-src 'self' + fonts/blob/connect https + object-src 'none' + frame-ancestors 'none' + upgrade-insecure-requests; added X-Permitted-Cross-Domain-Policies: none + CORP same-origin. (2) **PITFALL:** Apache `Header always set` OVERRIDES PHP headers — first deploy stripped API's strict `default-src 'none'`/CORP cross-origin → wrapped HTML headers in `<If "%{REQUEST_URI} !~ m#^/api#">`. (3) Blog article CSP override (PHP-rendered HTML would be blanked by default-src 'none'). (4) Reverse tabnabbing: dashboard NRB listing link + rel="noopener noreferrer". (5) Dead trycloudflare tunnel links in ai-caretaker.html/faq.html → live dashboard URL.
- **Verify:** live headers both sides correct; browser landing/dashboard/superadmin 0 console errors + 0 CSP violations; login + views render.
- **SA1-fullsite-v21c ✅ (2026-08-12, branch `superadmin-panel`)** — **Restore `web/faq.html` + `web/ai-caretaker.html`** (v21b clobbered them with stale unversioned copies). Caught by `test_superadmin` (`faq.html version 3.66` FAILED). v21b's "dead tunnel link" edit was applied to a stale deploy copy then whole-file synced into the repo, dropping `v=3.66` assets, GA4, `data-page`/`data-cms` hydration hooks (CMS edits stopped rendering), manifest link + breadcrumb JSON-LD; added wrong `../pwa/icon-192.png`. Restored from `d834bda` (v12 last-good; no later content commits touched them); markers back 56/55, zero tunnel links. Deployed live; curl-verified 56/54 markers. Lesson: never sync whole files from deploy copies — apply minimal patches; the version-marker assertions are the tripwire.
- **SA1-fullsite-v22 ✅ (2026-08-12, branch `superadmin-panel`, no version bump)** — **Rent-due push notifications** (user: "What's Next"). Digest emails chase tenants; push subs live on owner devices → new proactive owner alert. API: `rent_due_push_data()` (owners with ≥1 push device; invoices→leases→units→properties by `sub_email`, payments subtracted, month classification overdue/due_soon/upcoming-with-lookahead default 1 max 6; paid/far excluded); action `app-rent-due-push` (service X-Service-Key or recon user; `send=0` dry-run / `send=1` push per owner `৳X overdue · ৳Y due this month · ৳Z due next month` → dashboard; same gates as emails — `mail_switch('rent_reminders')` + `notify_ok('notify_rent')` → suppressed, device NOT removed; 404/410 dead endpoints removed; `platform_meta.last_rent_due_push` + audit). Dashboard: recon panel `🔔 Push owners` button + `pushRentDue()` toast + footer `push:` last-run; `app-collections-summary` returns `last_push`. Tests `test_rentdue.py` **26/26** (auth guards, exact buckets 12000/6000/9000, paid/far exclusion, lookahead=0, send path → 404 → removed, master/per-user suppression, meta written, cleanup). Regression **2961/2961, 0 failed (53 suites)** incl. v21c fix. Live: API 1,298,806 B + 60/60 files deployed; service dry-run 200 ok targeted 0 (pristine DB); browser: function + onclick live, UI dry-run toast works, 0 JS errors. Pre-existing (untouched): `loadRecon` parallel Promise.all flakes ~2/3 on live (SQLite concurrent empty body) → recon falls back to local data. Cron: add `/api/app-rent-due-push` with `X-Service-Key` + `{"send":1}` to the cPanel collections cron. Detail: `docs/sa1-fullsite-v22.md`, skill ref `sa1-fullsite-v22.md`.
- **SA1-fullsite-v23 ✅ (2026-08-12, branch `superadmin-panel`, panel-only — no version bump)** — **Superadmin panel: page header folded into the table toolbar** (user: "remove page-head, head-actions inside tbl-bar, pageSub.sub under tbTitle.tb-title, c-name click opens drawer"). `render()` no longer emits `.page-head`; `pageHead(sub,actions)` stores into `STATE._sub/_actions`; the **primary table's `tbl-bar`** now renders `<div class="tbTitle">` (title = page label, `.tb-sub` = subtitle on the next line) + `.head-actions` inline (verified they persist through filter/sort/pager re-renders — `tblBar` re-emits the block when `k===STATE._primary`, chosen on the first table of each `render()`). Multi-table pages (Onboarding 3 tables) host the header in the first table only (bug fixed en route: a DOM-existence stale-primary check misfired when several `tblPanel` calls were evaluated in one expression before any bar landed in the DOM — removed, `render()` reset suffices). **Non-table pages** (CMS, Settings, White-label, Health, API) get a `pageBar()` (tbl-bar-styled title/sub/actions) prepended to `pageBody`; API sub-view tables (`tkeys`/`apius`) + the drawer-embedded `aptb` are `noHead:true` so the API page's persistent pageBar is the single header. **c-name → drawer**: delegated click listener triggers the row's first `button.btn-ghost[onclick*="Detail("]` (fallback `Form(`) — the same drawer the 👁 View ghost opens; destructive handlers (Delete/Rotate/Toggle) are never matched (ledger rows with only 🗑 and tenant-key rows with only 🔄/✎/🗑 are safe no-ops); rows without drawer ghosts (dash/audit/misled/etc.) are untouched. Verified live: Subscribers/Onboarding/Tickets/Packages/Settings/API pages — no `.page-head`, header in bar, sub under title, actions in bar, c-name click opens detail drawers (Subscriber #124, Starter package), 0 JS errors; test_superadmin **511/511**, inline JS node --check clean.
- **SA1-fullsite-v24 ✅ (2026-08-12, branch `superadmin-panel`, no version bump)** — **Listings v3 — photo-first cards, filter sidebar, favorites, featured strip, detail modal** (user: "improve krtaker.com/listings.html … follow bproperty/bdhousing/rents/banglatolet/tolet.bd/bdtolet/toletbd.app/thetolet"). Reference-site features adopted: photo cards (16:9, hover zoom, SVG building placeholders), price/type/location/size/beds/furnishing/availability filters (sidebar desktop → drawer mobile), price presets (<30k/30–60k/60k–1L/>1L), sort (featured/newest/rent↑↓/size/৳/sqft), grid↔list toggle, ♥ favorites (localStorage + Saved-only filter + counter), ⭐ featured strip, active-filter chips + Clear all, load-more pagination, **detail modal** (photo, spec grid, description, amenities, per-unit list with per-unit WhatsApp, WhatsApp/call/email contact, share + copy-link), URL state sync (`?type/city/min/max/beds/sort/view/q`) + `?pid=` deep-link auto-open, debounced search incl. amenities, bilingual EN/BN, JSON-LD CollectionPage, empty states. **API enrichment** (additive): listings endpoint now returns `address/photo/description/featured/created_at` + per-unit `beds/baths/furnished` + `amenities[]` (icon+name) + `rent_per_sqft`/`total_units`; idempotent migrations add those columns; CRUD_FIELDS extended so the panel can edit them. **Live demo seed** (via app-crud, idempotent, 35 ops): 4 properties enriched (address/description/photo/featured/created_at), 8 vacant units created (U-011..U-018, realistic rents ৳28k–95k, beds/baths/furnished), 24 amenities; 5 SVG building placeholders added to deploy_landing.py. Verified live: cards/photos/badges, featured strip, all filters + URL state, list view, favorites persist, modal + WhatsApp per unit, `?pid=` deep-link, search, 0 JS errors; node --check clean; v=3.66 preserved (test_superadmin version gate). Pitfalls: template-literal string-concat mixups render literal `'+T(...)+'` text (badges) — use `${}` interpolation inside literals; chip onclick strings must use single quotes inside double-quoted attributes; `fmt('∞')` → ৳NaN (build labels with ternaries); run_all is single-instance — never run a suite concurrently or you get SQLITE_BUSY empty-response cascade.
